---
date: 2026-07-23
tags:
  - 每日归档
  - 对话记录
id: know-2026-07-23
---
# 2026-07-23 每日对话重点归档

## 一、完成的重要任务

### 1. Hermes Gateway SSL 证书修复验证

- **背景**：Gateway 进程（PID 15505）持续运行中，但界面显示"Gateway shutting down"中断提示
- **排查过程**：
  - 确认 Gateway 主进程仍在运行：`hermes_cli.main gateway run --replace`
  - 确认 SSL 修复代码已写入 `gateway/run.py` 第 38-44 行
  - 修复逻辑：通过 `certifi.where()` 获取 CA 证书路径，设置 `SSL_CERT_FILE`、`REQUESTS_CA_BUNDLE`、`CURL_CA_BUNDLE` 三个环境变量
  - 检查 `hermes.log` 近 200 行，无 SSL/飞书相关报错
- **结论**：Gateway 实际运行正常，SSL 修复已生效，中断提示为瞬时网络或服务重启导致
- **伴随进程**：`mcp_fetch_server.py` (PID 15515)、`mcp_stdio_watchdog.py` (PID 15514)

### 2. Obsidian 知识库孤立文档分析与修复

- **孤立文档发现**：共 5 个文档未被其他文档引用
  - `Daily/2026-07-18.md`
  - `Daily/2026-07-19-归档.md`
  - `Daily/2026-07-21-归档.md`
  - `Daily/2026-07-22-归档.md`
  - `00_System/Index.md`（导航索引，本身不需要被引用）
- **断链发现**：共 12 个断链，分布在 6 个文件中

## 二、待办事项

| 序号 | 事项 | 状态 |
|------|------|------|
| 1 | 确认飞书消息收发是否正常（SSL 修复后） | ⏳ 待验证 |
| 2 | 修复孤立文档的双向连接 | ✅ 已完成 |
| 3 | 修复断链 | ✅ 已完成 |

## 三、关键决策与结论

1. **SSL 修复方案有效**：在 `gateway/run.py` 中前置设置 `SSL_CERT_FILE` 等环境变量，解决了 macOS LibreSSL 不捆绑 CA 证书导致的 `SSLCertVerificationError` 问题。该方案已在代码中落地。

2. **Gateway 中断提示非致命**：进程实际仍在运行，日志无报错，中断提示可能为瞬时网络波动导致。

3. **知识库需要定期维护**：发现 5 个孤立文档和 12 个断链，说明日常归档后缺少双向链接的维护流程。

## 四、发现的问题与解决方案

### 问题 1：SSL 证书验证失败（历史遗留）
- **现象**：`web_search`、`mcp__fetch__fetch`、`pip install` 均报 `SSLCertVerificationError`
- **解决方案**：`gateway/run.py` 第 38-44 行，通过 `certifi.where()` 注入 CA 证书路径到环境变量

### 问题 2：Obsidian 孤立文档
- **现象**：5 个归档文档未被其他文档引用，图谱中孤立
- **解决方案**：
  - 为每个孤立归档文档添加末尾"## 🔗 相关笔记"部分
  - 在 `00_System/Index.md` 的 Daily 区域添加链接
  - 在 `00_System/Knowledge_Rules.md` 中修正断链

### 问题 3：断链
- **现象**：12 个 `\[\[链接\]\]` 无法匹配到现有文件
- **解决方案**：
  - `\[\[01_Projects/INDEX\]\]` → 改为 `00_System/Index`
  - `\[\[Inbox\]\]` → 对应 `Inbox/` 目录，Obsidian 不索引目录，移除
  - `\[\[文档A\]\]`、`\[\[文档B\]\]`、`\[\[文档名\]\]`、`\[\[链接\]\]`、`\[\[技能名\]\]`、`\[\[相关文档1\]\]`、`\[\[相关文档2\]\]` → 移除（`Knowledge_Link.md` 中的示例占位符）

## 五、Obsidian 知识库深度修复

### 1. 技能笔记桥接（7 篇新笔记）

为消除技能名称与 Obsidian 笔记之间的断链，创建了以下笔记：

| 笔记名 | 对应技能 | 路径 |
|--------|----------|------|
| AI 工具调研方法论 | `ai-tool-research` | `04_AI/` |
| AI 视频生成管线设计与质量优化 | `ai-video-pipeline` | `04_AI/` |
| ComfyUI 本地文生图 + 视频生成 | `comfyui-image-gen` | `04_AI/` |
| FFmpeg 命令行视频处理 | `ffmpeg-video-production` | `04_AI/` |
| FFmpeg 视频制作管线 | `ffmpeg-video-production` | `04_AI/` |
| Hermes Agent 自动化工作流 | 核心功能 | `04_AI/` |
| 本地 AI 模型部署 | `local-ai-model-setup` | `04_AI/` |

每篇笔记均包含 YAML frontmatter（tags、date）、核心内容概述、相关笔记链接。

### 2. 最终验证结果

| 指标 | 数值 |
|------|------|
| 总文档数 | 55 |
| 总链接数 | 283 |
| 独立链接数 | 60 |
| **断链数** | **0** ✅ |

### 3. 图谱分析

从用户截图观察：
- **中心枢纽**：中间大深色节点（`00_System/Index.md`）连接大量文档
- **绿色集群**：左下角为 `04_AI/` 目录下的技术文档
- **右侧密集区**：`Daily/` 目录下的归档文档群
- **孤立节点**：边缘黄色/橙色小点（6 个无入链文档）

## 六、系统状态快照

| 组件 | 状态 | 备注 |
|------|------|------|
| Hermes Gateway | ✅ 运行中 | PID 15505, SSL 修复已生效 |
| oMLX | ✅ 运行中 | PID 738, 端口 8000 |
| ComfyUI | ✅ 运行中 | MPS 加速 |
| MCP 服务 | ✅ 运行中 | mcp_fetch_server + watchdog |
| Obsidian 知识库 | ✅ 健康 | 55 文档, 283 链接, 0 断链 |

## 七、知识库维护建议

1. **每日归档后自动添加双向链接**：每次写入 `Daily/` 目录的归档文档，自动在末尾添加相关笔记链接
2. **定期运行断链检查**：建议每周运行一次孤立文档和断链扫描脚本
3. **技术文档需同步创建**：当代码或技能中提到 `\[\[技能名\]\]` 时，确保 Obsidian 中有对应笔记

## 🔗 相关笔记

- Daily_Review
- Knowledge_Management
- Knowledge_Rules
- Hermes_Workflow
- Execution_Safety
- Agent_Core
- Task_Tracking
- [[本机系统资产全景盘点]]
