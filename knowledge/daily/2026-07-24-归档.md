---
id: know-2026-07-24
---
# 2026-07-24 每日对话归档

## 一、完成的重要任务

### 1. 微博 cron 脚本卡死根因排查与修复
- **问题**：微博发布 cron 任务单次执行耗时约 2 小时，远超正常时间
- **根因**：cron job 的 context compaction 摘要中嵌入了约 18 万字符的无限循环文本 `as the latest message ...`，导致 agent prompt 溢出并陷入死循环
- **触发机制**：cron prompt（约 25 行）+ 原始技能内容（277 行）= 300 行，超过 context compaction 阈值
- **修复动作**：
  - 删除损坏的日志文件 `~/.hermes/cron/output/3f4a7ce9408a/2026-07-24_08-57-48.md`
  - 将 `weibo-publish` 技能文档从 277 行精简至 90 行（减少 67%），彻底规避阈值超限问题
  - 确认小红书任务（`15e58e1e0163`）的 `skill` 字段配置错位（当前为 `weibo-publish`），需后续修正

### 2. Unity CLI 基础验证
- **环境**：macOS，Unity 版本 `6000.5.3f1`，实际路径 `/Applications/Unity/Hub/Editor/6000.5.3f1/Unity.app/Contents/MacOS/Unity`
- **验证结果**：`-batchmode`、`-nographics`、`-projectPath`、`-executeMethod`、`-logFile`、`-quit` 均可正常解析，执行测试返回 `EXIT CODE: 0`
- **注意事项**：
  - Editor 脚本必须在 `Assets/Editor/` 目录下且需 `.asmdef` 限定
  - 调用方法必须是 `public static`
  - 无头服务器环境需预先激活许可证（`UNITY_LICENSE` 环境变量）

### 3. 新技能创建
- **`unity-editor-cli`**：Unity Editor 脚本命令行自动化流程，覆盖参数使用、项目结构、常见坑位
- **`unity-file-driven`**：纯代码驱动 Unity 项目开发方法论，确立文件驱动开发规范

## 二、待办事项

- [ ] 深入评估 Unity CLI 是否具备直接生成游戏场景和素材的能力（需结合 `AssetDatabase`、`BuildPipeline` 或程序化生成 API 验证）
- [ ] 小红书 cron 任务（`15e58e1e0163`）的 `skill` 字段配置错位问题，需修正为正确的发布技能
- [ ] **Bonsai -27B 模型查证**：网络 SSL 证书问题导致所有搜索失败，需等网络恢复后重新查询 HuggingFace 及开源社区
- [ ] 检查 oMLX 本地 LLM 服务器 SSL 证书问题（多个 cron 监控任务反映）

## 三、关键决策和结论

1. **cron 任务耗时根因已定位**：compaction 摘要污染是核心问题，技能精简后预期不再触发
2. **Unity CLI 完全支持无头批处理**：版本 `6000.5.3f1` 支持完整参数链，可用于自动化场景/素材生成
3. **Bonsai -27B 模型**：在训练数据中未找到对应知名模型，名称可能有误或为小众/内部模型，需用户提供更多信息源
4. **网络 SSL 证书异常**：今天所有外部搜索（DuckDuckGo、HuggingFace、Google、Yahoo 等）均因 SSL 证书验证失败或超时而失败

## 四、发现的问题和解决方案

| 问题 | 解决方案 | 状态 |
|------|----------|------|
| cron 任务执行 2 小时 | 删除损坏日志 + 精简技能至 90 行 | ✅ 已修复 |
| Unity CLI 路径不确定 | 动态探测 `Unity.app` 实际位置 | ✅ 已确认 |
| Bonsai 模型信息缺失 | 网络不通，待恢复后重新查询 | ⏳ 待办 |
| 小红书 skill 配置错位 | 记录任务 ID，待重建时修正 | ⏳ 待办 |
| 全网络 SSL 证书失败 | 需检查系统证书链或代理设置 | ⏳ 待办 |

## 五、相关文件

- `~/.hermes/skills/weibo-publish/SKILL.md` — 已精简至 90 行
- `~/.hermes/skills/unity-editor-cli/SKILL.md` — 新建
- `~/.hermes/skills/game-dev/unity-file-driven/SKILL.md` — 新建
- `/tmp/test-unity-cli/` — Unity CLI 测试项目
