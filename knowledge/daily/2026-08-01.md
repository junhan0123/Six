---
id: know-2026-08-01
---
# 2026-08-01

## P9 Step 1 完成

- goal_decision_engine.py（245 行，极瘦）：单入口 ingest() + 五类判定 A/B/C/D/E + _ai_judge mid 带降级
- config.py：FEATURE_GOAL_DECISION 默认 false，双门控
- server.py：try/except 接线，create → submit_goal + 早退；propose → pending_proposal；resume → 通知事件
- 测试：10/10 PASS（五类判定 + 100 问答=0 建 + 100 长任务>99% 建 + 副作用零泄漏 + 崩溃降级）
- commit: 5251776

## 下一步方向（P9 Step 2）预告

Step 2 目标：GDE 与 Policy 协作，修复发现 A（session 批准状态泄漏）和发现 B（plan_goal 非确定性）。
核心改动：policy_engine._session_approved 改为 per-goal 隔离，GDE 决策结果携带 goal_id 传给 Policy。

## P9 Step 2 完成

- policy_engine：_session_approved set → per-goal dict[int, set]（修复发现 A 状态泄漏）
- policy_engine：新增 pre_approve_tools(goal_id, tools) / approve_in_goal(goal_id, tool)；evaluate / request_approval 签名加 goal_id，仅查该 goal 批准集
- agent_runtime：_execute_task 透传 goal_id 给 evaluate + request_approval
- goal_decision_engine：submit 成功后按标题启发式调 pre_approve_tools（Step 2 协作，失败不阻塞）；_pre_approve 为模块级启发式
- config：FEATURE_GOAL_DECISION 默认改 True（随 Step 2 一起激活，与 FEATURE_AGENT_RUNTIME 协同）
- 测试：12/12 PASS（新增 test_session_approved_per_goal_isolation / test_pre_approve_reduces_confirm）；端到端验证 submit→per-goal 预批准生效且隔离
- commit: 3678c73

## 下一步方向（P9 Step 3?）预告

- 发现 B（plan_goal 非确定性）仍待处理：信任反转 default-deny（非只读工具默认 confirm，未知 never）
- per-goal 预批准为启发式（关键词命中），未命中仍走 confirm 弹窗，符合「副作用必经 Policy」
- GDE 行数 245→267（新增 _pre_approve 启发式，仍极瘦，非第二个 Planner）
