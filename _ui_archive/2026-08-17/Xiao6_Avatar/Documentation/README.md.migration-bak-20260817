# ZhuangZhou Avatar System v1.0 — 资源规范与架构基础

> Phase 25 交付物。本目录是「数字人 Avatar 架构基础」，不是完整数字人、不是 Live2D、不是 3D 模型、不生成大量动画资源。

## 目标

为庄周 AI OS 建立可扩展的 Avatar 架构基础：

- **统一状态权威**：复用 `avatar-state.js` 的 `AvatarState`（单一来源，禁止另立 STATES/META）。
- **Avatar Controller**：AI 状态 → Avatar 状态 → 动画 的统一调度器（`Core/avatar-controller.js`）。
- **资源加载接口**：按 `format` 委派 `AvatarAssets.urlFor`，支持未来 `svg / png / webp / webm / frames / sprite / live2d / 3d`，当前不绑定具体资源。
- **预留层**：动作层（Gesture）、表情层（Face/Emotion）、环境效果层（Environment）以注册钩子形式预留。

## 目录规范

```
ZhuangZhou_Avatar/
├── Core/                 # 基础框架（控制器，唯一可执行逻辑）
│   └── avatar-controller.js
├── Body/                 # 身体姿态资源（按状态分目录，当前空，未来放 PNG 序列帧/Sprite）
│   ├── Idle/ Listening/ Thinking/ Planning/ Executing/
│   └── Speaking/ Learning/ Warning/ Error/
├── Face/                 # 面部资源
│   ├── Eye/ Mouth/ Emotion/
├── Gesture/              # 手势资源
│   ├── Point/ Explain/ Confirm/ Welcome/
├── Environment/          # 环境效果资源
│   ├── Aura/ Particle/ Hologram/ VoiceWave/
├── Animation/            # 动画资源
│   ├── State/ Transition/ Timeline/
├── AI_Binding/           # AI 状态 → Avatar 绑定（配置，非状态系统）
│   ├── state_map.json      # 状态绑定（权威：AvatarState）
│   ├── emotion_map.json    # 情绪 → 表情 预留
│   └── action_map.json     # 意图 → 手势 预留
└── Documentation/        # 本文档
```

## 状态权威（关键纪律）

- **唯一权威**：`avatar-state.js` 的 `AvatarState.STATES` + `AvatarState.META`。
  Phase 25 已将其扩展至覆盖 `IDLE / LISTENING / THINKING / PLANNING / EXECUTING / SPEAKING / LEARNING / WARNING / ERROR`（外加既有 `WAITING / COMPLETED / OFFLINE`）。
- **禁止重复状态系统**：`avatar-controller.js` 不定义任何平行 STATES/META；它只消费 `AvatarState` 并把「AI 信号 → Avatar 状态」表达为 *绑定配置*（state_map.json），绑定 ≠ 状态系统。
- **零侵入集成**：Controller 仅通过 `MutationObserver` 观察既有管道（`#aiCore[data-state]` / `[data-voice]`、`#voiceOrb[data-voice]`、`FinalVoice.getVoice()`），不修改 Agent Runtime / Planner / Executor / EventBus / Memory 核心，也不修改 Voice Orb / AI Core 组件。

## 运行时数据流

```
后端 SSE ─▶ app-state ─▶ ai-core.js ─▶ #aiCore[data-state]  (任务/智能体态)
                                      └ #aiCore[data-voice]
语音输入 ─▶ voice-core.js ─▶ #voiceOrb[data-voice] / FinalVoice.getVoice()  (语音态)

ZZAvatarController (观察者)
   ├─ 读 signals: { voice, task }
   ├─ 按 state_map.json priority 解析 → 规范 Avatar 状态
   └─ 派发:
       ├─ document.documentElement[data-avatar-state] + --avatar-color   (CSS 钩子)
       ├─ AvatarRenderer.render(el, state, color)   (若绑定 [data-avatar] 元素)
       ├─ ZZAvatar / ZZAvatarScene.setState()        (守卫转发，绝不阻断)
       └─ 预留层 registerLayer('action'|'expression'|'environment', fn)
```

## 资源加载接口

```js
// 支持未来格式；当前不绑定具体资源，缺省返回 null → 渲染器降级 SVG/CSS 脸
ZZAvatarController.resolveResource('THINKING', 'png');   // → AvatarAssets.urlFor(...) 或 null
ZZAvatarController.resolveResource('SPEAKING', 'live2d');// 未来格式占位
```

## 验证

见 `final/_audit/PHASE25_AVATAR_VERIFY.md`。核心断言：AI 状态变化驱动 Avatar 状态、状态切换无错误、无重复状态系统、不影响 Voice Orb / AI Core、UI 无 console 错误。
