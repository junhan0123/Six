# P3-BASELINE · ZhuangZhou Core Runtime 基线（Phase 0 · VERIFY BEFORE CHANGE）

> 本文件基于 **实测源码** 生成（非依赖 P0–P2 历史报告）。所有结论均标注 `file:line` 或可复现的运行时证据。
> 红线：UI 永久冻结；不改动产品语义；不删除/降低测试；不 fake module；不 mock 真实 Runtime 执行链；
> 所有执行必须经统一 Runtime；不创建第二套 Runtime / Goal / Task / Event / Memory 系统。

---

## 1. 当前任务生命周期（Task Lifecycle）

**权威状态源 = SQLite `tasks` 表**（`db.py:39-42` 建表，列：`id,title,step,current_step,total_steps,status,step,note,goal_id,created,updated`；`_migrate_tasks` 在 `db.py:597` 补齐 `steps/note/goal_id`）。

状态集合（由 `tasks.py` 实际写入）：
- `open` —— 新建（`create_task`，`tasks.py:23`）
- `running` —— 执行中（`agent_runtime._run_goal` 在 `_emit_task_domain("TASK_STARTED"/"TASK_RUNNING")` 前将任务置 running；`goals.py`/`tasks.py` 未直接 SET running，由 agent_runtime 在内存编排时标记，落库见 §3）
- `done` —— 完成（`complete_task(...,success=True)` → `status='done'`，`tasks.py:96-115`；`_run_goal` 成功分支 `update_task_step(tid, status="done")`，`agent_runtime.py:269`）
- `failed` —— 失败（`complete_task(...,success=False)` 或 `_run_goal` 失败分支 `update_task_step(tid, status="failed")`，`agent_runtime.py:272`）
- `paused` —— 暂停（由 `update_task_step(status="paused")` 支持；`_run_goal` 把 `paused` 与 `open/running` 一并视为"未完"参与聚合，`agent_runtime.py:446`）

**结论**：单一权威 Task 状态源 = `tasks.status`。无第二套 Task 状态库。

## 2. Goal 与 Task 的关系

- **Goal**（`goals.py`）：用户意图/项目/长期方向。`goals` 表 `status` ∈ {active, paused, completed, archived, failed, max_steps_exceeded, blocked_by_policy}（`goals.py:220-223`）。
- **Task** 经 `tasks.goal_id` 软外键归属某个 Goal（`tasks.py` docstring；`recalc_progress` 按 `goal_id` 聚合子任务完成比例，`goals.py:380-434`）。
- **Goal 级 FSM**（`round_status`，`goals.py:112`）是**编排轮次状态机**，取值 {none, planned, running, observing, evaluating, COMPLETE, CONTINUE, REPLAN, BLOCK, FAIL}（`goals.py:177-180`）。它是 Goal 多轮/重规划的**衍生（DERIVED）**状态，不是独立的 Task 状态系统——同一 Task 的权威状态仍是 `tasks.status`。
- **映射关系（AUTHORITATIVE / DERIVED）**：
  - `tasks.status` = Task 权威状态（open→running→done/failed/paused）
  - `goals.round_status` = Goal 编排轮次状态（DERIVED，由 `_run_goal` 写入，`agent_runtime.py:213/232/283/286`）
  - `goals.status` = Goal 生命周期（DERIVED from 子任务聚合 + 终态判定）

## 3. Task 是否持久化 / 重启后能否恢复

- **持久化**：是。所有 Task 变更经 `db_conn()` 落 SQLite（`tasks.py` 全程使用 `db_conn()`）。
- **重启恢复**：`server.py:905` 在 `main()` 启动阶段调用 `recover_tasks()`（`tasks.py:165-177`）：把 `status='running'` 的中断任务翻回 `open`，使其可被续跑（**不会自动重执行**，仅状态回滚 + 由 ACI 注入"未完成任务"上下文）。
- **证据**：`tasks.py:165-177` `recover_tasks()` 仅执行 `UPDATE tasks SET status='open' WHERE status='running'`。

## 4. Runtime 是否支持长任务

- **同步执行模型**：`_run_goal`（`agent_runtime.py:149-336`）在一个调用内同步遍历 revision 任务、`_execute_task` 同步执行（阻塞式 `attempt in range(_MAX_RETRIES+1)` 循环，`agent_runtime.py:542`）。
- **无跨 HTTP 请求的 Task 级 WAITING/RESUME 状态机**：`tasks.status` 含 `paused` 但运行时未实现"暂停→异步恢复"的挂起/唤醒机制；approval 等待仅发 `AGENT_WAITING` 事件（`agent_runtime.py:547/603`），在默认 Chat（NONE 模式）下 `confirm` 工具直接经 policy 通道执行，不真正阻塞挂起。
- **结论**：当前架构**不支持**"一次任务生命周期跨越多次 HTTP 请求"的 WAITING/RESUME；长任务 = 单次调用内多步同步执行 + 轮次（round）循环重规划。属 **O 类（已知能力缺口/设计边界）**，非 A 类缺陷。详见 `P3-LONG-RUNNING-TASK.md`。

## 5. retry 当前在哪里实现

- **位置**：`agent_runtime._execute_task`（`agent_runtime.py:542-571`）。
- **上限**：`_MAX_RETRIES = 3`（`agent_runtime.py:54`）→ 最多 4 次尝试。
- **可重试类**：仅 `network`/`timeout`/`file`（有替代工具时）重试（`agent_runtime.py:557-567`）；`unknown/permission/tool/validation` 等**立即终止**，不重试（避免无意义等待与危险重复）。
- **退避**：`network` 类 `time.sleep(0.1*(attempt+1))` 短退避（`agent_runtime.py:560`）。

## 6. failure 当前在哪里处理

- **分类**：`_classify_error(e, tool)`（`agent_runtime.py:557`）→ `ERROR_TAXONOMY`（18 类，`agent_runtime.py:731-736`）。
- **致命类（不重试/拒绝）**：`_FATAL_ERROR_CATEGORIES`（`agent_runtime.py:739-744`）含 unknown/policy_blocked/injection_blocked/skill_error/computer_error 等。
- **计数**：`_consecutive_failures`（换工具用，`agent_runtime.py:53`）；**无** `clear_queue` 实现（P0.5 C2 已知 backlog）。
- **结果归一**：`_execute_task` 返回 `{ok, blocked, rejected, error, category, attempts, ...}`（统一结果契约，与 `capability_runtime.CapabilityResult` 同构）。

## 7. EventBus 是否承担状态持久化

- **否**。EventBus（`eventbus.py`）是**进程内发布/订阅脊柱**，承载领域事件（`DOMAIN_EVENT_NAMES`）与系统事件（`SYSTEM_EVENT_NAMES`），**不持久化业务状态**。
- **状态持久化在 SQLite**（`tasks`/`goals`/`tool_audit`/`chat_log`/`memories` 等）。
- EventBus 单例 `bus = EventBus()`（`eventbus.py:156`）；`publish_domain`/`publish_system` 强制单一事件名来源（`eventbus.py:225/285`）。

## 8. Memory 如何接收任务结果

- **审计级**：每次工具执行经 `execute_tool` → `sandbox.audit_tool()` 写入 `tool_audit` 表（`sandbox.py:129`，`db.py:211` INSERT）。这是**全量执行审计**来源（工具/参数/结果预览/风险/耗时）。
- **反思日志**：`ai_core/data/execution_reflections.jsonl` 记录 `execution_id, goal_id, tool, outcome`（用于 self-improvement）。
- **会话记忆**：`conversation_memories` 表沉淀对话摘要（`db.py:90-93`）。
- **缺失点（O 类）**：**无专用 "TaskResult → Memory 持久节点" 桥**；任务结果主要通过 `tasks.note` 字段 + `tool_audit` 留存，未自动写入 `memories` 事实图谱。详见 `P3-CONTEXT-MEMORY-INTEGRATION.md`。

## 9. Context 如何获得任务状态

- **入口**：`context.facade.build_context_prompt`（`context/facade.py:31`）是唯一 System Prompt 组装入口（Flags `FEATURE_CONTEXT_ENGINE` 控制新旧管线）。
- **任务状态注入**：`tasks.get_open_tasks(limit, goal_id)`（`tasks.py:160-162`）供 ACI 注入"未完成任务"上下文（`tasks.py` docstring 明确）；Goal 快照经 `goals.active_goals_snapshot`（`goals.py:444-458`，仅输出标题，进度脱敏）。
- **无反向写**：Context 只读消费 Task/Goal 状态，**不反向修改** `tasks.status` 权威状态（写入仅由 `tasks.py` API 与 `agent_runtime` 编排完成）。

## 10. WorldModel 如何获得任务状态

- **无独立 WorldModel 状态库**。"WorldModel" 由两个只读观测层实现：
  - `verification.py` 的 `VerificationLayer` / `PerceptionWorldModelObserver`（`verification.py:22/135`）——执行后校验（VERIFIED/UNVERIFIED），**只读、不产生新状态系统**。
  - Context 源层 `context/perception_source.py` / `context/goal_source.py` 等消费事件构造快照。
- **结论**：WorldModel = 只读观测 + Context 投影，不持有权威任务状态，无重复基础设施。

## 11. MCP / Skill / Capability 是否统一进入 Runtime

- **统一入口 = `ai_core.execution.run`（`_execution_run`）**（`ai_core/execution/api.py`）—— 所有默认 Chat 执行经此唯一 Policy 门。
- **声明真相 = `capability_os` registry**（`capability_os/registry.py` `is_known`/`tool_to_capability`）。
- **路由**：
  - 默认 Chat：`capability_runtime.execute` → `_execution_run`（`capability_runtime.py:159`）。
  - 复杂路径：`agent_runtime._execute_task` → 分支（skill:/external.mcp./computer/原生）→ 各自经 `execute_tool` 或 `capability_os.execute_capability` 或 `guard.plan/run`，最终仍汇入 `execute_tool`/`_execution_run`（统一执行内核）。
- **MCP**：`mcp_host/executor.py` `_authorize` 经 `policy_engine.evaluate(default_deny=True)`（`mcp_host/executor.py:38-69`），**复用 Policy Engine，无第二权限系统**。
- **Skill**：`skill:<name>` → `tools.execute_tool` → `skills.execute_skill`（返回指令包，非第二执行器）。
- **结论**：Skill/MCP/Native 经 Capability Registry → Capability Runtime → Policy/Permission/Approval → Executor 统一治理。详见 `P3-CAPABILITY-ECOSYSTEM.md`。

## 12. 是否存在重复基础设施

| 系统 | 数量 | 说明 |
|---|---|---|
| EventBus | 1 | `eventbus.bus` 单例 |
| Task 权威状态 | 1 | `tasks.status` |
| Goal 权威状态 | 1 | `goals.status` |
| Policy/Permission | 1 | `policy_engine` + `permission_guard.guard` |
| Executor | 1 | `tools.execute_tool`（MCP/skill/原生/电脑能力最终汇入） |
| Memory | 1 | `memories` 表 + `memory.py`；`memory_v2` 为 V2 演进（非重复，见 P4 roadmap） |
| Context | 1 | `context.facade` 门面 |

- **发现的衍生（非重复）状态**：`goals.round_status`（Goal 编排 FSM，DERIVED）。**非重复基础设施**。
- **事件重复发射（O 类，非执行重复）**：MCP 路径 `tool_started/tool_finished` 既由 `ai_core/execution/events.py`（包 `_execution_run`）又由 `mcp_host/executor.py:100/145` 发射；`COMPUTER_ACTION_PHASE` 双发布源。属事件级重复，不影响执行唯一性（见 `P2-EVENT-STATE-AUDIT.md` / `P3-AUDIT-TRACE.md`）。

## 13. 是否存在 legacy execution path

- **存在受控回退路径**：`FEATURE_CAPABILITY_RUNTIME=False` 时 `capability_runtime.execute` 回退到直接 `execute_tool`（`capability_runtime.py:159` 注释 + 回退分支）。这是**安全回退开关（kill switch）**，非"旁路"——OFF 时仍经 `execute_tool`（内含 Policy 钩子），且 ONE REQUEST = ONE PATH（ON/OFF 互斥，不双执行）。
- **结论**：legacy path 是显式、可审计、受 Feature Flag 控制的回退，非隐藏旁路。详见 `P2-FEATURE-FLAG-REPORT.md`。

## 14. 是否存在重复 executor

- **否**（见 §11/§12）。所有原生工具经 `execute_tool`（TOOL_FUNCS 分发）；MCP 经 `mcp_host.executor` 但授权仍走 `policy_engine`；电脑能力经 `permission_guard` → `computer_executor`；Skill 经 `execute_tool` → `skills.execute_skill`。**单一执行内核 `execute_tool` / `_execution_run`**。

## 15. 已知 C/D backlog（继承自 P0.5 / P1 / P2）

| ID | 分类 | 描述 | 影响真实 Runtime？ | 阻断 release？ |
|---|---|---|---|---|
| C1 | C（未实现） | `consecutive_failure_clear_queue`（连续失败清队列）未实现 | 否（仅清队列便利功能） | 否 |
| C2 | C（未实现） | `consecutive_failure_clear_queue` 测试依赖的功能 | 否 | 否 |
| D1 | D（环境） | ASR ffmpeg 环境差异 ×2（`test_asr_transcribe_disabled_without_key` / `test_status_default_all_disabled`） | 否 | 否 |
| O1 | O（设计边界） | 长任务无跨请求 WAITING/RESUME | 限制长任务形态 | 否（文档化） |
| O2 | O（审计缺口） | 无 (task_id, execution_id) 幂等去重账本 | 并发重入有重复执行风险 | 否（单调用安全；见 P3-C） |
| O3 | O（事件重复） | MCP/Computer 事件双发射 | 仅事件级噪声 | 否 |

---

## 基线裁定

- **A 类（真实产品缺陷）**：未发现。
- **B 类（陈旧错误测试）**：未发现。
- **重复基础设施**：未发现（Goal/Task/Event/Memory 各一，round_status 为衍生）。
- **统一 Runtime**：成立（默认 Chat 与复杂路径最终汇入 `ai_core.execution.run`）。
- **持久化/恢复**：成立（`tasks` 表 + `recover_tasks()`）。
- **已知缺口（O 类）**：长任务跨请求挂起、幂等去重账本、事件双发射——均文档化，不阻塞产品化。

→ 进入 P3-A … P3-M 设计与验证。
