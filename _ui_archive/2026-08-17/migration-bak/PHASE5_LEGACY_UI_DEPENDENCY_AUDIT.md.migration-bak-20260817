# PHASE 5 — LEGACY UI DEPENDENCY AUDIT

> 范围：Phase 3 遗留旧 UI（orb-ui / robot / gem-orb / liquid-orb / orb-pet）+ 其他旧 UI（root `app.js`、companion 壳等）。
> 方法：逐目录/逐文件静态依赖扫描（HTML `<script>`、JS `require`/引用、`electron` 加载、launcher、backend、package.json、preload、IPC、CSS、asset、runtime）。
> 分类：KEEP / ARCHIVE_SAFE / BLOCKED_BY_DEPENDENCY / UNKNOWN。

## 判定矩阵（桌面数字人 `desktop-avatar/`）

| 组件 | 被谁加载（active path） | 交叉引用 | 分类 |
|---|---|---|---|
| **dyna-orb** (`dyna-orb.html/js/voice.js`) | `electron/main.js` → `desktop-avatar/dyna-orb.html`（**活动桌宠窗口**） | 无（自包含）| **KEEP**（活动组件，5-state 契约载体）|
| **robot** (`robot.js`) | `desktop-avatar/index.html`（"数字人界面" 网页，feature `avatar-ui` 指向）+ `bridge.js` + `voice.js` | index/bridge/voice | **BLOCKED_BY_DEPENDENCY**（归档会破坏"数字人界面"页）|
| **gem-orb** (`gem-orb.html/js/voice.js`) | 仅自身 + `orb-ui-orb.js` | orb-ui-orb.js | **ARCHIVE_SAFE** |
| **liquid-orb** (`liquid-orb.html/js/voice.js`) | 仅自身 | 无 | **ARCHIVE_SAFE** |
| **orb-pet** (`orb-pet.html/js/voice.js`) | 仅自身 + `scripts/verify-speech-orb.js`（开发脚本）| 无 runtime | **ARCHIVE_SAFE** |
| **orb-ui** (`orb-ui.html` + `orb-ui-orb.js`) | 仅自身 + 被 gem-orb 引用 | gem-orb | **ARCHIVE_SAFE**（与 gem-orb 同簇）|

## 其他旧 UI / 壳

| 组件 | 位置 | 分类 | 理由 |
|---|---|---|---|
| **root `app.js` / `index.html`**（旧 Web 控制台）| `zhuangzhou-ui/` 根，`server.py` 路由 `/` → `index.html`+`app.js` | **KEEP** | 仍被 `/` 路由服务，自包含（无 orb/robot 引用，grep 零命中）；非孤儿，是受支持的 Web 控制台。 |
| **companion.html / event-bridge.js / runtime-visualization.js** | `zhuangzhou-ui/` 根 | **KEEP** | 被 `electron` 壳/测试引用；非孤立。 |
| **`G:/ZhuangZhou/electron`**（桌面伴侣壳 `zhuangzhou-desktop`, appId `com.zhuangzhou.desktop`）| 独立壳 | **KEEP** | 独立运行实体，不在本次活动/workspace 窗口路径。 |
| **`G:/xiao6-hub`**（Six Hub 呈现壳）| 独立壳 | **KEEP** | `appId`/品牌已 Phase 4 固化。 |
| **`scripts/verify-speech-orb.js`** | 开发校验脚本 | **KEEP（标注破损）** | 引用 `orb-pet`；因 orb-pet 已归档，该脚本失效，但非运行时组件，仅记录。 |

## 依赖扫描明细（BLOCKED 项）

- `desktop-avatar/index.html` 加载：`lottie.min.js` → `robot.js` → `bridge.js` → `voice.js`（即"数字人界面"页依赖 robot 簇）。**因此 `robot.js` 不得归档**。
- `electron/main.js` 仅加载 `dyna-orb.html` 与 `zz-space/index.html`；**未加载** gem/liquid/orb-pet/orb-ui/robot 任一。
- `electron/avatar-window.js` / `workspace-window.js` / `preload.js`：零 orb/robot 硬编码引用（除 dyna-orb 目标 URL）。
- `zz-workspace.js` / `app.js`：零 orb/robot 引用（grep 验证）。
- `dyna-orb.js`：零对 gem/liquid/orb-pet/orb-ui 引用（自包含活动组件）。

## 结论

- **ARCHIVE_SAFE 簇 = gem-orb + liquid-orb + orb-pet + orb-ui（共 11 文件）**：彼此仅互相引用 + 1 个开发脚本，无任何 active/served/runtime 入口引用。归档后活动路径（dyna-orb + robot/index.html + zz-space + root app.js）完整无缺。
- **robot.js → BLOCKED_BY_DEPENDENCY**：保留。
- **root app.js / companion 壳 / xiao6-hub / electron 伴侣壳 → KEEP**。
- 归档动作与清单见 `LEGACY_UI_ARCHIVE_MANIFEST.md`。
