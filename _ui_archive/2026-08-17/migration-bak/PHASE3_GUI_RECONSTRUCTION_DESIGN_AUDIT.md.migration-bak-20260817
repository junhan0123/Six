# PHASE 3 — GUI RECONSTRUCTION DESIGN AUDIT REPORT

> **STAGE 0 — READ ONLY / ZERO MODIFICATION**
> 生成时间：2026-08-17 · 审计范围：`G:\ZhuangZhou\zhuangzhou-ui` + `G:\xiao6-hub` + `G:\deepseek-harness`
> 纪律：本阶段**只读审计**，未修改任何 production / UI / CSS / HTML / JS / TS / Python / config / DSH / Xiao6Hub / ZhuangZhou Core 文件。**MODIFIED FILES = 0**。
> 所有"应修改"内容仅以 **PLANNED MODIFICATION** 形式记录，未执行。
> 继承：Phase 1（产品架构冻结）+ Phase 2（Voice Orb 收口）。STOP 后等待明确授权再进入 Implementation。

---

## 1. Executive Verdict

**PHASE 3 — DESIGN AUDIT COMPLETE · READ ONLY · ZERO MODIFICATION · PLAN READY · STOP.**

三仓库审计完成。核心结论：

1. **产品模型已冻结且正确**：ZhuangZhou = 单产品；Voice Orb（Primary Interaction）+ GUI（Advanced Workspace）= 两个 Presentation 层，共享唯一 Core（Runtime/Context/Memory/Decision/Policy/Approval/Lifecycle/Capability/Executor/Security）+ 基础设施 DSH。无三 Runtime。
2. **GUI 真实现状：当前 0 个可工作的 GUI 表面**。`zz-space`（唯一正确的 GUI 基础）在加载时**硬崩溃**（`zz-space.js:284` 对已被删除的 `#petOrb` 元素空引用）——距"可用 GUI"仅差一行 null-guard。其余 12 个 UI 表面要么冻结（root `index.html` Galaxy 套件，且其渲染器 `solar-system.js` 已被删，不可恢复）、要么孤儿/死（gem-orb/liquid-orb/orb-pet/orb-ui/chat-window/companion/mobile-app 等）。
3. **Xiao6Hub = 纯 Electron 壳 + 数据驱动功能网格 + 嵌入 DSH webview**。47 个 features.json 入口实为 37 个只读 `/api/*` 查询 + 少量 url/folder/cli。DSH webview 是**主要品牌泄漏向量**，必须替换为原生 Center Conversation。
4. **DSH Web 提供成熟 Workspace 模式库**（cordis 插槽组合外壳、流式增量 Markdown、Agent 活动泳道时间线、触发器/命令面板、原子化 keyed 工具视图、勾选门控风险确认、四态 StateDot）。这些**模式**值得原生采纳，**但 DeepSeek/DSH/DPH 品牌必须全部清洗**。
5. **GUI 应承担所有"可浏览/可配置/历史/复数"功能**（tasks/schedule/memory/knowledge/capabilities/settings/transcript/diagnostics/discovery），这些功能当前在死套件中**各自重复 3–5 份**，重建前须先去重为每功能单一归属。

**最终推荐 GUI 架构**：单 Electron 壳（`electron/`，接管 `gui/main.js` 中唯一的工作区窗口代码）→ 原生 Advanced Workspace（左导航 + 中对话/工作区 + 右上下文；命令面板 Cmd/Ctrl+K；Voice Orb 作为全局存在感浮层；DSH 仅作底层工作区底座经 HTTP 集成，绝不作为可见产品面）。

**最终 GUI 视觉方向**：Deep Black / Graphite 背景 + 克制青/teal 微光 + 大留白 + 软玻璃 + 低对比表面 + 强排版层级 + 极克制科幻细节。**高级 AI Operating System，而非科幻游戏 HUD。**

---

## 2. Current UI Reality

| 仓库 | 当前真实 UI 面 | 状态 |
|---|---|---|
| `ZhuangZhou/zhuangzhou-ui` | 13 个 HTML 承载 UI 面 / 7 大家族；仅 `dyna-orb`（Voice Orb）真正可用 | GUI 侧 **0 可用** |
| `xiao6-hub` | 4 视图单页壳 + 数据驱动功能网格 + DSH `<webview>` | ACTIVE（但中心=DSH 嵌入） |
| `deepseek-harness` | 成熟 cordis 插槽 Workspace（apps/web + packages/client + client-web） | ACTIVE（第三方产品，禁止直接复制） |

**真实启动链**（已核验）：`launcher/zz_launch.bat` → 后端 `server.py`(8010) → `electron .` → `package.json.main=electron/main.js` → `desktop-avatar/dyna-orb.html`（canonical orb）；Web → `zz-space/index.html`（**entry live，runtime dead**，见 §5 F1）。

---

## 3. Xiao6Hub Architecture

**身份**：纯 Electron 壳（v0.1.0，productId `com.zhuangzhou.xiao6hub`），**Presentation/Adapter**，非 ZhuangZhou Runtime。
- `main.js`：`BrowserWindow`(frame:false) + IPC（`server:get-status`/`xiao6:list-features`/`xiao6:launch`/`window:*`/`dialog:pick-workspace`/`config:set-workspace`/`shell:open-*`）+ DSH 查找/生成（`main.js:154-253`，`http://127.0.0.1:3080` 或生成 `dsh web` @3210）。
- `preload.js`：暴露 `window.xiao6`（contextBridge），`contextIsolation:true`。
- `renderer/`：仅 4 文件（index.html / renderer.js / style.css / icon.svg），**非多路由 SPA**；4 视图切换 `switchView`（`renderer.js:28-40`）；功能卡由 `features.json` 运行时生成（`renderer.js:105-133`）。
- **主要泄漏向量**：`renderer/index.html:122` `<webview>` 直接嵌入整个 DeepSeek Harness Web App；`index.html:44,82` 用户可见 "DeepSeek Harness · 工作区" 字样。

**分类**：Shell 与功能网格为 PRIMARY（应映射进 ZhuangZhou 原生）；DSH webview 为 INFRASTRUCTURE/第三方（REMOVE/REPLACE）；dev/infra 类 features 为 UTILITY（HIDDEN）。

---

## 4. DSH UI Architecture

**真实位置**（与任务假设偏差）：Web UI 不在 `apps/web` 的 UI 库本身，而在 `packages/client`（按 `ui-*` 拆分的 cordis 插件包）+ `packages/client/web`（外壳装配）+ `packages/client/web-react`。`packages/client-web` 不存在。

**模式库（高价值，可原生采纳）**：
1. 三栏可组合外壳 + 让步链几何（`ui-layout AppFrame`/`columns` + `ui-sidebar SidebarRoot`）：侧栏永不让步、窄屏折叠为图标轨道、纯函数几何求解。
2. 流式增量 Markdown（`ui-primitives MarkdownText`+`incremental`）：冻结前缀 + 重解析尾部（解决长回复 O(n²)）。
3. Agent 活动总览：泳道时间线 + 可折叠事件账本 + 搜索（`ui-trajectory TrajectoryView`/`Timeline`）——仓库最超前模式。
4. 触发器/命令输入机 + 命令面板（`ui-input-trigger`+`ui-commands`）：combobox a11y、键盘仲裁、模糊评分、每会话控制器。
5. 原子化 keyed 工具视图 + 工具结果原语（`ui-tool apply` + `TerminalBlock/DiffBlock/ReadBlock/WebBlock`）。
6. 勾选门控风险确认（`RiskConfirmation`+`PermissionRow`+`PlanReviewPanel`）；四态 `StateDot` 轻量状态词汇。

**必须清洗的品牌泄漏**（精确 file:line）：`apps/web/index.html:8` `<title>DeepSeek Harness</title>`；`manifest.webmanifest:3-4`；`ui-settings-models/onboarding-copy.ts:14-25`（"DeepSeek Harness"/"DSH plugin ecosystem"）；`ui-primitives/BrandWordmark.tsx`/`FishLogo.tsx`；`DocumentTitle.tsx:18`（标签后缀 `— DeepSeek Harness`）；测试快照 `examples/.../system-prompt.expected.md:1`（`"powered by DeepSeek Harness"`）。模型供应商名 "DeepSeek"（若 ZhuangZhou 使用 DeepSeek 模型）可合法保留；需清洗的是**产品名** DeepSeek Harness / DSH / DPH / DeepSeek agent / DSH workspace。

---

## 5. ZhuangZhou UI Architecture

**审计发现（关键，影响重建前提）**：

### 🔴 F1 — `zz-space` 加载即硬崩溃（当前无可工作 GUI）
`zz-space/js/zz-space.js:284` 顶层 IIFE 对已被删除的 `#petOrb` 元素空引用（`zz-space/index.html` 已用 `particleCanvas` 替代 Lottie 光球，`grep -c petOrb → 0`）。抛错中止整个脚本执行 → `window.__showHotTopics`、导航路由表、boot、时钟、粒子、nav、chat、overlay、轮询全部不运行。实测 Node 重放确认 `TypeError: Cannot read properties of null`。
> **PLANNED MODIFICATION（未执行）**：`zz-space.js:284` 加 null-guard 或移入 `initPetOrb()`。这是"有 GUI / 无 GUI"的唯一一行差距。

### 🔴 F2 — 文档化回滚基线已删除（不可恢复）
`index.html:4` 声称"改回 `/final/index.html` 即恢复旧入口"，但 `/final/` 与 `_legacy/` 已被删（`zz-space/_audit/CLEANUP_LIST_20260816.md`："旧 UI 全部删除 180.4MB"）。`solar-system.js`（Galaxy 渲染器）全仓不存在。Galaxy 是无渲染器的数据层，**不可复活，不作设计参考**。

### 🟠 F3 — 两个竞争 Electron 壳，含 GUI 窗口的那个从未启动
`gui/main.js` 是唯一拥有工作区窗口代码（`createWindow()` @97，加载 `zz-space/index.html`）的壳，但其 `showMain()`（@176）**零调用点**（托盘菜单仅 orb-toggle + quit），且 `gui/` 无任何 launcher 引用。launcher 走 `electron .` → `electron/main.js` → `dyna-orb.html`。

### 7 大家族清单（13 面）
- **A** `zz-space/` — Web Advanced Workspace（entry live / runtime dead，F1）。PRIMARY → **REBUILD**。
- **B** `desktop-avatar/` 6 orb 变体 — B1 `dyna-orb`（CANONICAL，KEEP/frozen）；B2 robot（LEGACY，ARCHIVE）；B3 gem-orb / B4 liquid-orb / B5 orb-pet / B6 orb-ui（DEAD/EXPERIMENTAL，ARCHIVE/REMOVE LATER；B6 还缺 `orb-ui-voice.js` 破裂引用）。
- **C** `chat-window.html` — ORPHANED（仅冻结 robot 套件触发）→ MERGE 进 GUI。
- **D** root `index.html` Galaxy 套件 — FROZEN/不可恢复 → ARCHIVE（仅取其信息架构）。
- **E** `companion.html` — DEAD → ARCHIVE。
- **F** `mobile-app.html` — DORMANT（独立 manifest 入口）→ FREEZE。
- **G** `selfcheck.html`（UTILITY，KEEP 从新 GUI 重入）/ `weather-modal-preview.html`（DEAD，REMOVE LATER）。
- **H** Electron 壳：H1 `electron/`（ACTIVE canonical，KEEP）；H2 `gui/`（ORPHAN 重复，MERGE `createWindow()`+tray 入 `electron/` 后 ARCHIVE）。
- **I** 孤儿根模块池（`avatar.js`/`bootstrap-wire.js`/`personal-ai-panel.js`/`control-center.js`/`dependency-manager.js`/`first-launch.js`/`capability-health.js`/`hud-ring.js`/`zz-space/js/voice-orb.js` 等）→ ARCHIVE。

### 重复（重建前必去重，每功能单一归属）
- D1 Voice pipeline 5× 复制（dyna-orb-voice / gem-orb-voice / liquid-orb-voice / orb-pet-voice / voice.js，98.6%–100% 相同）
- D2 Chat 3×（chat-window / zz-space.js:1373 / app.js）
- D3 Settings 3×（zz-space.js:1601 / settings.js / control-center.js:850）
- D4 Memory 4×（zz-space.js:1513 / memory-center / memory-panel / memory-query）
- D5 Capability 5×（zz-space.js:1557 / capability-center / capabilities-view / capability-matrix / capability-exposure）
- D6 Command bar 3×（zz-space #cmdForm / command-palette.js / command-dock.js）
- D7 Avatar 渲染 5×（dyna-orb / avatar.js / avatar-renderer+controller / voice-orb-simple / zz-space/js/voice-orb.js）
- D8 Electron 壳 2×（electron/ vs gui/，IPC 命名空间 `avatar:*` vs `pet:*` 分歧）

### 孤儿/破裂引用（无入口）
`avatar.js`(0 引用)、`bootstrap-wire.js`(0 引用)、`personal-ai-panel.js`(phantom)、`control-center.js`/`dependency-manager.js`/`first-launch.js`/`capability-health.js`(仅孤儿互引)、`hud-ring.js`(仅测试)、`zz-space/js/voice-orb.js`(0 引用)、`orb-ui-voice.js`(文件缺失)、`final/components/proactive-discovery.js`(已删)、`solar-system.js`(从不存)。

### 视觉资产（非功能）
`ZhuangZhou_Avatar/`(73 PNG)、`assets/avatar/*.svg`(8 态)、`textures/*.jpg`(含 `.broken`)、`vendor/earth`/`vendor/three`、`zz-space/assets/elements/*.png`(13 切图，0 引用)、`zz-space/assets/lottie/{siri,holographic}.json`(指向缺失 `#orbLottie`)、`zz-space/_verify/*.png`(≈40 设计候选截图)、`weather-modal-preview.html`。

### GUI vs Voice 归属
- **Voice Orb 保留**（已冻结正确）：唤醒/听写、ASR、单轮 chat、TTS、状态表达、在途/执行瞬时一瞥、窗口穿透/拖拽。
- **GUI 必须拥有**（当前破裂/重复/孤儿）：tasks/进度、schedule/reminders、memory 浏览/纠正/审计、knowledge graph、capability catalog、settings/providers/models、chat 历史、diagnostics/self-check、hotspots/discovery、right-rail today/processing/discover。

---

## 6. Feature Value Matrix（Xiao6Hub features.json，47 项）

> 完整 47 项逐条矩阵见审计代理输出；此处给汇总分布与分类。**结论：不要 47 个入口全展示。**

| 类别 | 数量 | 代表 | 推荐去向 |
|---|---|---|---|
| A — Core User Workflow | ~22 | web-ui / memory / conversations / notes / knowledge / tasks / weather / hotspots / briefing / asr-status / system-prompt | Center Conversation / Right Context |
| B — Advanced Workflow | ~15 | perception-ocr / perception-describe / calendar / episodes / goals / proactive-* | Right Context / Command Palette |
| C — Utility | ~6 | open-project / export-data / open-docs / github / version / notes-tag | Command Palette / Hidden |
| D — Developer/Admin | ~9 | health / ready / boot-state / sysmon / logs / selfcheck / capabilities / capability-os / agent-state | **Hidden** / Command Palette |
| E — Infrastructure | ~3 | backend-status / workspace config / dsh reload | Settings / Hidden |
| F — Legacy/Unused | 0（但 conditional 3 项 calendar/focus-app/clipboard 需能力门控） | — | Hidden until capability on |

**重复**：`capabilities` ↔ `capability-os`（合并为一）。**安全警示**：`open-config` 一键打开含 `.env` 密钥的目录 → 绝不做默认可见按钮。**条件门控**：calendar/focus-app/clipboard 后端 feature 关时不应展示（否则报错）。

**用户真正需要看到的**（建议默认可见）：对话、记忆、笔记、知识、任务、今日简报、热点、天气、能力目录（GUI 化）、语音状态。其余收进 Right Context / Command Palette / Hidden。

---

## 7. User Workflow Map

从真实用户任务出发（非代码），定义 13 条核心 workflow：

- **W1 Chat**："和庄周聊天" → Center Conversation（Voice Orb 也可发起，GUI 承接完整记录）。
- **W2 Ask/Search/Think**：提问 + 处理 → Conversation + Right Context（检索/引用）。
- **W3 Task**："帮我完成任务" → Task 视图（GUI 拥有进度/子步骤）。
- **W4 Capability**："执行某能力" → Capability 目录（GUI 单一归属）+ 命令面板触发。
- **W5 Memory**："我之前说过什么" → Memory 浏览/纠正/审计（Right Context）。
- **W6 Context**："现在项目什么状态" → Right Context（项目/状态/感知快照）。
- **W7 Project**：围绕项目工作 → Projects 视图（文件/结果/上下文）。
- **W8 Result**：查看任务结果 → Result 表面（产物文件 chip、diff/读取块）。
- **W9 Approval**：危险操作确认 → 勾选门控风险确认 Modal（统一范式）。
- **W10 History**：历史对话/工作记录 → Conversation History + Sessions。
- **W11 Workspace**：文件/代码/结果/上下文 → Native Workspace（目录选取器 + 改动以 diff/读取块呈现，非完整 in-app 编辑器）。
- **W12 Voice → GUI Handoff**：语音交互中进入 GUI 看详细结果 → Orb 触发打开对应 GUI 视图（带上下文）。
- **W13 GUI → Voice Handoff**：GUI 工作中用 Voice Orb 继续 → Orb 作为全局浮层承接口语指令。

---

## 8. Information Architecture

**唯一推荐 IA（基于真实审计，非机械抄模板）**：

```
LEFT  — Navigation / Sessions / Projects / Tasks / Memory / Capabilities / Settings
CENTER — Conversation (default) ⇄ Workspace ⇄ Results ⇄ Tool execution ⇄ Agent activity
RIGHT — Context / Tasks state / Memory details / Capability details / Result details / Approval
TOP   — Brand + Global status (backend/voice) + Command (Cmd/Ctrl+K) + Voice Orb presence
FLOAT — Voice Orb（全局存在感浮层）
```

**决策依据**：
- Left 收敛 Xiao6Hub 的 4 视图 rail + 47 功能网格（A/B 类入导航/命令面板，D/E 类 Hidden）。
- Center 默认 Conversation（继承 `zz-space` 8 路由中的 chat/home），Workspace/Results 为同区切换（采纳 DSH 三栏 + 让步链几何）。
- Right 承载 Xiao6Hub 37 个只读 `/api/*` 面板（memory/notes/knowledge/tasks/goals/weather/hotspots/briefing/perception…）——这些本就是"上下文"信息，归 Right 最自然。
- Voice Orb 不是 Left/Center 的按钮，而是 **TOP/FLOAT 的全局存在感**。

> 候选方向对比：① 经典三栏（采用）；② 全屏 Workspace 优先（适合重度工程，但弱化对话，不取）；③ 对话优先 + 可收起侧栏（采用为默认会话态）。最终选 **① 为骨架，③ 为默认会话态**。

---

## 9. Home Screen Architecture

**禁止**：功能墙 / 47 卡片 / HUD dashboard / 传统 control center。
**Home 回答**："庄周现在能帮我做什么？"

组成（极简、高级、大留白）：
- **Voice Orb 存在感**（中心视觉焦点，非按钮）
- **Greeting / Status**（时间 + 后端/语音就绪 + 当前 Context 一句话）
- **Current Context**（当前项目/任务轻量条）
- **Recent Work**（最近会话/任务，来自 Sessions）
- **Active Tasks**（进行中任务轻量列表，来自 Right/Task）
- **Quick Actions**（少量高频：Ask / Task / Memory / Capability，其余进命令面板）
- **Projects**（轻量入口）

视觉：Deep Black 背景 + 中心 Orb 微光 + 大留白 + 软玻璃卡片（极少边框）+ 强排版层级。

---

## 10. Advanced Workspace Architecture

**骨架**（借用 DSH 模式，原生实现）：
- **Shell**：单 Electron 壳（`electron/`，接管 `gui/main.js` 的 `createWindow()`+tray）。三栏 CSS grid + 可拖拽分隔条 + 窄屏折叠图标轨道（纯函数几何，侧栏永不让步）。
- **Center**：
  - Conversation（默认）：带类型 ChatNode 序列（assistant/command/tool/turn-error…），流式增量 Markdown（冻结前缀+重解析尾部），推理块独立渲染。
  - Workspace：目录选取器 + 文件改动以 diff/读取块呈现。
  - Results：产物文件 chip（宽度自适应、溢出计数）+ 工具结果原语（Terminal/Diff/Read/Web/JsonTree）。
  - Agent activity：泳道时间线（Input/Model/Tools）+ 可折叠事件账本 + 搜索。
- **Right**：Context/Memory/Tasks/Capability/Result 详情 + Approval（勾选门控确认）。
- **Command**：Cmd/Ctrl+K 命令面板（每会话控制器 + Host 目录 + 可注册贡献 + 模糊评分 + 弹选卡片）。
- **State/Loading/Error**：四态 StateDot（done/warning/ongoing/error，ongoing=轻量 CSS 像素追逐）+ 非阻塞横幅 + 首跑 inert 接管。

**ZhuangZhou owns**：Identity / Context / Memory / Decision / Policy / Approval / Lifecycle / Capability。
**DSH owns**：Workspace substrate / Agent implementation substrate / Tool/session infrastructure（经 HTTP 集成，不可见）。
GUI 仅做 Presentation / Adapter。

---

## 11. Voice Orb Integration

Phase 2 已冻结：Web = `zz-space petOrb`；Electron = `dyna-orb`。Phase 3 **不重新设计 Orb**。

**Orb 在 GUI 中的主角色（唯一推荐）：Persistent Companion / Global Presence（全局存在感浮层）**。
- 不是普通按钮；是庄周的"存在感"。
- 关系模型候选对比：Persistent companion（采用）/ Floating command button / Conversation focus / Global voice entry / Status indicator / Context-aware assistant。最终选 **Persistent companion + Context-aware assistant**——Orb 常驻（桌面 Electron 透明窗 or Web 浮层），感知当前 GUI 上下文并可视化状态（idle/listening/thinking/speaking/error 五态不变），口语指令经 Orb 进入 Core；GUI 承载详细结果。
- **W12/W13 handoff**：Orb 触发打开对应 GUI 视图（带上下文）；GUI 中口语指令回落 Orb。

---

## 12. DSH Workspace Integration

**角色模型选择**：**D — Embedded tool/result surface + B — Center workspace（原生）**。
- DSH 的"工作区"能力（文件/工具/agent loop）经 HTTP 作为**底层基础设施**集成，ZhuangZhou 原生 GUI 吸收其**成熟交互模式**（§4），但**最终用户绝不可见** DeepSeek Harness / DSH / DPH / DeepSeek agent / DSH workspace 等概念。
- Xiao6Hub 当前直接 `<webview>` 嵌入 DSH 必须**替换**为原生 Center Conversation。
- 边界：ZhuangZhou 拥有身份/上下文/记忆/决策/策略/审批/生命周期/能力；DSH 拥有工作区底座/agent 实现底座/工具会话基础设施。

---

## 13. Command System

**需要 Command Bar / Command Palette**（Cmd/Ctrl+K 风格，非传统软件菜单）。
- 模式：每会话控制器 + Host 目录（47 features 的去重后入口）+ 客户端可注册贡献 + 模糊评分 + 弹选卡片（搜索框夺焦、↑↓ 导航、Esc 关闭）。
- 命令域：Ask ZhuangZhou / Search / Open Project / Run Task / Memory / Capabilities / Settings / Voice toggle。
- 触发器架构（采纳 DSH `ui-input-trigger`）：把 `/命令` 与 `@提及` 统一为触发器源注册表；键盘仲裁 + 异步候选代际。
- 目标："用户直接告诉庄周我要做什么"——命令面板是第一入口，而非 47 卡片墙。

---

## 14. State / Interaction Model

GUI 状态可比 Voice Orb 丰富，但 **Voice Orb canonical 5 states（idle/listening/thinking/speaking/error）不得被 GUI 改写**（Phase 2 契约）。

**GUI projection 状态**（非第二 Runtime）：
- Idle / Thinking / Working（Task running）/ Tool executing / Waiting Approval / Speaking / Result ready / Error。
- 统一状态词汇：`StateDot` 四态（done/warning/ongoing/error），ongoing=轻量 CSS 像素追逐。
- 加载/错误/空：非阻塞横幅（仅真断线显示）+ 首跑 inert 接管 + 统一空态文本。
- 响应式：窄屏折叠侧栏为图标轨道；中心栏地板宽度保护；偏好值不可变（窗口拉宽自动恢复）。

---

## 15. Visual Reconstruction Map

| Surface | current | target | delta | priority |
|---|---|---|---|---|
| Global shell | `electron/main.js`(orb only) + `gui/main.js`(workspace, unlaunched) | 单壳 `electron/` + 原生 Workspace | 合并 `gui/createWindow()`+tray | P0 |
| Top area | 无 | Brand + 全局状态 + Cmd-K + Orb 存在感 | 新增 | P1 |
| Left nav | Xiao6Hub 4-view rail + 47 网格 | 收敛导航（Sessions/Projects/Tasks/Memory/Capability/Settings）+ 命令面板 | 重写 | P1 |
| Main content | `zz-space`(crashed) / DSH webview | Conversation ⇄ Workspace ⇄ Results ⇄ Agent activity（原生） | 重建+去 DSH | P0/P1 |
| Right context | Xiao6Hub 37 `/api/*` 面板 + `zz-space` right-rail(dead) | Context/Memory/Tasks/Capability/Result/Approval 栈 | 重写 | P1 |
| Voice Orb | `dyna-orb`(frozen) | 全局浮层存在感 | 保持不变 + GUI 锚定 | P2 |
| Command area | `zz-space #cmdForm`(dead) + `command-palette.js`(orphan) | Cmd/Ctrl+K 命令面板 | 新建（采纳 DSH 模式） | P1 |
| Task/result surface | 分散死套件（D3–D5） | 单一 Task/Result 归属 + 产物 chip + 工具结果原语 | 去重重建 | P2 |
| DSH workspace | Xiao6Hub `<webview>` DSH | 原生 Center Workspace（DSH 仅底座） | 替换 | P1 |
| Modal | `RiskConfirmation`(DSH) 模式 | 勾选门控风险确认（统一审批范式） | 新建（采纳模式） | P2 |
| Overlay | `zz-space` 单复用 sheet(dead) | 复用 overlay 栈（采纳 legacy overlay-manager 纪律） | 重建 | P2 |
| Toast | `zz-space` toast(dead, 缺 `#voiceHint`) | 非阻塞 toast/横幅 | 重建 | P2 |
| Loading | 无统一 | StateDot + 非阻塞横幅 | 新建（采纳 DSH） | P1 |
| Error | 无统一 | 统一错误/空态 + 重连横幅 | 新建（采纳 DSH） | P1 |
| Approval | 无统一 | 勾选门控确认 Modal + 计划评审决策卡 | 新建（采纳 DSH） | P2 |

---

## 16. UI Disposition Matrix

| 旧 UI | 状态 | 处置 |
|---|---|---|
| `zz-space/` | ACTIVE entry / DEAD runtime (F1) | **REBUILD**（唯一正确 GUI 基础，修 `:284` 即活） |
| `dyna-orb.*` | ACTIVE canonical | **KEEP**（frozen，Phase 2） |
| `desktop-avatar/index.html` robot | FROZEN | **ARCHIVE** |
| `gem-orb.*` / `liquid-orb.*` / `orb-pet.*` | DEAD/EXPERIMENTAL | **ARCHIVE** |
| `orb-ui.*` | DEAD+BROKEN ref | **REMOVE LATER** |
| `chat-window.html` | ORPHANED | **MERGE** 进 GUI |
| root `index.html` Galaxy 套件 | FROZEN/不可恢复 | **ARCHIVE**（仅取 IA，不取代码） |
| `companion.html` | DEAD | **ARCHIVE** |
| `mobile-app.html` | DORMANT | **FREEZE** |
| `selfcheck.html` | FROZEN entry | **KEEP**（从新 GUI 重入） |
| `weather-modal-preview.html` | DEAD | **REMOVE LATER** |
| `electron/` | ACTIVE canonical | **KEEP**（GUI 窗口加于此） |
| `gui/` | ORPHAN 重复 | **MERGE** `createWindow()`+tray 入 `electron/` 后 **ARCHIVE** |
| 孤儿根模块池 (I) | DEAD | **ARCHIVE** |
| 视觉资产集 (Q5) | LEGACY | **ARCHIVE**（删 `three.min.js`603KB / `siri.json`776KB / `textures/`4.8MB / `vendor/earth` / `vendor/three` / 未用 `elements/*`） |

**原则**：不因旧 UI 存在而保留。先证引用→分类→解除 active 接线→据真实依赖决定去留。

---

## 17. Final Recommended GUI Architecture

```
ZHUANGZHOU GUI  (Advanced Workspace / Control Surface)
│
├── Shell                ← electron/ (合并 gui/createWindow+tray)；三栏 + 可拖拽分隔 + 窄屏折叠轨道
│
├── Top                  ← Brand + 全局状态 + Cmd/Ctrl+K + Voice Orb 存在感
│
├── Navigation (Left)    ← Sessions / Projects / Tasks / Memory / Capabilities / Settings
│                          (收敛 Xiao6Hub 4-view + 47 网格；D/E 类 Hidden)
│
├── Home                 ← Orb 存在感 + Greeting/Status + Context + Recent + Active Tasks + Quick Actions + Projects
│
├── Conversation (Center)← 带类型 ChatNode + 流式增量 Markdown + 推理块独立
│
├── Projects             ← 项目文件/结果/上下文
│
├── Tasks                ← 单一 Task 归属（进度/子步骤）
│
├── Workspace (Center)   ← 目录选取器 + 改动以 diff/读取块呈现（DSH 底座，不可见）
│
├── Context (Right)      ← 37 `/api/*` 上下文面板（memory/notes/knowledge/tasks/goals/weather/hotspots/briefing/perception…）
│
├── Memory               ← 单一 Memory 归属（浏览/纠正/审计）
│
├── Results              ← 产物 chip + 工具结果原语（Terminal/Diff/Read/Web/JsonTree）
│
├── Approval             ← 勾选门控风险确认 + 计划评审决策卡
│
├── Command              ← Cmd/Ctrl+K 命令面板（模糊评分 + 每会话控制器）
│
└── Voice Orb (Float)    ← 全局存在感浮层（Phase 2 frozen；Persistent companion + Context-aware）
```

**裁决**：以上为审计后唯一推荐架构（非起始假设）。Voice Orb 边界不变；GUI 拥有所有可浏览/可配置/历史/复数功能，每功能单一归属（去重 D1–D8 后）；DSH 仅底层底座。

---

## 18. File-Level Implementation Plan（真实文件，仅 PLANNED）

> 全部为 PLANNED MODIFICATION，**本阶段零执行**。下一阶段（明确授权后）实施。

| Repository | File | Current Role | Target Role | Action | Risk | Dependency |
|---|---|---|---|---|---|---|
| zhuangzhou-ui | `zz-space/js/zz-space.js:284` | 顶层空引用 `#petOrb` 致全脚本崩溃 | null-guard / 移入 initPetOrb | **PLANNED FIX (P0)** | Low | — |
| zhuangzhou-ui | `electron/main.js` | 仅加载 dyna-orb | 增加 Workspace 窗口（接管 gui/createWindow） | REBUILD | Med | IA freeze |
| zhuangzhou-ui | `gui/main.js:97-122,153-174` | 唯一 workspace 窗口+tray（未启动） | 合并进 `electron/` | MERGE→ARCHIVE | Med | 单壳裁决 |
| zhuangzhou-ui | `electron/avatar-window.js:22-23` | 320×480 窗；球命中区≈2×可见 | 标准化正方形窗 + 命中区对齐 | MODIFY | Low | orb 几何 |
| xiao6-hub | `renderer/index.html:122` | `<webview>` 嵌入 DSH | 删除，替换为原生 Center | REPLACE | High | 原生 Conversation |
| xiao6-hub | `renderer/index.html:44,82` | "DeepSeek Harness · 工作区" | 改名 "AI 工作区/智能体工作区" | REBRAND | Low | — |
| xiao6-hub | `features.json` | 47 入口全展示 | 分类 A/B 入导航/命令面板，D/E Hidden，合并 capabilities/capability-os | REBUILD | Med | Feature Value Matrix |
| xiao6-hub | `main.js:154-253` | 查找/生成 DSH 后端 | 仅作底座经 HTTP 集成，移除 webview spawn | MODIFY | Med | DSH 集成模型 |
| zhuangzhou-ui | `zz-space/` 全套 | Web Advanced Workspace（crashed） | REBUILD 为原生 Workspace（IA §17） | REBUILD | High | 单壳 + IA |
| zhuangzhou-ui | `desktop-avatar/orb-*`(B3-B6) | 竞争 orb 变体 | ARCHIVE | ARCHIVE | Low | 分类完成 |
| zhuangzhou-ui | `root index.html` Galaxy 套件 | 冻结不可恢复 | ARCHIVE（取 IA） | ARCHIVE | Low | — |
| zhuangzhou-ui | 孤儿模块池 (I) | DEAD | ARCHIVE | ARCHIVE | Low | — |
| deepseek-harness | `apps/web/index.html:8` 等 | `<title>DeepSeek Harness</title>` | （仅模式参考）ZhuangZhou 分支清洗品牌 | REBRAND(移植时) | — | 不复制代码 |

---

## 19. Risks / Blockers

1. **🔴 F1 阻断 GUI 前提**：`zz-space.js:284` 崩溃使"当前 GUI"为零可用面。重建须先修此一行（P0），否则无基础可重建。**本阶段只读，未修**。
2. **🔴 F2 回滚基线丢失**：`/final/` 与 `solar-system.js` 已删，旧 UI 不可恢复；设计不得依赖旧套件代码，仅可借鉴其 IA。
3. **🟠 F3 双壳未裁决运行时**：仅 `electron/` 被启动；含 GUI 窗口的 `gui/` 从未启动。重建须先定单壳（合并 `gui/createWindow` 入 `electron/`）。
4. **去重负担**：D1–D8 跨死套件重复 3–5 份，须在移植前每功能单一归属，否则新 GUI 继承混乱。
5. **DSH 品牌泄漏**：Xiao6Hub `<webview>` + 多处 "DeepSeek Harness" 字样须清洗；DSH 模式可借，品牌不可带。
6. **runtime capture BLOCKED**：本环境为 headless，无法真实启动 Xiao6Hub / DSH Web / ZhuangZhou Web 进行截图/视觉观察。**未假装看过**——所有结论来自真实源码读取（file:line），非运行时截图。
7. **安全**：`features.json` 的 `open-config` 一键打开含 `.env` 密钥目录，新 GUI 须 Hidden + 确认。

---

## 20. Phase 3 Implementation Roadmap

本阶段**不实施**，仅制定。唯一推荐顺序：

- **Phase 3A — UI Architecture freeze**：依 §17 冻结 IA；定单 Electron 壳（合并 `gui/` 入 `electron/`）；澄清 DSH 集成模型（D/E）。
- **Phase 3B — Visual System**：建立 Deep Black/Graphite + 克制青/teal + 大留白 + 软玻璃设计 token；StateDot/横幅/toast 原语；清洗 DSH 品牌词汇。
- **Phase 3C — Shell reconstruction**：实现三栏可组合外壳（让步链几何 + 窄屏折叠轨道）；Top（Brand+状态+Cmd-K+Orb）；Left 导航。
- **Phase 3D — Workspace reconstruction**：Center Conversation（带类型 ChatNode + 流式增量 Markdown + 推理块）；Right Context 面板栈（37 `/api/*`）；Home；去重 D2–D6。
- **Phase 3E — DSH integration surface**：原生 Center Workspace（目录选取器 + diff/读取块）；DSH 仅 HTTP 底座；替换 Xiao6Hub `<webview>`。
- **Phase 3F — Voice/GUI handoff**：Orb 全局浮层存在感；W12/W13 上下文互转；命令面板触发 Orb/语音。
- **Phase 3G — Real user verification**：真实桌面启动 + 截图 + 用户走查 W1–W13。

**前置 P0（重建前必做，非本阶段）**：修 `zz-space.js:284`；定单壳；归档死套件。

---

## 21. Acceptance Criteria

本阶段（STAGE 0 审计）完成必须满足：

- [x] Xiao6Hub 全 UI 已真实审计（4 文件壳 + 47 features 矩阵）
- [x] features.json 全量审计（47 项逐条 Feature Value Matrix）
- [x] DSH Web UI 已真实审计（packages/client `ui-*` 模式库 + 品牌泄漏 file:line）
- [x] ZhuangZhou 当前 UI 已真实审计（13 面/7 家族 + F1/F2/F3 发现）
- [x] 旧 UI 已分类（Disposition Matrix §16）
- [x] 用户核心 workflow 已建立（W1–W13 §7）
- [x] GUI Information Architecture 已确定（§8 唯一推荐）
- [x] Home Architecture 已确定（§9）
- [x] Advanced Workspace Architecture 已确定（§10）
- [x] Voice Orb GUI 角色已确定（§11 Persistent companion）
- [x] DSH Workspace 角色已确定（§12 D/E 模型）
- [x] Command System 已确定（§13 Cmd/Ctrl+K）
- [x] GUI state model 已确定（§14，Voice 5 态不改写）
- [x] Visual Reconstruction Map 已建立（§15）
- [x] UI Disposition Matrix 已建立（§16）
- [x] Final GUI Architecture 已确定（§17）
- [x] File-level implementation plan 已建立（§18）
- [x] Implementation order 已确定（§20 唯一推荐）
- [x] Zero production modification（MODIFIED FILES = 0）
- [x] Zero Core modification
- [x] Zero DSH modification
- [x] Zero Xiao6Hub modification
- [x] Zero UI modification

> 注：§16-runtime capture 项（`BLOCKED — runtime capture unavailable`）因 headless 环境无法执行，已诚实标注，未伪称已看。

---

## 22. STOP

**PHASE 3 — DESIGN AUDIT COMPLETE · READ ONLY · ZERO MODIFICATION · PLAN READY · STOP.**

```
MODIFIED FILES = 0
CORE MODIFIED = 0
DSH MODIFIED  = 0
XIAO6HUB MODIFIED = 0
UI MODIFIED   = 0
```

最终状态：**PHASE 3 — DESIGN AUDIT COMPLETE / READ ONLY / ZERO MODIFICATION / PLAN READY / STOP**

等待下一次**明确授权**后，才进入 GUI Implementation（Phase 3A–3G）。不自动进入 Implementation，不自动进入 Phase 4。

---

*报告结束。所有结论基于 `G:\ZhuangZhou\zhuangzhou-ui` + `G:\xiao6-hub` + `G:\deepseek-harness` 真实源码读取（file:line），未修改任何文件。生成于 2026-08-17。*
