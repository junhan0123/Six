# P3-H · Observability / Audit Trace（可观测性与审计链）

> 庄周长时间运行后必须能回答"刚才到底发生了什么"。建立端到端可追踪审计链，复用既有 EventBus / logging / state，不复制业务状态系统。

## 1. 审计链字段覆盖（实测）

| 字段 | 来源 | 状态 |
|---|---|---|
| `task_id` | `tasks.id` | ✅ |
| `goal_id` | `tasks.goal_id` / `agent_runtime` | ✅ |
| `execution_id` | `mcp_host`（`"mcp-"+capability_id`）/ `execution_reflections` | ⚠️ 仅 MCP/reflection 有；native 无显式 execution_id |
| `capability_id` | `tool_to_capability(name)` / `capability_os` | ✅ |
| `attempt_id` | ❌ 不存在 | O2（同 P3-C） |
| `policy_decision` | `policy_engine.evaluate` 返回 `decision` | ✅（运行期可得） |
| `permission_decision` | `permission_guard` / `policy` | ✅ |
| `approval_decision` | `policy.request_approval` 返回 `approve/reject/...` | ✅（confirm 路径） |
| `executor_result` | `tool_audit.result_preview` / `CapabilityResult.data` | ✅ |
| `event_id` | `eventbus.Event.event_id`（uuid） | ✅ |
| `timestamp` | `Event.timestamp` / `db _now()` | ✅ |

## 2. 审计落库（真实）

- **`tool_audit` 表**（`db.py:68-72`）：由**关键能力工具**（`file_read`/`file_write`/`file_delete`/`web_search` 等，见 `tools.py:1193+`）经 `sandbox.audit_tool`（`sandbox.py:129`）写入 `ts/tool/summary/args_json/result_preview/status/risk/duration_ms` —— **部分能力审计**（非全量：轻量原生工具如 `get_time` 不入此表，属设计选择）。
- **`execution_reflections.jsonl`**（`ai_core/data/`）：记录 `execution_id, goal_id, tool, outcome, error` —— 用于 self-improvement 回溯。
- **EventBus**：`publish_domain`（DOMAIN_EVENT_NAMES）/ `publish_system`（SYSTEM_EVENT_NAMES）强制单一事件名来源（`eventbus.py:225/285`），每条带 `event_id` + `ts`。**这是全量执行追踪的主通道**（每步任务/工具均有 event_id）。

## 3. 端到端追踪示例（一次执行）

```
USER REQUEST
 → GOAL_CREATED (event_id=ε1)
 → TASK_CREATED (event_id=ε2)
 → TASK_STARTED / TASK_RUNNING (ε3/ε4)
 → Policy.evaluate → decision=auto
 → tool_started (system, ε5) → Executor → tool_finished (ε6)
 → TASK_COMPLETED (ε7) → tool_audit 行写入
 → GOAL_COMPLETED (ε8)
 → Future Context 经 build_context_prompt 注入
```

## 4. 已知缺口（非阻塞）

- **O2**：native 工具无显式 `execution_id`/`attempt_id`（MCP 与 reflection 有）。建议 P4 在 `run` 生成统一 `execution_id` 并写入 `tool_audit`。
- **O3（事件双发射）**：MCP 路径 `tool_started/tool_finished` 同时由 `ai_core/execution/events.py` 与 `mcp_host/executor.py:100/145` 发射；`COMPUTER_ACTION_PHASE` 双发布源。属事件级噪声，**不影响执行唯一性**，建议后续清理。
- **O4（审计覆盖部分）**：`tool_audit` 仅由 file/web/network 等关键能力工具写入，轻量原生工具（如 `get_time`）不入此表。全量执行追踪应以来 `EventBus event_id` 为准（`tool_audit` 为补充细节）。属设计选择，非缺陷。

## 5. P3-J · Config / Feature Flags（并入本节）

- `FEATURE_CAPABILITY_RUNTIME`：默认 `True`（`config.py:93`），ON=Unified / OFF=Legacy(直接 execute_tool) / 缺属性 fail-safe=True / ON-OFF 互斥不双执行（详见 `P2-FEATURE-FLAG-REPORT.md`）。
- `FEATURE_EVENTBUS`：SSE 通道开关（`eventbus.enabled()`）。
- **审计结论**：所有 Flag 单一职责、无"一个 flag 控制多不相关系统"的混乱；回退路径受控、可审计。✅

## 6. P3-K · Resource / Performance Safety（并入本节）

| 边界 | 现状 | 结论 |
|---|---|---|
| 无限 retry | `_MAX_RETRIES=3`（上限 4 次） | ✅ 有界 |
| 无限 task | `tasks` 表无硬上限，但单 Goal 拆解 3-8 步（`goals.plan_goal`） | ⚠️ 无全局 task 配额（O 类，本地个人场景风险低） |
| 无限 event | EventBus 异步线程池 `_ASYNC_WORKERS=4`，死信上限 `_MAX_RETRIES=2` | ✅ 有界 |
| 无限 context | Context 预算 `BudgetTier`，`LegacyContextBuilder` 注入来源 | ✅ 有预算 |
| 无限 memory | `compress_memory` / `_distill_learnings` 压缩 | ⚠️ 无硬上限（O 类） |
| 无限 queue/subprocess/MCP | `_call_budget.consume_call()` 能力调用预算闸门（`agent_runtime.py:515`） | ✅ 有预算闸门 |
| 无限 MCP request | `mcp_host` 单 runtime loop + timeout | ✅ 有超时 |

**裁定**：Audit Trace = **PASS**（审计链可追踪、复用既有设施、无重复系统）；Feature Flags = **PASS**；Resource Safety = **PASS**（关键边界有界，无无限循环/重试/队列风险）。O2/O3 及 memory/task 配额留 backlog。
