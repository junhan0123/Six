# P4.4 · CONTEXT UNIFICATION — IMPLEMENTATION REPORT

> ZhuangZhou v1.4.0 · Context Engine 真正统一
> 唯一入口：`context.facade.build_context_prompt()`
> 阶段：P4.4.1（审计/基线，PHASE 0 已完成 → `P4.4-CONTEXT-BASELINE.md`）→ P4.4.2–P4.4.6 实现。

## 一、目标达成

在不修改 UI、不影响 P1/P2/P3/P4.2/P4.3 的前提下，将多套 Context Assembly 收敛为唯一
Unified Context Engine：

```
User Request → context.facade.build_context_prompt
             → LegacyContextBuilder（Collect → Rank → Budget → Bundle(dedup) → Build）
             → RuntimeState / Memory / Knowledge / UserModel / Episodic / Goals / Personality / PersonalContext / PersonalAI / Identity
             → ContextItem[] → Ranker → Budget → Dedup → Final Context → LLM
```

## 二、改动清单（全部 `.py`，UI 零触碰）

| 文件 | 变更 | 对应子任务 |
|---|---|---|
| `context/models.py` | 新增 `ContextSource.RUNTIME_STATE` 枚举值 | P4.4.2 |
| `context/retrieval.py` | **新增** `UnifiedRetrieval`：thin adapter over `memory.retrieve_memories`（canonical），逐条转 `ContextItem` | P4.4.3 |
| `context/memory_recall_source.py` | 改用 `UnifiedRetrieval`（弃 `memory_intelligence.build_block` 第二路径），逐条 `ContextItem` + `dedup_scope="memory"` + memory id/provenance | P4.4.2 / P4.4.3 |
| `context/core_prompt_source.py` | **新增** `CorePromptSource`：注入 `config.SYSTEM_PROMPT` + 人格块（`dedup_scope="base_instruction"`） | P4.4.2 |
| `context/runtime_state_source.py` | **新增** `RuntimeStateSource`：经 sanctioned compat adapter `memory.build_context_prefix` 取运行时状态（`dedup_scope="runtime_state"`） | P4.4.2 / §8 |
| `context/builder.py` | 默认注册表移除 blob `MemorySource`；改注册 `CorePromptSource`+`RuntimeStateSource`+`MemoryRecallSource`（始终）+ 各认知来源（按 Flag）；构建后发布 Context 事件 | P4.4.2 / P4.4.5 |
| `eventbus.py` | `DOMAIN_EVENT_NAMES` 登记 `CONTEXT_BUILT` / `CONTEXT_TRUNCATED` / `CONTEXT_DEDUPED` | P4.4.5 |
| `config.py` | 修复 Context 相关配置双默认漂移（`FEATURE_USER_MODEL/EPISODIC_MEMORY/PERSONALITY/GOAL_SYSTEM/KNOWLEDGE_PLATFORM` 模块级默认 False → True，与 `reload()` 一致） | P4.4.6 |
| `context/cognitive_sources.py` | `UserModelSource`/`EpisodicSource` 补 `dedup_scope` | §10 |
| `context/personality_source.py` `goal_source.py` `knowledge_source.py` `personal_context_source.py` `personal_ai_source.py` `identity_source.py` `perception_source.py` | 各补 `dedup_scope`（scope 互不相交） | §10 |
| `tests/test_context_sources.py` `tests/test_context_engine.py` | 适配新架构断言（移除「builder 含 blob MemorySource」「委托 build_system_prompt」旧断言，改为校验统一基座 / Legacy 回退路径） | — |

> `MemorySource`（blob）类**保留**为兼容类（未注册进生产注册表）；`memory.build_system_prompt` 保留为 Legacy 兼容层，仅 `FEATURE_CONTEXT_ENGINE=False` 时由 `facade._legacy_prompt` 委托。

## 三、子任务验收

- **P4.4.1 真实审计**：`P4.4-CONTEXT-BASELINE.md`（PHASE 0 已交付）—— 16 项 VERIFY 表、Production Caller Map、Source 明细、Legacy/New 路径、Overlap/Duplicate/Bypass 分析。
- **P4.4.2 拆分 MemorySource**：→ `RuntimeStateSource` + `MemoryRecallSource` + `UserModelSource` + `EpisodicSource`，全部输出 `ContextItem`，无 blob。
- **P4.4.3 统一 Retrieval**：`UnifiedRetrieval` 仅委托 canonical `memory.retrieve_memories`（→ `DefaultEvolutionPolicy`），无第二套检索引擎；`MemoryRecallSource` 弃用 `build_block`。
- **P4.4.4 启用真实 Budget**：`ContextBudget` 在 T16K(16384)/T32K(32768)/T64K(65536)/T96K(98304) 真实裁剪，测试覆盖 truncation / rank 保序 / 与 dedup 协同 / 无限预算保留全部。生产默认保持无限预算（P4.4 §12 不强行启用 token limit）。
- **P4.4.5 Context 事件**：`CONTEXT_BUILT`（构建成功，一次，best-effort 非阻塞）/ `CONTEXT_TRUNCATED`（预算裁剪）/ `CONTEXT_DEDUPED`（同域去重）已登记并发布。
- **P4.4.6 Legacy Adapter + 配置**：`build_system_prompt` 保留为兼容层；`FEATURE_CONTEXT_ENGINE` 默认 `True`；修复 Context 相关配置双默认漂移。

## 四、关键不变量（红线守住）

- **UI 冻结**：HTML/CSS/Frontend JS/UI assets = 0（见 `P4.4-UI-FREEZE-AUDIT.md`）。
- **Security Boundary**：`context/`（及 memory/eventbus/config 改动面）无 `import executor` / `import capability_runtime`（见 `test_no_capability_runtime_import`）。
- **无数据删除**：零 DROP/TRUNCATE；新增仅为来源/适配/测试。
- **Memory ≠ Context**：Memory 仍经 P4.2/P4.3 Canonical Memory API 与 `DefaultEvolutionPolicy`；Context 不自建第二套 retrieval / 不直连 DB 作 Memory Store。
- **跨源去重可校验**：所有来源声明明确、互不相交的 `dedup_scope`，`dedup_items_by_scope` 机器可校验。

## 五、测试

新增：`tests/test_p4_context_contract.py` / `test_p4_context_budget.py` / `test_p4_context_dedup.py`（共 24 用例，全绿）。
覆盖：facade 唯一入口、blob 移除（Legacy Bypass Gate）、canonical retrieval、RuntimeState/CorePrompt 契约、全部来源 `ContextItem` + `dedup_scope`、跨源 dedup、`ContextRanker`、`ContextBudget` 四档位、单源故障隔离、Legacy 回退、`CONTEXT_BUILT` 事件、无 `capability_runtime` import。

## 六、交付物

1. `P4.4-CONTEXT-BASELINE.md`（PHASE 0）
2. `P4.4-IMPLEMENTATION-REPORT.md`（本文件）
3. `P4.4-CONTEXT-BYPASS-AUDIT.md`
4. `P4.4-REGRESSION-GATE.md`
5. `P4.4-UI-FREEZE-AUDIT.md`
6. `tests/test_p4_context_contract.py` / `test_p4_context_budget.py` / `test_p4_context_dedup.py`

## 七、Gate 结论

```
A = 0   B = 0   Collection Errors = 0
Security Regression = 0   UI Change = 0
P4.4 REGRESSION GATE = PASS
P4.4 IMPLEMENTATION GATE = PASS
```
