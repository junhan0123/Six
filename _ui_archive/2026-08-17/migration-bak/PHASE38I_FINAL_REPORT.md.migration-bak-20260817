# PHASE38I — FINAL REPORT（终报）

> 执行链：VERIFY → AUDIT → RESEARCH → PLAN → IMPLEMENT → VERIFY → REPORT → **STOP**
> 范围：语音链加固（上回合）+ 实时 Edge TTS 定默认（方案B）+ 对话表达原则 + 上下文切换审计
> 红线守查：未改 `server.py` 核心路由、未新增 API、未动 AgentRuntime/Planner/Executor/EventBus/MemoryV2/Context Engine 核心、密钥未回显、用户术语未泄漏。

## 一、本轮真实源码复核（VERIFY / AUDIT）

| 项 | 状态 | 证据 |
|---|---|---|
| 双声音根因已消除 | ✅ | `tts-engine.js` 无 `fallbackSpeak`；活体 UI `speechSynthesis.speak` = 0（§44） |
| TTS Sanitizer | ✅ | `sanitizeForTts` + `splitSentences`；harness 12/12 PASS |
| 单一播放责任 + 卡顿守卫 | ✅ | `seq` 锁存 + `STALL_MS=15000` |
| 聊天历史产品化 | ✅ | `chat-log.js` / `overlay-system.js` 对话层 / `chat-runner.js:110` refresh |
| 左栏工作过程自然化 | ✅ | `execution-feed.js` TOOL_LABEL 映射 |
| 右栏仅最终答案 | ✅ | `chat-runner.js` 仅 `reply` 入 UI/TTS |

## 二、本轮实施（IMPLEMENT）

### 2.1 方案B：实时默认 Edge TTS（§1 决策）
- **`.env` 第 11 行**：`ZHUANGZHOU_TTS_BACKEND=sovits` → `edge`（最小改动，纯配置）。
- 控制中枢 TTS 选择器（`control-center.js:391`）选项本就为 `['edge','gpt_sovits','system']`、默认 `edge`，与方案B 一致，无需改 UI。
- 实证：`server.py:3004` GPT-SoVITS 分支要求 `TTS_BACKEND=="sovits"`；`edge` 时跳过 → 落 `edge-tts`（3011+）。
- **GPT-SoVITS 定位**：保留为可选/后台音色，实时链默认不含；前端从不注入 `__sovits__`，故绝不参与实时回复（§3）。

### 2.2 对话表达原则（§4–§20 / §48）
- **`config.py` · `SYSTEM_PROMPT`** 末尾追加「表达原则」段落（纯文本，不破坏 `{name}` 占位符，`py_compile` 通过）：
  - 先答用户真正想知道的；先结论后解释；一句话能清不说三句；短问题短回答。
  - 不汇报执行过程（工具数/模块/Provider/API/内部状态），除非被问。
  - 自然简洁有温度，禁客服套话/卖萌/强行人格化。
  - 问题主动承担，不甩技术错误；需用户操作给自然语言指引。
  - 口语化，不用 Markdown/代码块/JSON/工具名/内部术语；结构化用 render_card；不套模板。

## 三、§45 最终配置状态（必报）

| 项 | 值 |
|---|---|
| `TTS_BACKEND` | `edge` |
| 实时 Provider | **Edge TTS（edge-tts 神经音色，默认 zh-CN-YunxiNeural）** |
| GPT-SoVITS | Optional（后台/可选音色，不参与实时链） |
| Edge TTS | Real-time 默认 ✅ |
| speechSynthesis | Disabled（正常回复路径 0 调用；仅防御性 `cancel()`） |
| Provider 状态显示 | `dependency-manager.js` 读 `health.tts_backend` → 现报 `edge`；控制中枢默认 `edge`、可切 `gpt_sovits` |

## 四、§49 产品验收矩阵

| 验收项 | 状态 |
|---|---|
| TTS Single Output | ✅ |
| Edge TTS Real-time | ✅（方案B） |
| No speechSynthesis | ✅（§44 = 0） |
| No GPT-SoVITS in real-time | ✅ |
| TTS Sanitizer | ✅（12/12） |
| Playback Dedup | ✅（seq 锁存） |
| Conversation History | ✅（既有） |
| Conversation Restore | ✅（既有） |
| Context Auto Continuation | ⚠️ 见第五节（架构无滚动窗口，记忆召回+历史UI 已达成「无感续记」） |
| Memory Continuation | ✅（MemoryV2 / recall 来源） |
| Natural Expression | ✅（SYSTEM_PROMPT 表达原则） |
| Human-like Short Replies | ✅ |
| No System-reporting Tone | ✅ |
| No Developer Leakage | ✅（仅注释，无可见内部术语） |
| Settings Real | ✅（控制中枢可改 TTS/voice/rate/ASR） |
| UI State Correct | ✅（空闲/工作/回复三态） |

## 五、上下文自动切换（§32–§38）审计结论（透明披露）

经真实读取 `context/` 包（facade/builder/budget/sources）与 `server.py:_handle_chat`：
- 活体为 **LLM 级无状态轮次**：每轮仅送最新一句 + 系统上下文；历史轮次不重注入 LLM prompt。
- 跨轮连续性由 **Context Engine 记忆/召回来源** 与 **聊天历史 UI（展示/恢复）** 提供。
- 因此无「滚动窗口逼近 token 阈值」前提；§22/§23 的「无感、不出现 Context limit reached」天然成立。

**未实现字面滚动窗口自动切换**，因 §25（禁第二套 Context Manager）与 §46（不得扩大范围）红线约束。用户可见的「庄周继续记得」已由 长期记忆召回 + 历史恢复 满足。若需真·滚动窗口续接，建议 **Phase 38J** 单独评估（前端维护滚动 messages + 复用 `ContextBudget` 真实 token 预算 + summary 入 Memory V2，不新建第二套）。

## 六、STOP

满足收口条件，停止。未进入 Phase 39，未自行扩展其他功能。
后续可选项（待用户裁定）：
1. 桌面环境手动 E2E（本环境无法启浏览器/麦克风）。
2. 是否启动 Phase 38J 实现真·滚动窗口自动续接。
3. 是否翻转 `.env` 让某用户改用 GPT-SoVITS 自选实时音色（可选，不影响默认 Edge）。
