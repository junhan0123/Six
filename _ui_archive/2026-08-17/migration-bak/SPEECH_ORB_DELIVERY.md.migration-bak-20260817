# 小6 · 语音球改造交付说明（2026-08-16）

> 环境故障说明：本会话子进程创建工具全部崩溃（0xC0000142），无法执行 git/语法检查/运行验证。
> 以下改动已完成（write/edit 落盘），待环境恢复后需：语法检查 + 全链路验证 + git 提交。

## 一、小6 桌宠 → 纯语音球（白龙马风格移植，独立设计）

| 文件 | 说明 |
|---|---|
| `desktop-avatar/orb-pet.html` | 新语音球页面：点云能量球 + 状态文字 + 执行/完成对话框（独立样式，青色能量风格，非 bailongma 原样） |
| `desktop-avatar/orb-pet.js` | 点云粒子球渲染核心（Fibonacci 球面采样 + 正弦噪声脉动，8 种状态配色/振幅/转速） |
| `desktop-avatar/orb-pet-voice.js` | 语音驱动：VAD 持续监听 → `/api/asr` → `/api/chat`(SSE) → 执行事件跟踪 → `/api/speak`；点击球开关麦克风 |
| `gui/main.js` | 桌宠窗口：150→320px，PET_URL 指向 orb-pet.html |

### 球状态动态
- idle/listening：青色缓慢呼吸
- recognizing：蓝色快速脉动（听到声音）
- thinking/processing：紫色旋涡（思考/处理）
- executing：红色能量环绕（工具执行中）
- speaking：琥珀金高亮（朗读回复）
- done：绿色闪烁（完成）
- error：红色（麦克风不可用）

### 执行/完成对话框（参照参考图样式实现）
- 执行任务时右侧滑入「⚡ 小6 正在执行」：显示用户意图 + 当前工具步骤（tool_start/tool_end 实时更新）+ 流式回复
- 完成后右下弹出「✅ 任务完成」：显示用户输入 + 小6 回复
- 球窗口交互：球体区域可点击（开关麦克风）/ 可拖拽移动，球外透明区鼠标穿透

## 二、Qwen3-TTS 默认声线 + Base 克隆接口

| 文件 | 说明 |
|---|---|
| `qwen3_tts.py` | 新增：Qwen3-TTS-12Hz-1.7B-CustomVoice 合成（OpenAI 兼容 `/v1/audio/speech`）+ 音色克隆（reference_audio / QWEN3_TTS_CLONE_URL） |
| `config.py` | 新增 QWEN3_TTS_URL/MODEL/VOICE/CLONE_URL/REF_AUDIO 配置项（ENV_KEYS + reload + global） |
| `server_handlers_chat.py` | `_handle_speak` 加 `TTS_BACKEND=qwen3` 分支（默认声线；配参考音频自动克隆；失败降级 edge-tts） |

### 使用方式
```env
# .env 已实际启用（2026-08-16）：默认声线 Qwen3-TTS-12Hz-1.7B-CustomVoice
ZHUANGZHOU_TTS_BACKEND=qwen3
QWEN3_TTS_URL=http://127.0.0.1:8001/v1     # vLLM 部署 Qwen3-TTS 服务
QWEN3_TTS_MODEL=Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice
# 音色克隆（Base 接口预留）：已配置复用 xiao6.wav 参考音频
QWEN3_TTS_REF_AUDIO=D:/GPT-SoVITS/voices/xiao6.wav   # 或 base64 data URI
QWEN3_TTS_CLONE_URL=                         # 可选独立克隆服务
# 验证：GET /api/health 的 tts_backend 字段应为 "qwen3"
```

vLLM 部署命令（参考 [vllm-omni Qwen3-TTS 文档](https://github.com/vllm-project/vllm-omni/blob/v0.24.0/recipes/Qwen/Qwen3-TTS.md)）：
```bash
vllm serve Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice --dtype bfloat16
```

## 三、三个开源项目研究结论

### 1. sherpa-onnx（k2-fsa）— [GitHub](https://github.com/k2-fsa/sherpa-onnx)
- **定位**：基于 next-gen Kaldi + onnxruntime 的本地离线语音框架（STT/TTS/说话人分离/增强），无需联网
- **可借鉴**：① 本地语音推理完全离线（隐私优先，与小6 FunASR 同理念）；② 跨语言/跨平台（Web/嵌入式/服务器）；③ Bailongma 已用 sherpa-onnx-node 1.13.3 —— 可作为 Qwen3-TTS 之外的本地 TTS 备选

### 2. HeyClicky — [介绍](https://moge.ai/tw/product/heyclicky)
- **定位**：语音驱动的 Mac AI 伙伴，能看懂屏幕、实时指导操作、派 agent 完成任务
- **可借鉴**：① **语音驱动任务执行闭环**（说→理解→执行→汇报）——正是小6 语音球的形态；② 屏幕理解（小6 已有 FEATURE_PERCEPTION 可联动）；③ 进度反馈 UI（执行过程可视化）

### 3. analysis（alive-analysis 等）— [GitHub](https://github.com/with-geun/alive-analysis)
- **定位**：AI 分析工作流框架（ALIVE loop：Ask→Look→Investigate→Voice→Evolve），可追踪的分析流程
- **可借鉴**：① 分析流程的结构化追踪（Ask/Look/Investigate）→ 对应小6 执行对话框的步骤展示；② "Voice" 环节 = 结果语音化汇报 → 对应完成对话框 + TTS 朗读

## 四、待办（环境恢复后）
1. 语法检查 + API 探测：`node G:\ZhuangZhou\zhuangzhou-ui\scripts\verify-speech-orb.js`（一键）
2. 重启后端 + 桌宠，验证：球渲染/麦克风/ASR/对话/执行对话框/完成对话框
3. 验证 Qwen3 声线：`GET /api/health` 的 `tts_backend` 应为 `qwen3`（需先 `vllm serve Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice`）
4. git 提交

## 五、审查修复记录（subagent 人工审查发现并已修复）

| 文件 | 问题 | 修复 |
|---|---|---|
| `qwen3_tts.py` L109 | `b"\xff\xe"` 非法 `\x` 转义 → 整模块编译期 SyntaxError → qwen3 后端静默降级 edge，功能失效 | 改 `b"\xff\xe0"`（MP3 帧同步 0xFFE0）✅ |
| `config.py` reload() | global 列表漏 `FEATURE_PERSONAL_AI` / `FEATURE_COMPUTER_ACTION` → .env 无法控制这两个开关 | 已补入 global 声明 ✅ |
| `orb-pet-voice.js` startMic | AudioContext 用默认采样率（可能 44100/48000），WAV 头硬编码 16000 → ASR 失真/变速 | 改为 `new AudioContext({ sampleRate: 16000 })` 固定 16k ✅ |

非阻塞提示（审查提出，未改）：
- `orb-pet.js` fibSphere n===1 除零（调用参数 2200/900 实际无碍）
- `config.py` ENV_KEYS 有死键（ZHUANGZHOU_DEFAULT_CITY/LOCATION/PROACTIVE_* 无对应 reload 读取）
- `server_handlers_chat.py` _handle_stream finally 的 for-else 是误用（行为恰好正确）
