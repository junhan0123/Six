# PHASE 6 — REAL USER ACCEPTANCE BASELINE

> 建立方式：VERIFY-BEFORE-CHANGE。重新读取真实源码（不依赖 Phase 5 报告假设）+ 真实运行后端探测（LIVE @127.0.0.1:8010）。
> 日期：2026-08-17。环境：Windows / Administrator；本 agent 为 headless（无 GUI 显示会话、无屏幕截图能力、无麦克风音频输入）。
> 用途：作为 Phase 6 真实用户验收的唯一真实基线；所有 file:line 均于本次重新核验。

---

## 1. Electron Entry — `electron/main.js`

- Canonical 单一壳：root `package.json` `"main":"electron/main.js"`（Phase 2 裁决）。
- `bootstrap()`（L80）经 `resolveBackendPort(ZHUANGZHOU_PORT||'8010')` **仅探测端口身份**（L48-54, L82-88），**不拉起后端**（`server.py` 由 launcher 负责）。不构成第二 Runtime。
- 创建两窗口共享 `backendBaseUrl()=http://localhost:<port>`（L90-103）：
  - `avatarWin` ← `desktop-avatar/dyna-orb.html`（透明置顶桌宠）
  - `workspaceWin` ← `zz-space/index.html`（三栏主界面）
- `ensureSoVITS()`（L56-78）：可选 GPT-SoVITS 自启，路径守卫缺失即跳过，**不阻断主流程**。
- IPC 仅 `avatar:*`：`close/hide/focus/open-chat/set-ignore-mouse/move`（L108-146）——**零执行语义**。
- Tray tooltip `"Six · Advanced Workspace"`（L178）；无托盘时不常驻（L202-205）。

## 2. Workspace Window — `electron/workspace-window.js`

- 1600×920，标题 `"Six · Advanced Workspace"`（L50）。
- `webPreferences`：`contextIsolation:true`、`nodeIntegration:false`、`webSecurity:true`、`sandbox:true`（L53-56）。

## 3. Avatar Window — `electron/avatar-window.js`

- `transparent:true`（L47）；`contextIsolation:true`、`nodeIntegration:false`（L56-57）。
- 加载 `dyna-orb.html`（URL 由 main.js 指定）。

## 4. Preload — `electron/preload.js`

- `contextBridge.exposeInMainWorld('electronAPI', { close, hide, openChat, setIgnoreMouse, moveWindow, focusOrb })`（L12-32）。
- 全部映射至 `avatar:*` IPC；**不暴露任何 node / exec / fs 能力**。

## 5. Security — `electron/security.js`

- fail-closed：`isSafeExternalUrl` / `isSafeNavigation` 仅放行 `http(s)://localhost|127.0.0.1`，**拒绝 userinfo / 非白名单 scheme**（L21-22, L45-65）。
- 异常一律视为不安全。

## 6. Workspace — `zz-space/index.html` + `js/zz-workspace.js`

- `index.html`：`<title>小6 · Advanced Workspace</title>`（L6）；品牌 `小6`（L17）；顶栏 / 左导航 / 中栏 Home+Conversation 四 Tab+列表 / 右上下文栈 / Command Palette / Toast+Banner / 输入区 / 流式 markdown / Tool·Approval·Result block / responsive 均存在（核验 L6,17,23,32,85,130,136,161,189,200）。
- `zz-workspace.js` `FEATURE_REGISTRY`（L567-615）：**47 条**，字段 `{id,name,cat,vis}`，`vis` ∈ `default/advanced/hidden/conditional`；`featureVisible()` 对 `calendar/focus-app/clipboard` 做 conditional gate（L617-627）。
- `COMMANDS` + 派生命令（L630-646）；`openPalette/renderPalette` 模糊匹配（L658-667）。
- SSE 解析（L220-242）：`getReader()` 行解析、`[DONE]`/`"DONE"` 终止、`zhuangzhou_event` 分发 `tool_start/tool_end/agent_state/hud_state/proactive/wakeword_detected`。

## 7. Voice Orb — `desktop-avatar/dyna-orb.js`

- `STATES = { idle, listening, thinking, speaking, error }`（L17-25）——**冻结 5 态契约 intact，未修改**。
- `window.ZZDynaOrb.setState(s)`（L237,242），默认 `'idle'`。

## 8. Backend — LIVE @127.0.0.1:8010

- `/api/health`（本次实测）：`{status:"alive", model:"agnes-2.5-flash", provider:"agnes", tts_backend:"qwen3", ai_name:"小6", theme:"dark-cyan", memory_graph:true, key_present:true, tools:[...]}`。
- `/api/config`：`ai_name:"小6"`、`theme:"dark-cyan"`、`build_channel:"rc"`、LLM `agnes-2.5-flash`。
- `/api/ready`：`{ready:true, ok:false, degraded:true, key_present:true}`——核心聊天/工具可用；部分可选子系统降级（与 Phase 5 一致）。

## 9. SSE

- `POST /api/chat`，body `{messages:[{role,content}]}`（实测 `"messages required"` 当缺该键）。
- 本次实测返回：`data: {"choices":[{"delta":{"content":"在。"}}]}` → `data: "[DONE]"`。

## 10. Tool Event（本次实跑确认）

- 查询 `"现在几点了"` → SSE 流真实出现：`zhuangzhou_event` → `tool_start` → `get_time` → `zhuangzhou_event` → `tool_end` → `get_time`。
- User→Chat→Tool Start→Tool End→Result 链路在运行时确证。

## 11. Approval Event

- `.env`：`AGENT_POLICY_DEFAULT=auto`（L50）。策略：低危工具 `auto` 自驱，高危工具仍 `confirm` 审批（Phase 5 已核验 Policy Engine 边界）。
- `tool_started` 经 EventBus 发布；高危工具需 Workspace Approval UI 确认；Core Policy 为最终控制点。
- 本 headless 环境未执行任何会触发高危审批的安全查询（见 FINAL §8）。

## 12. TTS

- `.env`：`ZHUANGZHOU_TTS_BACKEND=qwen3`（L14，当前生效，与 `/api/health` 一致）；默认在线音 `zh-CN-YunxiNeural`（`ZhuangZhou_TTS_VOICE`，L4）。
- 可选：`GPT-SoVITS` @ `http://localhost:9880`（`ZHUANGZHOU_GPT_SOVITS_URL`，L15）；`Qwen3-TTS` 本地 @ `http://127.0.0.1:8001`（`QWEN3_TTS_URL`，L21）。
- 设计契约：`thinking` / 工具内部事件 / 审批内部事件 **不被朗读**；仅最终回答发声（事件流中未作为语音暴露）。

## 13. Current Launch Method

- 后端：`server.py`（由 `zz_launch.bat` / `zz_launch.ps1` 拉起）→ `http://localhost:8010`。
- Electron：规范壳 `electron main.js` → 加载 `dyna-orb.html` + `zz-space/index.html`。
- Six Hub（`G:\xiao6-hub`）：独立 Presentation/Adapter 壳，`features.json` 47 条路由层（非第二 Runtime）。

## 14. Current Port

- **8010**（`ZhuangZhou_PORT=8010`，`.env` L58）。`8000/9880/3080/3210` 与 Phase 5 基线一致，未变。

## 15. Current Six Brand

- 中文 **小6**（`AI_DISPLAY_NAME=小6` 见 Phase 4；`index.html` 品牌 `小6`）；英文 **Six**；Hub **Six Hub**。
- 内部标识符全部保留（红线）：`ZHUANGZHOU_*`、`window.zhuangzhou`、`localStorage.zhuangzhou_sid`、`zhuangzhou_event`、`appId`、物理路径、仓库 URL、IPC、EventBus。
- 唤醒词 `庄周 / 小周 / 小6` 仅审计未改（Voice 配置，按设计保留）。
