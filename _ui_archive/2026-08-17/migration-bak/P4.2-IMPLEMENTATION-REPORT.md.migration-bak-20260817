# P4.2 IMPLEMENTATION REPORT

> 阶段：P4.2 — Canonical Memory API + Distiller Correction（实现阶段，DESIGN 已通过）
> 项目：`G:\ZhuangZhou\zhuangzhou-ui` · Core v1.4.0
> 前置：P0 / P0.5 / P1 / P2 / P3 PASS；P4 Phase 0 BASELINE PASS；P4 Phase 1 DESIGN GATE PASS

---

## 1. Verdict

**P4.2 IMPLEMENTATION = PASS**

- 不变量：UI 永久冻结（HTML/CSS/UI JS = 0 改动）；不重建 Memory 系统；不 DELETE 旧实现；不做数据破坏性迁移；安全边界 untouched；测试无作弊。
- 所有生产 `memories` 写入收敛到 Canonical Memory API；蒸馏器 confidence/source 落真实列；content_hash 幂等；持久化成功后才发领域事件。
- 契约测试 `tests/test_p4_memory_contract.py`：**20 passed**。
- Legacy Bypass Audit：**PASS**。
- 全量回归：**A=0, B=0，仅 6 个环境/历史遗留失败（均未触碰的模块）**，无 P4.2 引入的新未分类失败。

---

## 2. Scope（P4.2 §4，仅 A–G）

| 项 | 内容 | 状态 |
|----|------|------|
| A | Canonical Memory API（CREATE/READ/UPDATE/ARCHIVE/RESTORE/SEARCH/RETRIEVE/CONSOLIDATION/PROVENANCE/DEDUPLICATION） | ✅ |
| B | Canonical 写入路径（Caller → API → 校验 → dedup → 持久化 → 事件） | ✅ |
| C | 读适配器（search/retrieve 映射既有引擎） | ✅ |
| D | Distiller 修正（confidence/source 落真实列） | ✅ |
| E | Legacy 调用方适配器（distiller/import/person/archive handler） | ✅ |
| F | 最小 memory-event 基础设施（持久化成功后发布） | ✅ |
| G | 回归 | ✅ |

**显式推迟（不在 P4.2 范围）：** Context Source 重构、Retrieval 全量切换、MemorySource 拆分、Knowledge 重构、MemoryV2 淘汰、Lifecycle 全量整合。

---

## 3. Files Changed

| 文件 | 改动 | 性质 |
|------|------|------|
| `memory.py` | 文件末尾追加 Canonical Memory API（约 350 行）：`create_memory` / `get_memory` / `update_memory` / `archive_memory` / `restore_memory` / `delete_memory` / `upsert_memory` / `search_memories` / `retrieve_memories` / `consolidate_memories` + 私有助手与 `_publish_memory_event` | 新增 API（不删不改旧逻辑） |
| `memory_distiller.py` | `_persist` 改走 `memory.create_memory`，confidence/source 作为显式参数（不再塞 tags） | 修正 |
| `db.py` | `import_memories` / `upsert_memory_by_mem_id` / `upsert_person` / `archive_memory` 四条 writer 改走 Canonical API | 适配器 |
| `tests/test_p4_memory_contract.py` | 新增 20 个契约 + 静态/运行时 bypass 测试 | 测试 |
| `tests/test_goal_decision_engine.py` | 修复 `test_distill_habit`/`test_distill_preference` 的全局 `_persist` 污染（补 `finally` 还原） | 测试隔离修复（未改任何断言） |

**未改动：** 任何 `.html` / `.css` / 前端 `.js`；`capability_runtime` / `executor` / `tools` / `server_handlers_chat.py` 安全边界；既有 `memory_evolution` / `memory_intelligence` 治理逻辑（按 §4 推迟）。

---

## 4. Canonical Memory API

入口单一、唯一权威：所有 `memories` 写入均须经此。关键契约：

- **`create_memory(content, event_type, *, title, detail, mem_id, entities, concepts, tags, links, salience, source_ref, visibility, confidence, source, status, verified_at, timestamp)`**
  - 校验：content 非空；`confidence` 钳制到 `[0.0, 1.0]`；`status` 归一化到 `active|deprecated|conflict|pending_review|consolidated|decayed`。
  - 幂等：`ON CONFLICT(content_hash) DO NOTHING`，返回新行 `id` 或冲突时 `None`。
  - 事件：仅当 `cur.rowcount == 1` 时发布 `MEMORY_CREATED`（持久化之后）。
- **`update_memory`** / **`upsert_memory`**：跳过 `None` 字段；content 变更会重算 content_hash 并在与他处冲突时中止（防静默覆盖）。
- **`archive_memory` / `restore_memory` / `delete_memory(tombstone=True)`**：逻辑删除 → `status='deprecated'` + `archived=1`，不物理删除。
- **`search_memories` / `retrieve_memories` / `consolidate_memories`**：读/治理适配器，映射到既有 `memory_query.query_memory` / `memory_evolution.default.retrieve` / `.consolidate`。

---

## 5. Canonical Write Path

```
Caller (distiller / import / person / server-handler)
   → memory.create_memory | upsert_memory | archive_memory | restore_memory
       → 校验 + content_hash 归一化 + dedup(ON CONFLICT DO NOTHING)
       → 持久化（memories 表，唯一权威）
       → _publish_memory_event（持久化成功后，best-effort，绝不阻塞写入）
```

`db.py` 四条 writer 已全部改走上述 API，无直接 `INSERT/UPDATE/DELETE FROM memories`（migration 与 canonical 内部除外）。

---

## 6. Distiller Correction（P4.1 发现 → P4.2 修正）

**P4.1 根因：** `memory_distiller._persist` 把 `confidence`/`source` 错误塞进 `tags` JSON，违反 P4-MEMORY-CONTRACT（应为 `memories` 真实列）。

**P4.2 修正后：**
```python
memory.create_memory(
    content, event_type=it.get("type"), title=it.get("type"),
    confidence=it.get("confidence", 0.7),
    source=it.get("source", "heuristic"),
    tags={"session_id": session_id, "type": it.get("type")},
)
```
- `confidence=0.7`、`source='heuristic'` 写入 **真实列**；
- `tags` 仅承载 `session_id` / `type`（不再含 confidence/source）；
- 契约测试 `test_distiller_correction_writes_real_columns` 在真实 DB 上断言：`confidence==0.7`、`source=='heuristic'`、`tags` 无 confidence/source 键。

---

## 7. Dedup / content_hash

- 归一化：`db.memory_content_hash(content)` 对 content 做空白归一后 sha1。
- 唯一约束：`memories.content_hash TEXT UNIQUE`（schema 已存在）。
- 写入：`ON CONFLICT(content_hash) DO NOTHING` → 同内容二次写入返回 `None`，不新增行、不重复发事件。
- 覆盖测试：`test_dedup_idempotent` / `test_content_hash_normalization` / `test_archive_then_recreate_idempotent` / `test_idempotency_same_mem_id_twice` / `test_upsert_mem_id_conflicting_content`。

---

## 8. Memory Event

- 经 `eventbus.publish_domain(name, payload)`；事件名须注册于 `DOMAIN_EVENT_NAMES`（MEMORY_CREATED / MEMORY_UPDATED / MEMORY_STORED / MEMORY_LINKED / MEMORY_ARCHIVED）。
- **发布顺序：持久化成功之后**（`test_event_after_persistence` 断言仅一次 `MEMORY_CREATED` 且 payload 含 content_hash；重复写入不再发事件）。
- `_publish_memory_event` 懒导入 `eventbus`，best-effort，异常不影响持久化。

---

## 9. Legacy Caller Adapter

| 调用方 | 改造 |
|--------|------|
| `memory_distiller._persist` | → `memory.create_memory`（confidence/source 真实列） |
| `db.import_memories` | → `memory.create_memory`（content_hash 幂等 + 同源预清） |
| `db.upsert_memory_by_mem_id` | → `memory.upsert_memory` |
| `db.upsert_person` | → `memory.upsert_memory({event_type:'person', mem_id:'person_<hash>'})` |
| `db.archive_memory`（被 `server_handlers_memory.py:89` 调用） | → `memory.archive_memory` / `memory.restore_memory`（本次新增修复，消除服务端 handler 旁路） |

---

## 10. Migration（幂等，无新增 schema 破坏）

- **无新增迁移**：`memories` 已具备 `archived` / `confidence` / `source` / `status` / `verified_at` / `content_hash`（由 `_migrate_memories` + `_migrate_memory_truth` 既有幂等迁移补齐）。
- 契约测试 `test_existing_migration_idempotent`：在「当前库」与「半迁移裸表」上重跑既有迁移，均列齐全、无异常、不删数据。
- 全仓无任何 `DROP` / `TRUNCATE`；半迁移补列用 `ALTER TABLE ... ADD COLUMN`（缺列才加）。

---

## 11. Security Boundary（§2 红线）

- grep 确认 `capability_runtime` / `executor` 仅由 `server_handlers_chat.py` / `tools.py` 调用；`memory` / `context` / `cognitive` / `memory_distiller` 从不 import/调用 capability 运行时。
- 策略链 `Policy → Permission → Approval → Execution` 未被绕过；Memory API 不触碰权限/审批运行时。
- 无新增对外动作（发邮件/发推等）；仅对内读/写/治理。

---

## 12. UI Freeze

- `git diff --stat` 对 `*.html` / `*.css` / 前端 `*.js`：**0 改动**。
- P4.2 仅触及后端 `memory.py` / `memory_distiller.py` / `db.py` 与测试，**未触碰任何 UI**。

---

## 13. Regression Summary

- 契约测试：`tests/test_p4_memory_contract.py` → **20 passed**。
- 全量回归（详见 `P4.2-REGRESSION-GATE.md`）：**A=0, B=0**，仅 6 个环境/历史遗留失败（均未触碰模块），**无 P4.2 引入的新未分类失败**。

---

## 14. Gate

**P4.2 IMPLEMENTATION GATE = PASS**

| 维度 | 结果 |
|------|------|
| 不变量（§1/§2） | PASS |
| 契约测试 | PASS（20/20） |
| Legacy Bypass | PASS |
| 全量回归 A/B | 0 / 0 |
| UI 改动 | 0 |
| 安全旁路 | 0 |
| 数据丢失 / 破坏性迁移 | 0 |

---

## 15. Remaining / Follow-up（P4.3 跟踪，不影响本 GATE）

1. `consolidation.py:115` 直接 `INSERT` 合并产物记忆 → P4.3 改走 `memory.create_memory`（带 content_hash + 事件）。风险低。
2. `memory_intelligence` / `memory_evolution/*` 治理列更新 → P4.3 统一纳入 canonical 治理管线（当前为正向治理，维护真实 confidence/source 列）。
3. `import_memories` 同源预清 `DELETE`（db.py:262）→ P4.3 提供 canonical `delete_by_source_ref` 助手。
4. 6 个环境/历史遗留失败（phase41 gbk、phasec 子进程、test_db shell echo、test_external ffmpeg/status、agent_runtime KeyError 'status'）→ 属独立环境问题，建议单列跟踪，不在本阶段范围。

---

## 16. Execution Discipline（§21）

STEP1 VERIFY → STEP2 BASELINE → STEP3 IMPLEMENT → STEP4 TEST → STEP5 AUDIT → STEP6 FULL REGRESSION → STEP7 GATE → **STEP8 STOP**。

**P4.2 已完成，必须 STOP。等待下一阶段明确指令。**
