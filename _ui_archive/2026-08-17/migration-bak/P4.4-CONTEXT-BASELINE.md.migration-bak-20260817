# P4.4 — UNIFIED CONTEXT ENGINE · CONTEXT-BASELINE（VERIFY ONLY）

- **项目**：`G:\ZhuangZhou\zhuangzhou-ui`（Core v1.4.0）
- **身份**：ZhuangZhou（庄周）Senior Systems Architect / Runtime Engineer
- **阶段**：P4.4 PHASE 0 — VERIFY → BASELINE（本文件为只读审计产物，**未修改任何产品代码**）
- **红线遵守**：UI 永久冻结（零 UI 变更）；未改动任何 `.py` 产品源码；所有结论均来自真实源码逐行读取，无推断、无伪造。
- **前置状态**：P4.2 Canonical Memory API = PASS；P4.3 Memory Governance/Retrieval/Lifecycle = PASS；P4.3 STOP 已确认。
- **本基线锚定**：`P4-MEMORY-CONTEXT-BASELINE.md`（P4 总基线，已逐行复核并继承其结论）、`P4-CONTEXT-ENGINE-DESIGN.md`、`P4.2-IMPLEMENTATION-REPORT.md`、`P4.3-IMPLEMENTATION-REPORT.md`、`P4.3-MEMORY-BYPASS-FINAL-AUDIT.md`。

---

## 0. 方法

- 全量读取 `context/` 全部模块（含指令未列出的 `cognitive_sources.py` / `memory_recall_source.py` / `interfaces.py` / `serializer.py` / `cache.py` / `goal_source.py`）。
- 读取 `memory.py`（legacy 三函数 +  canonical `retrieve_memories`）、`memory_evolution/`（`retrieval_policy.py` / `context_source.py` / `__init__.py`）。
- 读取全部 11 个 Context Source 实现。
- 读取 `config.py` 特性开关（模块级默认 vs `reload()` env 解析层）以确认**运行期有效值**。
- 读取 `eventbus.py` `DOMAIN_EVENT_NAMES` 确认 `CONTEXT_BUILT` 是否存在。
- 全仓 grep 追踪 `build_context_prompt` / `build_system_prompt` / `build_memory_block` / `build_context_prefix` / `retrieve_memories` 的真实调用方。

---

## 1. 十六项 VERIFY 确认（指令 §四）

| # | 确认项 | 真实结论（file:line） |
|---|--------|----------------------|
| 1 | context.facade 唯一入口 | ✅ `context.facade.build_context_prompt(user_text, tier=T32K)` 为唯一入口；生产调用方：`server.py:468`、`server_handlers_chat.py:162/337`、`social_inbound.py:108/139`、`tools.py:3352`（均为 `system` 角色内容）。 |
| 2 | LegacyContextBuilder | `context/builder.py:27` `LegacyContextBuilder.build` → 五阶段 `Collect→Rank→Budget→Bundle→Build`（`:116-122`）。是"新管线"，但内部 `MemorySource` 委托回旧 monolith（见 #3）。 |
| 3 | MemorySource 当前行为 | ❌ `context/sources.py:27` `MemorySource.collect` → `memory.build_system_prompt(ctx.user_text)`（`:30`）→ 返回**单条完整 prompt blob** 作为 1 个 `ContextItem`（source=MEMORY, scope="memory"）。**这是对 Unified Retrieval 的旁路**。 |
| 4 | build_system_prompt 剩余调用方 | 定义 `memory.py:372`；生产调用方：`facade._legacy_prompt`(`facade.py:28`)、`MemorySource.collect`(`sources.py:30`)；其余仅测试。 |
| 5 | build_memory_block 剩余调用方 | 定义 `memory.py:237`；**仅被 `build_system_prompt` 内部调用（`:387`）**；无独立生产调用方。 |
| 6 | build_context_prefix 剩余调用方 | 定义 `memory.py:327`；**仅被 `build_system_prompt` 内部调用（`:387`）**；另有 dev 脚本 `e2e_d.py:60`。 |
| 7 | Runtime State 真实内容 | `build_context_prefix`（`memory.py:327-369`）聚合：`[当前上下文]` = 现在时间/星期 + 待办提醒(reminders done=0) + 当前焦点(recent_foci 6) + 未完成任务(get_open_tasks 5) + 定位(build_geo_block) + 热点(_cached_hotspot, 2s 超时) + 预取(prefetch 天气/新闻)。**全部塞进单块**。 |
| 8 | Memory Retrieval 当前真实 API | ✅ `memory.retrieve_memories(query, top_k=8)`（`memory.py:854`）→ `memory_evolution.default.retrieve`（`memory_evolution/__init__.py:66`）→ `DefaultEvolutionPolicy.retrieve` → `list[ScoredItem]`。返回后 best-effort 发 `MEMORY_RETRIEVED`。**当前无任何 Context Source 在生产路径调用它**。 |
| 9 | DefaultEvolutionPolicy API | ✅ `DefaultEvolutionPolicy.retrieve(query, top_k=8) -> list[ScoredItem]`（`retrieval_policy.py:35`）；组合 `memory_intelligence.recall` + `cognitive.episodic.recall_episodes`，过滤 `deprecated/consolidated/pending_review` 与 `archived`。 |
| 10 | ContextRanker API | ✅ `ContextRanker(weights).rank(items) -> list[ContextItem]`（`ranker.py:95`）；加权线性（source_priority .35 / priority .25 / recency .15 / importance .15 / user_relevance .10），稳定排序。 |
| 11 | ContextBudget API | ✅ `ContextBudget(max_tokens=None, tier=None, max_calls=None).apply(items)`（`budget.py:104`）；**默认无限预算（不裁剪）**；支持 `BudgetTier` T16K/T32K/T64K/T96K（models.py:44）。另含 `max_calls` 能力调用预算（FAIL CLOSED）。 |
| 12 | dedup_items_by_scope API | ✅ `dedup_items_by_scope(items)`（`models.py:97`）；按 `(scope, content)` 同域逐字节去重，无 scope 透传。**当前仅 `MemorySource`+`MemoryRecallSource` 声明 scope**，其余 Source 均未声明 → 跨源去重实际失效。 |
| 13 | 每个 Source 当前输出 | 见 §3 表。 |
| 14 | 重复读取 | MemorySource(blob) 经 `build_memory_block`+`build_context_prefix` 读 profile/memory_summary/chat_log/learnings/reminders/foci/tasks/hotspot/prefetch；同时 `UserModelSource` 读 `user_model`、`PersonalAISource` 经 `personal_ai` 再次聚合 user_model+personal_context+memory、`PersonalContextSource` 经 `os_bridge.personal_context` 再读 focus/tasks → **user_model 与 runtime state 被多源重复读**。 |
| 15 | 重复注入 | MemorySource blob 已含【用户画像】+【长期记忆摘要】+【近期对话记忆】+【自我学习经验】+【待办提醒】+焦点+任务+热点+预取；而 `PersonalAISource`/`UserModelSource`/`PersonalContextSource` 又各自注入画像/摘要/实时状态 → **画像/摘要/任务/焦点被多源重复注入**。 |
| 16 | 仍是生产路径的旧实现 | `memory.build_system_prompt`（经 `MemorySource` + `facade._legacy_prompt`）仍为事实上的记忆上下文源；`build_context_prefix`/`build_memory_block` 作为其内联子块仍活跃；`memory_v2` 已死（未注册）；`EvolvedMemorySource` 存在但默认 OFF（`FEATURE_MEMORY_EVOLUTION` 未定义→False）。 |

---

## 2. Production Caller Map（Caller → Context API → Source → Data → Ranking → Budget → Output）

```
User/Server/Tools
   │  server.py:468 · server_handlers_chat.py:162/337 · social_inbound.py:108/139 · tools.py:3352
   ▼
context.facade.build_context_prompt(user_text, tier=T32K)          [UNIQUE ENTRY]
   │  reads config.FEATURE_CONTEXT_ENGINE (dynamic)
   ├─ OFF/异常 ──▶ _legacy_prompt ──▶ memory.build_system_prompt(user_text)   [LEGACY FALLBACK]
   └─ ON ───────▶ LegacyContextBuilder().build(BuildContext)
                       │
                       ├─ Collect : SourceRegistry.collect(ctx)  ── 各 Source.collect → list[ContextItem]
                       ├─ Rank    : ContextRanker.rank(items)          [ranker.py:95]
                       ├─ Budget  : ContextBudget.apply(items)         [budget.py:104]  (默认 unlimited)
                       ├─ Bundle  : dedup_items_by_scope(items)        [models.py:97]
                       └─ Build   : "\n\n".join(i.content) → prompt_text [builder.py:154]
```

### 2.1 Source 明细（运行时有效注册，见 §6 配置漂移）

| Source（file:line） | 后端 Data | 输出 ContextItem | scope | flag（运行期有效值） |
|---|---|---|---|---|
| `MemorySource` (`sources.py:18`) | `memory.build_system_prompt` → 全 blob | 1× blob, source=MEMORY, prio 1.0 | `"memory"` | **总是注册** |
| `UserModelSource` (`cognitive_sources.py:15`) | `cognitive.user_model` (load/render) | 1×, source=USER, prio .95 | **无** | USER_MODEL = **True** |
| `EpisodicSource` (`cognitive_sources.py:46`) | `cognitive.episodic` (recall top_k=5) | 1×, source=EPISODIC, prio .6 | **无** | EPISODIC = **True** |
| `PersonalitySource` (`personality_source.py:13`) | `personality` (generate/render) | 1×, source=PERSONALITY, prio .3 | **无** | PERSONALITY = **True** |
| `GoalSource` (`goal_source.py:13`) | `goals.active_goals_snapshot(limit=3)` | 1×, source=GOAL, prio .7 | **无** | GOAL_SYSTEM = **True** |
| `KnowledgeSource` (`knowledge_source.py:18`) | `knowledge.search(q, limit=4)` | 1×, source=KNOWLEDGE, prio .6 | **无** | KNOWLEDGE_PLATFORM = **True** |
| `PersonalContextSource` (`personal_context_source.py:62`) | `os_bridge.personal_context()` | 1×, source=CURRENT_TASK, prio .95 | **无** | PERSONAL_CONTEXT = True |
| `PersonalAISource` (`personal_ai_source.py:15`) | `personal_ai.build_unified_personal_context()` | 1×, source=SYSTEM, prio .9 | **无** | PERSONAL_AI = True |
| `MemoryRecallSource` (`memory_recall_source.py:19`) | `memory_intelligence.build_block` | 1×, source=MEMORY, prio .9 | `"memory"` | MEMORY_INTELLIGENCE = True |
| `IdentitySource` (`identity_source.py:14`) | `identity.provider.IdentityProvider` | 1×, source=IDENTITY, prio .97 | **无** | IDENTITY = True |
| `PerceptionSource` (`perception_source.py:14`) | `perception.build_context_block` | 1×, source=WORLD, prio .85 | **无** | PERCEPTION_CONTEXT = False |
| `EvolvedMemorySource` (`memory_evolution/context_source.py:19`) | `DefaultEvolutionPolicy.retrieve(top_k=6)` | 1×, source=MEMORY, prio .92 | **无** | MEMORY_EVOLUTION = False |
| `MemoryV2Source` | `memory_v2.source` | — | — | MEMORY_V2_ENABLE = False（死） |

> **关键**：运行期（reload 后）除 `PerceptionSource`/`EvolvedMemorySource`/`MemoryV2Source` 外，**全部 10 个 Source 同时激活**，其中 `MemorySource` 是整块 blob + 其余 9 个粒度源 → 严重重叠（见 §4/§5）。

---

## 3. Legacy Context Path vs New Context Path

| 维度 | Legacy（旧 monolith） | New（context.builder 管线） |
|------|----------------------|----------------------------|
| 入口 | `memory.build_system_prompt` | `context.facade.build_context_prompt` |
| 组装 | 单函数拼装 persona+base+prefix+block | Collect→Rank→Budget→Bundle→Build |
| 记忆源 | `build_memory_block`（profile/summary/chat_log/learnings） | `MemorySource` 仍委托 `build_system_prompt`（**包裹旧 monolith**） |
| 运行时态 | `build_context_prefix`（时间/待办/焦点/任务/热点/预取） | 仍由 `MemorySource` blob 内联；`PersonalContextSource` 另注入一份 |
| 检索 | 无（整块读出） | `EvolvedMemorySource`(OFF) / `MemoryRecallSource`(独立路径) 均未走 canonical |
| 去重 | 无 | `dedup_items_by_scope`（仅 MemorySource/Recall 声明 scope，实际失效） |
| 回退 | — | `facade` 异常→`_legacy_prompt`（整段退回旧实现） |

**结论**：存在**两套上下文组装**，`context.builder` 当前只是"新管线外壳"——核心事实源 `MemorySource` 直接把旧 monolith 当单条 ContextItem 注入，Ranker/Budget 对内部子块无粒度。

---

## 4. Overlap（重叠）

1. **Memory 域三源重叠**：`MemorySource`(blob, scope=memory) + `MemoryRecallSource`(build_block, scope=memory) + `EvolvedMemorySource`(OFF, 无 scope)。三者 source 均为 `MEMORY`；blob 已含记忆块，Recall 又加一份关联记忆 → 同域重复（仅因内容不同，`dedup_items_by_scope` 抓不到）。
2. **User Model 双读**：`UserModelSource` 读 `user_model`；`PersonalAISource` 经 `personal_ai` 再聚合 `user_model`；`build_memory_block`(在 blob 内) 读 `profile` 并经 `canonical_project` 校验 → user_model 被 2~3 次读取。
3. **Runtime State 双注入**：`build_context_prefix`(blob 内) 注入时间/待办/焦点/任务/热点/预取；`PersonalContextSource` 经 `os_bridge.personal_context` 再注入焦点/任务/目标 → 任务/焦点重复。

---

## 5. Duplicate（重复注入，对用户可见）

| 事实 | 注入源（≥2） | 后果 |
|------|-------------|------|
| 用户画像/长期摘要 | `MemorySource` blob + `UserModelSource` + `PersonalAISource` | 同一用户信息 2~3 次出现，措辞可能不一致 |
| 待办/未完成任务 | `MemorySource` blob(`build_context_prefix`) + `PersonalContextSource`(`open_tasks`) | 任务列表重复 |
| 当前焦点 | blob(`recent_foci`) + `PersonalContextSource`(`focus`) | 焦点重复 |
| 记忆本体 | `MemorySource` blob(`build_memory_block`) + `MemoryRecallSource`(`build_block`) | 记忆块重复（不同渲染） |

> 由于默认预算无限（`ContextBudget` unlimited），这些重复**不会被裁剪**，全部进入 prompt。

---

## 6. Bypass（Legacy Bypass Gate 预审，对应 §18）

| 调用点 | 旁路性质 | 分类 | 目标 |
|--------|----------|------|------|
| `sources.py:30` `MemorySource.collect → memory.build_system_prompt` | 生产主路径直接调旧 monolith，绕过 Unified Retrieval | **生产 bypass（须消除）** | 改 `EvolvedMemorySource`（canonical `retrieve_memories`） |
| `facade.py:28` `_legacy_prompt → memory.build_system_prompt` | 仅 flag OFF / 异常回退；保留为 compat 兜底 | 兼容层（可保留） | 保持，但须确保 Unified 路径稳定后不再被主路径依赖 |
| `memory_recall_source.py:28` → `memory_intelligence.build_block` | 第二套检索路径（非 canonical `retrieve_memories`），scope=memory 与 MemorySource 重叠 | 重复检索/重叠 | 并入 canonical retrieval 或去重 scope |
| `context_source.py:26` `EvolvedMemorySource → DefaultEvolutionPolicy.retrieve`（直接） | 用了 canonical policy 但**未走 `memory.retrieve_memories` 适配器**（故不发 `MEMORY_RETRIEVED`） | 偏离指令 §C 链路 | 改为经 `memory.retrieve_memories` 调用 |
| `build_context_prefix`/`build_memory_block` | 仅被 `build_system_prompt` 内联；Runtime State 未独立成源 | 运行时态未拆分 | 拆出 `RuntimeStateSource` |

**目标（预审）**：生产主路径直接 legacy 组装 = 1 处（`MemorySource.collect`），必须经 P4.4 收敛为 0。

---

## 7. Runtime State 拆分（指令 §八，为 RuntimeStateSource 备料）

`build_context_prefix`(`memory.py:327`) 内容分类：

| 数据 | 类别 | 归属 Source |
|------|------|-------------|
| 现在时间 / 星期 | B. Runtime State | **RuntimeStateSource** |
| 待办提醒 (reminders) | D. Task State | RuntimeStateSource |
| 未完成任务 (get_open_tasks) | D. Task State | RuntimeStateSource |
| 当前焦点 (recent_foci) | C. User Context / D. Task State | RuntimeStateSource |
| 定位 (build_geo_block) | C. User Context | RuntimeStateSource |
| 热点 (_cached_hotspot) | E. External Observation | RuntimeStateSource |
| 预取 (prefetch 天气/新闻) | E. External Observation | RuntimeStateSource |

记忆本体（`build_memory_block`：profile/summary/chat_log/learnings）归 `EvolvedMemorySource`（canonical retrieval），**不**进 RuntimeStateSource。禁止简单复制整块 `build_context_prefix`，须逐项迁移。

---

## 8. 配置双默认漂移（指令 §十六，critical）

`config.py` 模块级默认与 `reload()` env 解析层**矛盾**：

| flag | 模块级默认 | reload() env 默认 | 运行期有效值 |
|------|-----------|------------------|--------------|
| FEATURE_CONTEXT_ENGINE | True (`:58`) | "true" (`:280`) | **True** |
| FEATURE_USER_MODEL | False (`:60`) | "true" (`:282`) | **True** |
| FEATURE_EPISODIC_MEMORY | False (`:61`) | "true" (`:283`) | **True** |
| FEATURE_PERSONALITY | False (`:65`) | "true" (`:286`) | **True** |
| FEATURE_GOAL_SYSTEM | False (`:67`) | "true" (`:306`) | **True** |
| FEATURE_KNOWLEDGE_PLATFORM | False (`:71`) | "true" (`:292`) | **True** |
| FEATURE_PERSONAL_CONTEXT | True (`:73`) | "true" (`:295`) | True |
| FEATURE_PERSONAL_AI | True (`:75`) | "true" (`:297`) | True |
| FEATURE_MEMORY_INTELLIGENCE | True (`:77`) | "true" (`:300`) | True |
| FEATURE_IDENTITY | (默认On) | — | True |
| FEATURE_PERCEPTION_CONTEXT | False (`:137`) | "false" (`:346`) | False |
| FEATURE_MEMORY_EVOLUTION | 未定义 | 未定义 | False |

**后果**：运行期除 `PERCEPTION_CONTEXT`/`MEMORY_EVOLUTION` 外全部为 True → `LegacyContextBuilder` 实际注册 **MemorySource(blob) + 9 个粒度源**，即 §5 重复注入的**根因**。本漂移属"Context 相关配置"，按 §16 允许在 P4.4 修复（仅限 Context 相关 flag，不得扩大范围）。

---

## 9. EventBus 状态（指令 §十四）

- `DOMAIN_EVENT_NAMES`（`eventbus.py:177`）已含：`MEMORY_RETRIEVED` / `MEMORY_CONSOLIDATED` / `MEMORY_DECAYED`（`:185`）。
- **`CONTEXT_BUILT` 不存在**（grep 未命中）。P4.4 须在 `DOMAIN_EVENT_NAMES` 登记 `CONTEXT_BUILT`，并在 Context 构建成功后（一次、best-effort、不阻塞）`publish_domain("CONTEXT_BUILT", {...})`。
- 禁止同义事件：`context.created` / `context.assembled` / `context.ready` 等。

---

## 10. 安全边界（指令 §二/§二十一）

- 全部 Context Source 单向 import 下游模块（`memory`/`cognitive`/`knowledge`/`goals`/`identity`/`personality`/`personal_ai`/`os_bridge`/`perception`/`memory_intelligence`/`memory_evolution`），**无 Source import `capability_runtime` / `executor` / `Tool` / `Permission` / `Approval`**。
- `context/` 不反向依赖执行层；`facade` 仅在函数内惰性 import `memory`/`config`。安全边界 intact。

---

## 11. Migration Target（指向，非实现；留待 PHASE 1 DESIGN）

| 现状 | 目标（方向） |
|------|--------------|
| `MemorySource` → blob | 以 `EvolvedMemorySource` 为唯一记忆源，经 `memory.retrieve_memories` → `DefaultEvolutionPolicy` → **逐条 ContextItem[]**（每条带 memory id / importance / recency / relevance / dedup_scope / provenance），不再整块注入 |
| Runtime State 在 blob | 新增 `RuntimeStateSource`，逐项迁移 §7 数据，独立 scope |
| 多源无 dedup_scope | 每源声明明确 scope（memory / user_model / episode / runtime_state / goal / knowledge / identity / personality / personal_context / personal_ai / perception），使 `dedup_items_by_scope` 真正生效 |
| `MemoryRecallSource` 独立路径 | 并入 canonical retrieval（或明确 scope 去重） |
| 配置双默认漂移 | 仅修 Context 相关 flag 的单一来源（§8） |
| `CONTEXT_BUILT` 缺失 | 登记并发布 |
| Legacy 函数保留 | `build_system_prompt`/`build_context_prefix`/`build_memory_block` 暂留作 compat 兜底；生产主路径不再直接依赖；最终 Legacy 生产调用 = 0 |

---

## 12. 红线合规（本阶段只读）

- ✅ 未修改任何产品代码（仅读取 + 本基线文档）。
- ✅ UI 零变更（HTML/CSS/前端 JS/布局/动画/资产/Home/Galaxy/Chat 均不动）。
- ✅ 未删除/伪造任何测试、schema、数据。
- 下一步（DESIGN/IMPLEMENT）继续遵守：Memory≠Context≠Knowledge 分层；Context 不直连 DB 作 Memory Store、不自建第二套 retrieval、Source 不做最终 ranking/prompt 拼接；先 verify→classify→migrate→test 再 deprecate，不删旧能力；不新建第二套 Context Engine；不破坏 P4.2/P4.3 Canonical Memory Contract。

---

*Generated by P4.4 Phase-0 read-only audit. Every claim traced to file:line. No code modified.*
