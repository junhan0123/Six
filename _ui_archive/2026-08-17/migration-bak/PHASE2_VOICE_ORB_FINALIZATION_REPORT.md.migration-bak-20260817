# PHASE 2 — VOICE ORB FINALIZATION REPORT

> 生成时间：2026-08-17 · 核验基线：`G:\ZhuangZhou\zhuangzhou-ui` 真实源码 + 运行中后端（`:8010` 健康检查 200）
> 纪律：VERIFY-BEFORE-CHANGE。Phase 2 = 最小 Presentation / Integration 收口。**未触碰任何 Core 权限**（Context/Memory/Decision/Policy/Approval/Lifecycle/Capability/Executor/Security）。无第二 Runtime/Memory/Capability/Policy/Lifecycle。DSH 仍为基础设施；Xiao6Hub 仍为 Presentation/Adapter。
> 依据前文：Phase 1 PRODUCT ARCHITECTURE FREEZE 已冻结产品方向（ZhuangZhou = 单产品；Voice OS + GUI OS = 两个 Presentation 层共享一个 Core）。Phase 2 不依赖 Phase 1 记忆，全部基于真实源码重新核验。

---

## 1. Executive Verdict

**PHASE 2 COMPLETE.** Voice Orb 已收敛为恰好两个 canonical 表现体：

- **Web Orb** = `zz-space` petOrb（入口 `zz-space/index.html`，状态机 `coreStage[data-state]`）
- **Electron Orb** = `desktop-avatar/dyna-orb`（入口 `desktop-avatar/dyna-orb.html` + `dyna-orb.js` + `dyna-orb-voice.js`）

入口冲突已裁决：`electron/main.js`（root `package.json` `"main":"electron/main.js"`）为 canonical Electron 入口；`gui/main.js` 为孤儿（无任何 launcher 引用），保留磁盘作历史/实验参考，**不参与生产启动**。全部 6 处 launcher 引用已统一指向 `zz-space/index.html`。dyna-orb 拖拽/鼠标穿透经 `electron/preload.js` + `electron/main.js` IPC 完整接线。竞争 orb（orb-pet / gem-orb / liquid-orb / orb-ui-orb / robot）已分类并**解除 active 接线（unexpose，不删）**。

§14 验收闸门 25/25 通过；§15（本报告）15 个章节齐备；§16 触发后 **STOP**。

**回归结论**：全量测试 `9 failed, 518 passed`（183s），与 P5.4 基线（518 passed / 9 failed / 0 errors）**完全一致** → Phase 2 引入 **0 回归**。9 个失败全部位于 `phase7` / `phase8` / `phasec_native_runtime` / `test_external`（Core/契约/外部依赖模块），**没有任何一项位于 orb / launcher / electron 表现层代码**。

---

## 2. Before / After Architecture

### Before（Phase 2 前）
- **Web 入口混乱**：launcher 同时引用过 `/final/index.html`（目录已不存在）、根 legacy UI、`/desktop-avatar/index.html`（旧 lottie robot）。
- **Electron 入口冲突**：`electron/main.js` 与 `gui/main.js` 均为潜在入口，`gui/main.js` 自带 dyna-orb + zz-space + 自动拉起后端/SoVITS + 托盘逻辑，但无 launcher 引用 → 真实生产路径不明确。
- **多竞争 orb 同存磁盘**：dyna-orb（设计选定）、orb-pet、gem-orb、liquid-orb、orb-ui-orb、robot 共存，互相竞争，无统一分类。
- **dyna-orb 拖拽/穿透桥缺失**：`setIgnoreMouse` / `moveWindow` 仅存在于孤儿 `gui/preload.js`，canonical `electron/preload.js` 未提供 → dyna-orb.html 内联调用无桥可通。
- **配置文档过期**：旧 `VOICE_ORB_CONFIG.md` 声称 “zz-space 已移除 / PET_URL 加载 / 主界面已移除”，与真实源码矛盾。

### After（Phase 2 后）
- **Web 入口唯一**：全部 launcher → `http://localhost:8010/zz-space/index.html`。
- **Electron 入口裁决**：`electron/main.js` → `createAvatarWindow({ url: /desktop-avatar/dyna-orb.html, preload: electron/preload.js })`；`gui/main.js` 标记 NOT CANONICAL 保留。
- **竞争 orb 分类收口**：dyna-orb = CANONICAL ELECTRON；petOrb = CANONICAL WEB；robot = LEGACY/FROZEN；orb-pet/gem-orb/liquid-orb/orb-ui-orb = DORMANT/EXPERIMENTAL/NOT EXPOSED（零生产引用，未删）。
- **dyna-orb 拖拽/穿透桥补全**：canonical `electron/preload.js` 新增 `setIgnoreMouse` / `moveWindow`，`electron/main.js` 新增对应 IPC。
- **配置文档重写**：`VOICE_ORB_CONFIG.md` 重写为 11 节 canonical 契约。

---

## 3. Canonical Entry Points

| 层 | Canonical 表现体 | 入口 | 加载链 |
|---|---|---|---|
| Web Orb | `zz-space` petOrb | `zz-space/index.html` → `js/zz-space.js` | 全部 launcher Web 步骤统一打开 |
| Electron Orb | `desktop-avatar/dyna-orb` | `desktop-avatar/dyna-orb.html` → `dyna-orb.js` + `dyna-orb-voice.js` | `launcher/*.bat\|ps1` → `electron .` → `electron/main.js` → `createAvatarWindow` |

全部 launcher 的 Web 步骤（已核验）：
- `launcher/zz_launch.bat:87-88` → `http://localhost:8010/zz-space/index.html`
- `launcher/zz_launch.ps1:44` → `http://localhost:{PORT}/zz-space/index.html`
- `start-zhuangzhou.bat:40`（electron 缺失 fallback）、`:56`（open_chat）→ `http://localhost:8010/zz-space/index.html`

---

## 4. Electron Entry Conflict Resolution

**裁决**：`electron/main.js` 为 canonical Electron 入口。
- 依据：`root package.json:6` → `"main": "electron/main.js"`；全部 launcher 调用 `electron .`（隐式加载 `package.json.main`）。
- `electron/main.js:43-46` 现显式 `createAvatarWindow({ url: base + '/desktop-avatar/dyna-orb.html', preload: preload })`。
- `gui/main.js`（+ `gui/preload.js` + `gui/package.json`）虽功能更全（自动拉起后端/SoVITS、托盘、dyna-orb+zz-space），但**无任何 launcher 引用** → 分类为 **NOT CANONICAL**，保留磁盘用于回滚与历史参考，**不删除、不接线**。

---

## 5. Web Orb Verification

- 入口文件：`zz-space/index.html`（HTTP 200 ✅）。
- 状态机宿主：`zz-space/js/zz-space.js`，经 `coreStage[data-state]` 驱动。
- 真实状态集：`idle` / `listening` / `thinking` / `speaking` / `error`（符合 §5 契约）。
- 全部 launcher Web 步骤统一打开 → 入口一致性 ✅。
- 后端契约未变更（Phase 2 仅 Presentation/Integration 收口）✅。

---

## 6. Electron Orb Verification

- 入口：`electron/main.js:44` → `desktop-avatar/dyna-orb.html`（HTTP 200 ✅）。
- 依赖脚本：`dyna-orb.js`（HTTP 200 ✅）、`dyna-orb-voice.js`（HTTP 200 ✅），均经 `node --check` 语法通过 ✅。
- 窗口交互（`dyna-orb.html` 内联脚本）：
  - `electronAPI.setIgnoreMouse(!onBall)`（球面命中→窗口接收事件；球外透明区→鼠标穿透）
  - `electronAPI.moveWindow(dx, dy)`（拖拽移动）
- 桥接链已补全：
  - `electron/preload.js:24-29` 暴露 `setIgnoreMouse` / `moveWindow` → `ipcRenderer.send('avatar:set-ignore-mouse' / 'avatar:move')`
  - `electron/main.js:73-86` 注册 `avatar:set-ignore-mouse`（→ `win.setIgnoreMouseEvents(ignore,{forward:true})`）、`avatar:move`（→ `win.setPosition(x+dx,y+dy)`）
- `window.ZZDynaOrb` API（`dyna-orb.js`）：`init / start / stop / setState / setVolume / getState`；内部过渡态 `recognizing/processing/executing/done` 自动回落 `idle` ✅。
- 默认回退 URL：`electron/avatar-window.js:87` → `http://localhost:8010/desktop-avatar/dyna-orb.html` ✅。

---

## 7. Voice Pipeline

```
Microphone
   ↓  (dyna-orb-voice.js: getUserMedia + VAD)
ASR
   ↓  POST /api/asr          (server.py @772)
Chat
   ↓  POST /api/chat  (SSE)  (server.py @756, 流式工具事件)
ZhuangZhou Runtime
   ↓  (AgentRuntime 编排: Decision→Policy→Approval→Executor→Lifecycle)  ← 未触碰
Final Response
   ↓
TTS
   ↓  POST /api/speak        (server.py @758)
Audio
   ↓  (Web Audio / <audio> 播放)
Orb
   ↓  (window.ZZDynaOrb.setState / zz-space.js data-state)
```

- 错误路径：`error` 态 + 回退提示（无静默崩溃）✅。
- `/api/stream`（server.py @379）**仅用于 HUD / 事件观察**，**不是** Orb 状态机 ✅。
- 后端 contract **未变更**（红线）✅。

---

## 8. Voice State Contract

| 状态 | 含义 | 层级 |
|---|---|---|
| `idle` | 等待用户 | 产品级 |
| `listening` | 正在采集用户语音（VAD 激活） | 产品级 |
| `thinking` | ASR 已结束，ZhuangZhou 正在处理 | 产品级 |
| `speaking` | TTS 正在输出最终回答 | 产品级 |
| `error` | Voice pipeline 出错 | 产品级 |

- 允许的内部视觉态（不构成产品级 Voice state）：`recognizing` / `processing` / `executing` / `done`（dyna-orb.js 内部过渡态，自动回落 idle）。
- **禁止**出现第二套状态名（`processing`/`working`/`generating`/`responding`/`waiting` 不得作为产品级 Voice state）✅。
- 两 UI 共享同一契约（Web `zz-space` + Electron `dyna-orb`）✅。

---

## 9. Orb Classification Matrix

| Orb | 路径 | 分类 | 生产连接 |
|---|---|---|---|
| dyna-orb | `desktop-avatar/dyna-orb.html` + `.js` + `-voice.js` | **CANONICAL (Electron)** | `electron/main.js` 经 `createAvatarWindow` 加载 |
| petOrb | `zz-space/index.html` + `js/zz-space.js` | **CANONICAL (Web)** | 全部 launcher Web 步骤 |
| robot (旧 lottie) | `desktop-avatar/index.html` + `robot.js` | **LEGACY / FROZEN** | 已无 launcher 引用；`avatar-window.js` 旧默认回退已改为 dyna-orb |
| orb-pet | `desktop-avatar/orb-pet.*` | DORMANT / EXPERIMENTAL | 无任何生产引用（仅自身 + `scripts/verify-speech-orb.js`） |
| gem-orb | `desktop-avatar/gem-orb.*` | DORMANT / EXPERIMENTAL | 无任何生产引用 |
| liquid-orb | `desktop-avatar/liquid-orb.*` | DORMANT / EXPERIMENTAL | 无任何生产引用 |
| orb-ui-orb | `desktop-avatar/orb-ui-orb.*` | DORMANT / EXPERIMENTAL | 无任何生产引用 |

**竞争 orb 处理原则（unexpose 不删）**：先证引用 → 分类 → 解除 active 接线 → 再据真实依赖决定是否删除。当前 legacy / experimental orb **安全删除条件不足**（保留用于回滚与历史参考），统一标记 `NON-CANONICAL / NOT EXPOSED`。

**生产引用扫描结果**（grep `orb-pet|gem-orb|liquid-orb|orb-ui` 全树）：仅命中各 orb 自身 html/js 自引用、`P1_PRODUCT_ARCHITECTURE_FREEZE.md`、`scripts/verify-speech-orb.js`、`SPEECH_ORB_DELIVERY.md`、`VOICE_ORB_CONFIG.md` 文档/校验脚本 —— **零 launcher / 零 `electron/main.js` / 零 `preload.js` / 零 `zz-space` 生产引用** ✅。

---

## 10. Files Modified

| 文件 | 行 | 原因 | 改动 |
|---|---|---|---|
| `electron/main.js` | `43-46` | canonical Electron 入口加载 dyna-orb | `createAvatarWindow({ url: base + '/desktop-avatar/dyna-orb.html', preload })` |
| `electron/main.js` | `73-86` | dyna-orb 拖拽/穿透所需 IPC | 新增 `avatar:set-ignore-mouse`（→ `setIgnoreMouseEvents(ignore,{forward:true})`）、`avatar:move`（→ `setPosition(x+dx,y+dy)`） |
| `electron/preload.js` | `23-29` | 补全窗口控制桥（原仅 `gui/preload.js` 有） | 新增 `setIgnoreMouse`、`moveWindow` 经 `ipcRenderer.send` |
| `electron/avatar-window.js` | `87` | 默认回退 URL 指向 canonical orb | `opts.url \|\| 'http://localhost:8010/desktop-avatar/dyna-orb.html'` |
| `launcher/zz_launch.bat` | `87-88` | Web 入口统一为 zz-space | `start "" "http://localhost:8010/zz-space/index.html"` |
| `launcher/zz_launch.ps1` | `44` | Web 入口统一为 zz-space | `$WebURL = "http://localhost:$Port/zz-space/index.html"` |
| `start-zhuangzhou.bat` | `40` | electron 缺失 fallback 指向 zz-space | `/zz-space/index.html`（原 `/desktop-avatar/index.html`） |
| `start-zhuangzhou.bat` | `44` | 注释更新 | `:: 启动 Electron 桌面分身（加载 canonical Voice Orb: desktop-avatar/dyna-orb.html）` |
| `start-zhuangzhou.bat` | `56` | open_chat 指向 zz-space | `/zz-space/index.html`（原 `/`） |
| `VOICE_ORB_CONFIG.md` | 全文（11 节） | 过期文档重写 | 重写为 canonical 契约：产品 / Web Orb / Electron Orb / canonical 入口 / Voice States / Pipeline / Backend Contract / Runtime Authority / Orb 分类矩阵 / Forbidden / IPC 表 |

---

## 11. Files Not Modified（红线 & 保留项）

- **Core / 权限层（红线，零改动）**：`server.py`、`agent_runtime.py`、`cognitive/*`、`memory.py`、`policy_engine.py`、`decision/*`、`approval/*`、`lifecycle/*`、`capability/*`、`executor/*`、`security/*`。
- **后端契约（红线）**：`POST /api/asr` `@772`、`POST /api/chat` `@756`、`POST /api/speak` `@758`、`GET /api/stream` `@379` 均未改动。
- **孤儿入口（保留不接线）**：`gui/main.js`、`gui/preload.js`、`gui/package.json`（NOT CANONICAL，无 launcher 引用）。
- **Legacy（冻存）**：`desktop-avatar/index.html`、`desktop-avatar/robot.js`（旧 lottie robot，无 launcher 引用）。
- **Dormant 竞争 orb（unexpose 不删）**：`desktop-avatar/orb-pet.*`、`gem-orb.*`、`liquid-orb.*`、`orb-ui-orb.*`。
- **Canonical orb 表现层（仅核验未改）**：`zz-space/index.html`、`zz-space/js/zz-space.js`、`desktop-avatar/dyna-orb.js`、`desktop-avatar/dyna-orb-voice.js`。
- **基础设施（未动）**：DSH（DeepSeek Harness）= 基础设施/工作区底座；Xiao6Hub = Electron Presentation/Adapter shell。

---

## 12. Tests（真实 PASS/FAIL）

**Phase 2 本身未新增测试代码**（spec 授权最小 Presentation/Integration 收口；仅接线与文档，无新增被测逻辑）。

**全量回归（真实运行）**：
```
cd G:\ZhuangZhou\zhuangzhou-ui
python -m pytest tests/ -q -p no:cacheprovider --no-header
→ 9 failed, 518 passed in 183.15s
```
- 与 P5.4 基线（518 passed / 9 failed / 0 errors）**计数与性质完全一致** → **Phase 2 引入 0 回归**。
- 9 个失败所在模块：`tests/phase7*`、`tests/phase8*`、`tests/phasec_native_runtime.test.py`、`tests/test_external.py` —— 均为 **Core / 契约 / 外部依赖（ASR/ffmpeg 环境）** 模块，**无任何一项位于 orb / launcher / electron 表现层代码**。

**静态 / 运行时验证（Phase 2 专项）**：
- `node --check`（5/5 通过）：`electron/main.js`、`electron/preload.js`、`electron/avatar-window.js`、`desktop-avatar/dyna-orb.js`、`desktop-avatar/dyna-orb-voice.js` ✅。
- HTTP 200（5/5）：`/api/health`、`/zz-space/index.html`、`/desktop-avatar/dyna-orb.html`、`/desktop-avatar/dyna-orb.js`、`/desktop-avatar/dyna-orb-voice.js` ✅。
- 生产引用扫描：竞争 orb 零生产引用 ✅。

---

## 13. Regression Verification

- **Core 回归**：全量测试 `9 failed / 518 passed`，与 P5.4 基线逐字节一致 → 无新增失败。
- **入口一致性**：grep 确认 6 处 launcher/启动脚本 Web 引用全部指向 `zz-space/index.html`，无遗留 `/final/`、`/desktop-avatar/index.html`(robot)、根 legacy UI 引用。
- **Electron 接线**：grep 确认 `electron/main.js:44` → dyna-orb.html；`:73-86` IPC  handlers；`electron/preload.js:24-29` 桥；`electron/avatar-window.js:87` 默认回退。
- **后端契约**：`server.py` 端点行号与签名未变（仅读取核验，未编辑）。
- **结论**：Phase 2 改动面严格限定在 Presentation/Integration 层，对 Core/契约零影响，回归闸门通过。

---

## 14. §14 Acceptance Gate（25 / 25 通过）

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 单产品 ZhuangZhou（无第二产品） | ✅ VOICE_ORB_CONFIG §1 |
| 2 | Voice OS + GUI OS = 两 Presentation 层共享一 Core | ✅ §1 |
| 3 | DSH = 基础设施，非 ZhuangZhou 身份 | ✅ §8 |
| 4 | 恰好两个 canonical orb：Web=zz-space petOrb / Electron=dyna-orb | ✅ §2,§3 |
| 5 | Web Orb 入口 = zz-space/index.html | ✅ grep launchers |
| 6 | 全部 launcher 打开 zz-space/index.html（非 final/robot） | ✅ zz_launch.bat:87-88 / ps1:44 / start-zhuangzhou.bat:40,56 |
| 7 | zz-space 状态机经 coreStage[data-state] | ✅ zz-space.js |
| 8 | Web 状态 ⊆ canonical 5 | ✅ |
| 9 | Electron Orb 入口 = desktop-avatar/dyna-orb.html | ✅ main.js:44 |
| 10 | electron/main.js 加载 dyna-orb.html | ✅ main.js:44 |
| 11 | dyna-orb.html 加载 dyna-orb.js + dyna-orb-voice.js | ✅ |
| 12 | dyna-orb 拖拽/穿透桥完整（preload+IPC） | ✅ preload:24-29 / main:73-86 |
| 13 | window.ZZDynaOrb API 完整 | ✅ dyna-orb.js |
| 14 | electron/main.js = canonical 入口（package.json main） | ✅ package.json:6 |
| 15 | gui/main.js 分类 NOT CANONICAL 并保留 | ✅ 无 launcher 引用 |
| 16 | 生产路径无第二 Electron 入口 | ✅ |
| 17 | Mic→VAD（dyna-orb-voice.js） | ✅ |
| 18 | POST /api/asr 接线 | ✅ server.py @772 |
| 19 | POST /api/chat SSE 接线 | ✅ server.py @756 |
| 20 | Runtime 编排 Decision→Policy→Approval→Executor→Lifecycle 未触碰 | ✅ 红线 |
| 21 | POST /api/speak 接线 | ✅ server.py @758 |
| 22 | Audio→Orb setState | ✅ |
| 23 | /api/stream 非状态机（仅 HUD） | ✅ config §6 |
| 24 | Voice State Contract canonical 5 定义，无重名 | ✅ config §5 |
| 25 | Orb 分类矩阵 + 竞争 orb unexpose（不删） | ✅ config §9 + grep 零生产引用 |

---

## 15. Phase 3 Readiness

**最终判断：PHASE 2 COMPLETE.**

- 所有 §14 验收项 25/25 通过。
- 所有 §15 章节（本报告 1–15）齐备。
- 全量回归 `9 failed / 518 passed` 与 P5.4 基线一致 → 0 回归。
- 红线全部守住：无 Core 权限改动、无第二 Runtime/Memory/Capability/Policy/Lifecycle、DSH 仍为基础设施、Xiao6Hub 仍为 Presentation/Adapter。

### Remaining Risks（已知残留，非 Phase 2 阻塞）
1. **真实桌面运行时未验证**：GUI 渲染、麦克风采集、ASR/TTS/音频播放需在真实桌面 Electron 环境执行；headless 环境无法运行。已通过静态 + HTTP（5/5 200）+ 语法（5/5 node --check）+ 引用扫描（零生产引用）覆盖至可行极限。
2. **9 个既有 Core/契约/外部失败残留**：位于 `phase7`/`phase8`/`phasec_native_runtime`/`test_external`，与 Phase 2 无关，与 P5.4 基线一致；属既有技术债，超出 Phase 2 范围。
3. **`gui/main.js` 孤儿保留**：禁止在未来任何 launcher 中接线，除非重新评估并显式裁决。
4. **Dormant orb 保留磁盘**：`orb-pet/gem-orb/liquid-orb/orb-ui-orb` 仍存于磁盘（unexpose 不删），未来不得被任何生产路径引用。

### §16 — ABSOLUTE STOP
Phase 2 完成后 **STOP**。不自动进入：
**Phase 3 · GUI Reconstruction / Phase 4 / Phase 5 / P5.5 / P5.6 / P5.7 / P5.8**。

---
*报告结束。所有结论均基于 `G:\ZhuangZhou\zhuangzhou-ui` 真实源码与运行中后端核验（2026-08-17）。*
