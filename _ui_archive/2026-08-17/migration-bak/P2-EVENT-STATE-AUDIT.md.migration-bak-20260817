# P2-EVENT-STATE-AUDIT.md — Runtime Event / State 完整性审计

> 阶段：P2 · REAL CHAT RUNTIME VALIDATION & RELEASE GATE
> 目标：审计 Runtime Event 流，确认每个状态单一来源、无重复事件 / 重复执行 / 缺失事件 / 错误状态。
> 方法：全仓 `publish_domain` / `publish_system` 发布点核对 + `run` 状态机核对（file:line）。

---

## 0. 理想状态链（来自 P2 指令）

```
Task Started → Capability Selected → Permission → Approval
→ Execution → Tool Result → Task Completed / Failed
```

要求：**每个状态只有唯一来源**。

---

## 1. 事件发布点全映射（单一来源核对）

### 1.1 执行平台事件（SYSTEM_EVENT_NAMES，经 publish_system）

| 事件 | 发布源 | 单一来源？ |
|---|---|---|
| `execution_started` / `execution_completed` / `execution_cancelled` | `ai_core/execution/events.py:75-87` | ✅ 唯一（run 内） |
| `tool_started` / `tool_finished` | `ai_core/execution/events.py:89-104` | ⚠️ **见 §3.1（MCP 路径重复）** |
| `retry_started` / `retry_finished` | `ai_core/execution/events.py:104-110` | ✅ 唯一 |
| `tool_started` / `tool_finished`（MCP） | `mcp_host/executor.py:100,145` | ⚠️ **与 events.py 重复（MCP 路径）** |

### 1.2 领域事件（DOMAIN_EVENT_NAMES，经 publish_domain）

| 事件族 | 发布源 | 单一来源？ |
|---|---|---|
| `GOAL_*`（PLANNED/COMPLETED/FAILED…） | `agent_runtime.py:503,1077-1166`；`goals.py:87` | ✅ 统一由 agent_runtime / goals 发布 |
| `TASK_*` | `agent_runtime.py:1077-1166`（`_emit_task_domain`） | ✅ 唯一 |
| `AGENT_*` | `agent_runtime.py` | ✅ 唯一 |
| `INTENT_*` | `intent_gateway.py:32` | ✅ 唯一 |
| `COMPUTER_ACTION_*`（PLANNED/CALLED/DONE/VERIFIED/DENIED/FAILED） | `permission_guard.py:51-125` | ✅ 唯一 |
| `COMPUTER_ACTION_PHASE` | `computer_action/__init__.py:65-77` **与** `os_bridge.py:776-843` | ⚠️ **见 §3.2（双发布源）** |
| `SCREEN_CAPTURED/_FAILED` | `capture_runtime.py:43,62` | ✅ 唯一 |
| `PERCEPTION_*` | `perception_runtime.py` | ✅ 唯一 |
| `MEMORY_CREATED` | `reflector.py:81`；`MEMORY_UPDATED/STORED/LINKED/ARCHIVED` | ✅ 唯一 |
| `TOOL_CALLED` / `TOOL_DONE` | **无发布点**（仅 eventbus.py:183 声明） | ⚠️ **死声明（见 §3.3）** |

---

## 2. 状态终态唯一性（无 failure → completed）

`ai_core.execution.run`（api.py:60-153）状态机：

- 成功：`state.set(session, "completed")` (L131) → `execution_completed` (L132)。
- Policy block：`state.set(session, "failed")` (L87/110) → `execution_cancelled`/`reflection.record_failure`。
- 异常：`state.set(session, "failed")` (L147) → `execution_cancelled`。

**互斥性**：每个分支只设一个终态，无「失败后误置 completed」路径。✅

`agent_runtime` Goal FSM（:152-160）：轮次 FSM 收敛到 Goal 4 终态之一（completed/failed/max_steps_exceeded/blocked_by_policy）；`GOAL_COMPLETED` 仅成功终态路径发出（:210/334），`GOAL_FAILED` 仅失败路径发出。`round_status` 永不为 `completed`。✅ **无 completion duplicate / failure→completed**。

---

## 3. 发现（均非阻塞，记录为 O-class 观察项）

### 3.1 ⚠️ MCP 路径 `tool_started`/`tool_finished` 重复发布

- **现象**：`run`（events.py:122/129/139）在执行 `execute_tool` 前后发布 `tool_started`/`tool_finished`；而 MCP 工具经 `execute_tool → capability_os.execute_capability → mcp_host.executor` 时，`executor.py:100/145` **再次**发布同名事件。
- **结果**：一次 MCP 工具执行 → `tool_started`/`tool_finished` 各发出 **2 次**（一次来自 run 包裹、一次来自 MCP executor）。
- **性质**：**事件重复，非执行重复**。`execute_tool` 仅调用一次（执行唯一），故**不影响「Duplicate Execution = 0」门禁**。
- **影响**：前端进度条可能对 MCP 工具产生双倍更新。属可观测性瑕疵，建议后续去重（如 `events.py` 对 `external.mcp.*` 工具跳过自身 tool_* 事件，交由 MCP executor 发）。
- **分类**：**O-class（观察项）/ 非阻塞**。不计入 A/B/C/D，不阻断 release。

### 3.2 ⚠️ `COMPUTER_ACTION_PHASE` 双发布源

- `computer_action/__init__.py:65-77` 与 `os_bridge.py:776-843` 均发布 `COMPUTER_ACTION_PHASE`（observe/plan/execute/verify）。
- 若同一电脑动作同时经两个模块流转，可能产生重复 phase 事件。属 Complex Path（非默认 Chat）。
- **分类**：O-class（观察项）/ 非阻塞。

### 3.3 ℹ️ `TOOL_CALLED` / `TOOL_DONE` 为死声明

- `eventbus.py:183` 声明于 `DOMAIN_EVENT_NAMES`，但全仓 **无任何 `publish_domain("TOOL_CALLED"|"TOOL_DONE")` 调用**（grep 零命中）。
- 默认 Chat 工具进度经 `emit({"zhuangzhou_event":"tool_start"/"tool_end"})` 自由事件消费（不经 publish_domain 校验），与 `TOOL_CALLED/TOOL_DONE` 无关。
- **性质**：冗余声明，非重复源、非风险。建议后续清理或接入。
- **分类**：ℹ️ 信息项 / 非阻塞。

---

## 4. 重点检测项核对

| 检测项 | 结果 |
|---|---|
| duplicate event | ⚠️ MCP 路径 `tool_started/tool_finished` 重复（O-class，非阻塞，见 §3.1） |
| duplicate execution | ✅ 无（`execute_tool` 每请求单次） |
| missing event | ✅ 执行生命周期事件均由 events.py 完整发出 |
| stale state | ✅ 状态机终态唯一，无悬挂 |
| wrong status | ✅ 无 failure→completed |
| retry duplicate | ✅ 重试循环 `attempt < max_retry` 约束，成功即返回，不重复发事件 |
| approval duplicate | ✅ `request_approval` 每次确认仅调用一次（api.py:92 / agent_runtime.py:548） |
| completion duplicate | ✅ GOAL_COMPLETED 仅成功终态发出 |
| failure→completed | ✅ 已排除（§2） |

---

## 5. 结论

- ✅ **执行层完整性 PASS**：每个执行唯一落地、终态唯一、无重复执行、无 failure→completed、无重复审批。
- ✅ **默认 Chat 路径事件单一来源**（run / events.py 为唯一执行事件源）。
- ⚠️ **已知事件发射瑕疵（非阻塞）**：MCP 路径 `tool_started/tool_finished` 重复发射（O-class）；`COMPUTER_ACTION_PHASE` 双发布源（O-class，Complex Path）；`TOOL_CALLED/TOOL_DONE` 死声明（ℹ️）。
- 📌 上述不影响 P2 门禁「Duplicate Execution = 0」与「Legacy Bypass = 0」；事件完整性以「执行级唯一」为准，发射级去重列为后续改进。
- **分类**：本次审计无 A/B 类缺陷；O-class 观察项不阻断 release。
