# P1-UNIFIED-RUNTIME-DESIGN · ZhuangZhou 统一 Capability Execution 架构设计

- 版本：1.4.0
- 日期：2026-08-16
- 阶段：P1-B DESIGN ONLY（未修改源码，基于 P1-RUNTIME-BASELINE 实测）
- 纪律：VERIFY → DESIGN → IMPLEMENT → TEST → REGRESSION → GATE；无冲突则进入 IMPLEMENT。

---

## 0. 冲突核对（CONFLICT CHECK）

| 目标架构要求 | 真实代码现状 | 冲突？ |
|---|---|---|
| 单一执行入口 `ai_core.execution.run` | 默认 Chat 已走 `tools.py:3312`；Agent Runtime 走 `agent_runtime.py:553` | 无 |
| 不新建第二套 EventBus/Policy/Permission/Registry | 均唯一（`eventbus.py` / `policy_engine.py` / `permission_guard.py` / `capability_os.registry`） | 无 |
| 默认 Chat 不强制进 Goal | `FEATURE_GOAL_DECISION` 默认关，`run_fc_loop` 为 Simple Path | 无 |
| 能力选择须经 Capability Platform | 默认 Chat **绕过** `capability_os`，用 `TOOLS` schema | **有缺口（设计目标，非冲突）** |
| `default_deny=True` 保留 | `api.py:106/84`、`mcp_host/executor.py:40` 均保留 | 无 |
| 不删除安全机制 | 本设计不删任何 Policy/Permission 调用 | 无 |

**结论：无阻断性冲突。** 缺口（能力选择旁路）正是 P1 要收敛的目标，且现有 `capability_os.invoke_capability`（`capability_os/__init__.py:214`）已是可复用的能力级门面——**无需重写 Capability OS**，只需让默认 Chat 经它（或等价适配器）选择/执行。进入 IMPLEMENT。

---

## 1. 新旧 Runtime 对比

| 维度 | 旧（P1 前）默认 Chat | 新（P1 后）默认 Chat |
|---|---|---|
| 能力选择 | LLM `TOOLS` schema + `select_tools` 正则启发式（`tools.py:1105`） | **优先** `capability_os.discovery.dispatch_tool_list()` 作为可见能力真相；保留 `select_tools` 裁剪作为延迟优化 |
| 执行入口 | `execute_tool_calls → _execution_run`（统一） | 不变（`_execution_run` 仍唯一执行入口） |
| 能力层 | 不经过 `capability_os` | 经 `capability_runtime` 适配器：**已注册能力走 `invoke_capability`，未注册回退 `execute_tool`** |
| 结果契约 | 裸字符串（execute_tool 返 str） | 适配器包装为 `CapabilityResult`（向后兼容旧返回值） |
| 权限 | NONE（block-only） | 不变（HIGH/CRITICAL 仍 block；MEDIUM 本地 chat 仍 auto，设计内） |
| 事件 | `tool_start/tool_end` SSE + execution_* | 复用既有，能力级可选 `TOOL_CALLED/TOOL_DONE` |

Agent Runtime（复杂路径）**完全不变**——它已是用 `capability_os` 的完整统一路径。

---

## 2. Default Chat 最终路径（设计后）

```
POST /api/chat → _handle_chat                         server_handlers_chat.py:146
  → build_context_prompt(user_text)                    Context Engine（不变）
  → [Intent Gateway 门：FEATURE_GOAL_DECISION 开 且 runtime 跑 → 复杂路径，见 §4]
  → save_turn("user")                                   Memory（不变）
  → run_fc_loop(messages, emit,
        tools = select_capabilities(user_text, allowed),   ← 改：可见能力清单来自 capability_os
        ...)                                              tools.py:3356（签名不变，仅 tools= 来源变）
       └ LLM tool_calls
       └ execute_tool_calls(tool_calls, allowed)          tools.py:3291（run_one 改走适配器）
            └ run_one(p): capability_runtime.execute(p["name"], p["args"], allowed=allowed)
                            ↓ 已注册 → capability_os.invoke_capability(cap_id, args)  → execute_tool → _execution_run
                            ↓ 未注册 → execute_tool(name, args)                       → _execution_run
                            ↓ 统一包装 CapabilityResult
  → 漏调兜底：capability_runtime.execute(name, args, allowed)   server_handlers_chat.py:315（改走适配器）
  → 事件 / 文本 / Memory（不变）
```

> 执行内核、`policy` 门、`eventbus`、`memory` 全部复用，无第二套。

---

## 3. Simple Path（低复杂度，保持低延迟）

```
Chat → Context → Capability Selection → Policy → Permission → Execute → Result → Response
```
- **Capability Selection**：`select_capabilities(text, allowed)` 返回 LLM 可见工具 schema。实现 = `capability_os.discovery.dispatch_tool_list()` 交集 `select_tools(text, allowed)` 的启发式裁剪（后者仅作 token 优化，不增新选择逻辑）。
- **Policy**：`ai_core.execution.run` NONE 门（`api.py:101-115`）拦截 `block`（NEVER/危险命令）。`default_deny=True` 保留。
- **Permission**：本地交互式 chat 的 MEDIUM 保持 `auto`（设计内，`api.py:81-82`）；HIGH/CRITICAL 由 Policy `block`。远程会话受白名单约束（`server_handlers_chat.py:296`）。
- **Execute**：`_execution_run` 单一入口。
- **Response**：结构化 `CapabilityResult.data` 回填 LLM / 直接返回。

Simple Path **绝不**进入 Goal/Planner。

---

## 4. Complex Path（复杂任务，复用 Agent Runtime）

```
Chat → Intent → Goal → Planner → Capability Router → Execution → Observation → Replan → Completion
```
- 入口：`intent_gateway.run_intent_gateway`（`server_handlers_chat.py:226`，受 `FEATURE_GOAL_DECISION` 门控）。
- 复用：`agent_runtime._run_goal` / `_execute_task`（含 `capability_os.execute_capability` / `permission_guard` / `policy.request_approval` / `ai_core.execution.run(GOAL)` / checkpoint）。
- **不重写** Planner；不新建 GoalSystem。

---

## 5. Capability Selection 方式

新增最小适配器模块 **`capability_runtime.py`**（单一职责：默认 Chat 的能力选择 + 执行桥接）：

```python
# capability_runtime.py（设计草稿，IMPLEMENT 阶段落地）
from capability_os import discovery, invoke_capability, tool_to_capability
from capability_os.execution_mapping import get_executor
from tools import execute_tool, select_tools, TOOLS

def select_capabilities(text, allowed=None):
    """默认 Chat 下发给 LLM 的可见能力清单。
    以 capability_os.discovery.dispatch_tool_list() 为真相，select_tools 仅做延迟裁剪。"""
    visible = set(discovery.dispatch_tool_list())          # 本地 TOOL_FUNCS + external.mcp.* + skill:*
    if allowed is not None:                                # 远程白名单
        visible &= set(allowed)
    # 延迟优化：无明确意图的闲聊不下发工具（沿用 select_tools 启发式，不改写）
    trimmed = select_tools(text, allowed=list(visible))
    # 若 select_tools 返回空（纯闲聊）则不下发，避免误调
    return trimmed

def execute(name, args, *, allowed=None, permission_mode="none", goal_id=None) -> "CapabilityResult":
    """默认 Chat 的统一能力执行桥。
    - 会话白名单（远程 allowed）在边界先校验，与 execute_tool 同语义、安全不退化。
    - 能力 id 经 capability_os.tool_to_capability 解析（Capability OS 为能力真相源）。
    - 执行**必须**经 ai_core.execution.run 的 policy 门（default_deny）—— 与 P1 前 run_fc_loop
      严格一致；严禁直连 execute_tool / invoke_capability 绕过 Policy。
      （注：invoke_capability 委派 execute_tool 会跳过该 policy 门，故默认 Chat 执行统一走
      _execution_run，不在此分叉；capability_os 仍用于选择真相与 capability_id 解析。）
    - 结果统一包装为 CapabilityResult。"""
    if allowed is not None and name not in allowed:
        return CapabilityResult(success=False, capability_id=tool_to_capability(name),
                                error="工具 %s 在远程会话中不可用（受白名单限制）" % name,
                                error_type="policy_denied")
    cap_id = tool_to_capability(name)
    raw = _execution_run(name, args or {}, allowed=allowed)   # 唯一 policy 门
    return CapabilityResult.from_raw(name, cap_id, raw)
```

- `select_capabilities` 与现有 `select_tools` 签名兼容（`server_handlers_chat.py:298` 仅替换入参来源），**不破坏 `run_fc_loop` 签名**。
- `dispatch_tool_list()` 已含 `TOOL_FUNCS` 全量（`discovery.py:104-106`），**不会丢能力**。

---

## 6. Tool Adapter 方式

- 适配器 = `capability_runtime.execute`（§5）。它**不复制**任何执行逻辑：
  - 执行统一经 `ai_core.execution.run`（`_execution_run`）—— 含 `policy_engine.evaluate(default_deny=True)` 门，与 P1 前 `run_fc_loop` 严格一致；HIGH/CRITICAL 由 Policy `block`，MEDIUM 本地 chat `auto`（设计内）。
  - `capability_os` 用于：① 选择真相（`discovery.dispatch_tool_list`）；② 能力 id 解析（`tool_to_capability`）；③ `CapabilityResult.capability_id` 标注。
  - `external.mcp.*` / `skill:*` / 动态工具：仍经 `_execution_run → execute_tool` 既有分支（`tools.py:4003-4018`），行为不变。
- 现有 `execute_tool_calls.run_one`（`tools.py:3308-3312`）改为调用 `capability_runtime.execute`，默认 Chat 主路与漏调兜底统一经同一适配器。
- **不新建第二执行器**；不修改 `TOOL_FUNCS` 实现；执行入口内核零改动。

---

## 7. Policy / Permission 接入点

- **Policy**：完全复用 `ai_core.execution.run` 内置 `policy_engine.evaluate(default_deny=True)`（`api.py:84/106`）。默认 Chat 走 NONE（block-only）。
- **Permission/Approval**：
  - 默认 Chat Simple Path：本地 MEDIUM 保持 `auto`（设计内，不强制弹审批，避免高延迟）；HIGH/CRITICAL 由 Policy `block`。远程会话白名单已限。
  - 若未来需让默认 Chat 对 MEDIUM 也走审批：**复用** `permission_guard` / `policy_engine.request_approval`（与 Agent Runtime 同源），**不新建**模态通道。本期不启用（保持低延迟，符合 Simple Path 要求）。
- **红线**：任何能力不得 `Capability → Tool` 直连绕过 Policy；所有执行入口都过 `ai_core.execution.run` 的 policy 门（NONE 或 GOAL）。

---

## 8. EventBus 接入点

- **复用既有**，不新增 `CAPABILITY_*`（避免与 `TOOL_*` / `execution_*` 重复命名）：
  - SSE：`tool_start` / `tool_end`（`server_handlers_chat.py:3392-3396`）—— 默认 Chat 已发。
  - 系统事件：`execution_started/updated/completed/cancelled/tool_started/tool_finished`（`eventbus.py:274-281`）—— `ai_core.execution` 已发。
  - 领域事件：`TOOL_CALLED` / `TOOL_DONE`（`eventbus.py:183`）—— 能力级语义可直接复用这两个已登记名。
- 仅在确有「能力级」语义需求时，才向 `DOMAIN_EVENT_NAMES` 增补 `CAPABILITY_*`（须同步前端 `zz-events.js`，禁裸发）。本期**优先复用** `TOOL_*`，不新增。

---

## 9. Memory 接入点

- 执行结果进入 Memory 的链路**不变**：`memory.py` 的 `save_turn` / `compress_memory` / `extract_*`（`server_handlers_chat.py:242/433/435-447`）。
- `CapabilityResult.data` 作为工具结果回填 LLM（`run_fc_loop` 的 `tool_msgs`），下一轮 Context 经 `build_context_prompt` 自然利用——与现状一致。
- **防重复写入**：`CapabilityResult` 仅是返回值包装，不触发额外 Memory 写；Memory 写点仍集中在 `_handle_chat` 既有调用处。

---

## 10. Failure / Retry / Replan

- **Simple Path 失败**：一次失败 → `CapabilityResult(success=False, error_type=...)` → 最终响应（不重试、不 replan，保持低延迟）。错误分类复用现有 `ERROR_TAXONOMY` / `policy_engine` 语义（policy_denied / approval_required / execution_failed / timeout / network / not_found / validation / unknown）。
- **Complex Path 失败**：继续复用 `agent_runtime` 的 `_MAX_RETRIES` + `_classify_error` + `_try_alternative_tool` + `recovery.checkpoint`（`agent_runtime.py:542-571`）。**不重写 FSM**。
- `test_consecutive_failure_clear_queue`（C-class 未实现）本期**不实现**；记入 backlog。

---

## 11. Backward Compatibility

- `CapabilityResult` 为**只读包装**：`execute_tool` 的旧调用方（Agent Runtime、MCP、Skill）继续拿 str/原始返回值，不受影响。
- `select_capabilities` 返回值结构与 `select_tools` 一致（list of tool schema dict），`run_fc_loop` 签名不变。
- 未注册工具回退 `execute_tool` → 行为 100% 兼容。
- `capabilities.py`（遗留视图）不删除、不扩展；`capability_os.registry` 为唯一能力真相。
- `invoke_capability` 对 `mcp`/`none`/`umbrella`/`context` 的处理保持不变（`capability_os/__init__.py:234-254`）。

---

## 12. Rollback Strategy

- 单点开关：新增 `config.FEATURE_CAPABILITY_RUNTIME`（默认 `True`）。若置 `False`，`capability_runtime.execute` / `select_capabilities` 直接委托回 `execute_tool` / `select_tools`（1 行分支），行为回退到 P1 前。
- 模块化：`capability_runtime.py` 为独立新文件，删除即回退（adapter 逻辑全部在其中，不侵入 `tools.py` 执行体）。
- 执行入口内核零改动：`ai_core.execution.run` / `execute_tool` 不变，rollback 不影响 Agent Runtime。
- 测试门：P1 全量回归 + 新增 E2E（TEST 1-10）必须 A=0 B=0；任一失败即 rollback（关 `FEATURE_CAPABILITY_RUNTIME`）。

---

## 13. 新增最小模块清单（IMPLEMENT 阶段）

| 模块 | 职责 | 是否新 |
|---|---|---|
| `capability_runtime.py` | 默认 Chat 能力选择 + 执行桥（→ invoke_capability / execute_tool 回退）+ CapabilityResult 包装 | **新**（唯一新增） |
| `capability_os.invoke_capability` | 已存在，复用（不改动） | 复用 |
| `capability_os.discovery.dispatch_tool_list` | 已存在，复用（不改动） | 复用 |
| `ai_core.execution.run` / `tools.execute_tool` | 已存在，复用（不改动） | 复用 |
| `tools.run_fc_loop` / `execute_tool_calls.run_one` | 仅改入参来源/调用适配器，签名不变 | 小改 |
| `server_handlers_chat.py:298/315` | 改用 `select_capabilities` / `capability_runtime.execute` | 小改 |
| `config.FEATURE_CAPABILITY_RUNTIME` | 开关 | 小改 |

**不碰**：Agent Runtime / Capability OS / Policy / Permission Guard / Tool Runtime / Memory / 任何 UI。
