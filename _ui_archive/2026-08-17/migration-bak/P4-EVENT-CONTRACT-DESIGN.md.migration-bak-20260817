# P4.1 · P4-EVENT-CONTRACT-DESIGN

> 阶段：P4.1 DESIGN ONLY · 项目：ZhuangZhou Core v1.4.0
> 关联：`P4-MEMORY-CANONICAL-CONTRACT.md` / `P4-MEMORY-LIFECYCLE-DESIGN.md`

---

## 1. 事件总线权威

**唯一事件权威 = `eventbus.py`**（`DOMAIN_EVENT_NAMES` / `SYSTEM_EVENT_NAMES`，`publish_domain` / `publish_system` 强制名称校验）。任何 Memory/Context/Knowledge 状态变更必须经此发布，**禁止散落自定义回调 / 直接函数调用绕过**。

```mermaid
graph TD
    A[Memory 写入/归档/衰减] -->|publish_domain| EB[(EventBus)]
    B[Context 装配完成] -->|publish_domain| EB
    C[Knowledge 更新] -->|publish_domain| EB
    D[工具/执行/调度] -->|publish_system| EB
    EB --> S1[订阅者: 审计]
    EB --> S2[订阅者: 主动智能]
    EB --> S3[订阅者: 前端 HUD]
```

---

## 2. 现有事件（真实源码已含）

`DOMAIN_EVENT_NAMES` 已有：`MEMORY_CREATED` / `MEMORY_UPDATED` / `MEMORY_STORED` / `MEMORY_LINKED` / `MEMORY_ARCHIVED`。
`SYSTEM_EVENT_NAMES` 已有：执行/工具/调度类事件。

```mermaid
graph LR
    subgraph EXIST[已有 DOMAIN 事件]
      E1[MEMORY_CREATED]
      E2[MEMORY_UPDATED]
      E3[MEMORY_STORED]
      E4[MEMORY_LINKED]
      E5[MEMORY_ARCHIVED]
    end
```

---

## 3. 需扩展的事件（单一名字，无同义词）

| 新事件名 | 触发点 | payload 建议 |
|---|---|---|
| `MEMORY_RETRIEVED` | DefaultEvolutionPolicy 召回命中 | memory_id, query_hash, score |
| `MEMORY_CONSOLIDATED` | MERGE/合并完成 | kept_id, merged_ids[] |
| `MEMORY_DECAYED` | lifecycle 状态变更 | memory_id, from, to |
| `CONTEXT_BUILT` | context.facade 完成装配 | source_count, token_used, budget_tier |
| `KNOWLEDGE_UPDATED` | knowledge_runtime 写文件 | doc_path, action |

```mermaid
flowchart TB
    subgraph ADD[新增 DOMAIN 事件]
      N1[MEMORY_RETRIEVED]
      N2[MEMORY_CONSOLIDATED]
      N3[MEMORY_DECAYED]
      N4[CONTEXT_BUILT]
      N5[KNOWLEDGE_UPDATED]
    end
    N1 --> EB[(EventBus 单一名字表)]
    N2 --> EB
    N3 --> EB
    N4 --> EB
    N5 --> EB
```

---

## 4. 发布点映射（Source → Event）

```mermaid
sequenceDiagram
    participant W as 写入者(Canonical API)
    participant L as lifecycle
    participant R as DefaultEvolutionPolicy
    participant CE as context.facade
    participant KR as knowledge_runtime
    participant EB as EventBus
    W->>EB: MEMORY_CREATED/UPDATED
    L->>EB: MEMORY_ARCHIVED / MEMORY_DECAYED
    W->>EB: MEMORY_LINKED
    R->>EB: MEMORY_RETRIEVED
    R->>EB: MEMORY_CONSOLIDATED
    CE->>EB: CONTEXT_BUILT
    KR->>EB: KNOWLEDGE_UPDATED
```

| 发布点 | 事件 | 唯一发布者 |
|---|---|---|
| Canonical Memory API 写 | `MEMORY_CREATED`/`MEMORY_UPDATED` | Canonical API |
| Canonical Memory API link | `MEMORY_LINKED` | Canonical API |
| lifecycle 转归档/衰减 | `MEMORY_ARCHIVED`/`MEMORY_DECAYED` | lifecycle |
| Retrieval 命中 | `MEMORY_RETRIEVED` | DefaultEvolutionPolicy |
| Retrieval 合并 | `MEMORY_CONSOLIDATED` | DefaultEvolutionPolicy |
| Context 装配完成 | `CONTEXT_BUILT` | context.facade |
| Knowledge 写 | `KNOWLEDGE_UPDATED` | knowledge_runtime |

---

## 5. 契约约束

```mermaid
graph TD
    A[事件契约] --> B[名字只来自 DOMAIN/SYSTEM_EVENT_NAMES]
    A --> C[publish_domain/publish_system 强制校验]
    A --> D[禁止同义词/散落字符串]
    A --> E[禁止绕过 EventBus 直连订阅者]
```

| 约束 | 说明 |
|---|---|
| 单一名字表 | 所有事件名集中在 `eventbus.py`，新增必须在此登记 |
| 强制校验 | `publish_*` 拒绝未登记名字（fail closed） |
| 无同义词 | `MEMORY_WRITTEN` 等不得与 `MEMORY_CREATED` 并存 |
| 无绕过 | 订阅者经 EventBus，记忆模块不直连 Agent/Tool |

---

## 6. 与 Policy/Permission/Approval/Executor 的关系

```mermaid
graph LR
    EV[EventBus 事件] --> POL[Policy Engine]
    POL --> PERM[Permission]
    PERM --> APP[Approval]
    APP --> EX[Executor]
    EV -.不直接.-> EX
```

- 事件本身不触发执行；若需执行动作，须经 Policy→Permission→Approval→Executor 全链（红线）。
- Memory/Context/Knowledge 发布事件，但**不直连 Executor**（与 Canonical Contract 红线一致）。

---

## 7. 测试策略（DESIGN，P4.5 执行）

| 测试 | 验证 |
|---|---|
| 名字唯一 | 无同义词事件；全部源自 EventBus 表 |
| 强制校验 | 未登记名字 publish 失败 |
| 发布点完整 | 每个映射表发布点都有发布调用 |
| 无绕过 | grep 确认记忆/上下文模块无直连 Executor |

---

## 8. P4.5 执行入口（摘要）

- **修改**：`eventbus.py`（扩展 `DOMAIN_EVENT_NAMES`）、各发布点补 `publish_domain` 调用。
- **复用**：现有 `publish_domain`/`publish_system` 强制校验机制。
- **回归门**：事件名唯一 + 强制校验 + 无绕过。
- 详见 `P4-IMPLEMENTATION-ROADMAP.md` P4.5。
