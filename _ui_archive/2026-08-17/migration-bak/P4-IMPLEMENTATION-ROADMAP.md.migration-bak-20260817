# P4.1 · P4-IMPLEMENTATION-ROADMAP

> 阶段：P4.1 DESIGN ONLY（本文件为 P4.2–P4.6 的可执行计划，本身不改代码）
> 项目：ZhuangZhou Core v1.4.0 · 关联：全部 P4.1 DESIGN 文档
> 红线贯穿：不丢历史数据 / 不隐写测试 / 不绕过 Policy→Permission→Approval→Executor / 不碰 UI / DESIGN 阶段零代码变更。

---

## 0. 总体时序（串行主链 + 并行支线）

```mermaid
gantt
    title P4.2 - P4.6 执行时序
    dateFormat  X
    axisFormat  P%L
    section 主链
    P4.2 Memory 契约      :p42, 0, 1
    P4.3 统一检索         :p43, 1, 1
    P4.4 重构 Context      :p44, 2, 1.5
    P4.5 知识+事件+孤儿    :p45, 3.5, 1.5
    P4.6 配置+弃用收口     :p46, 5, 1
    section 并行支线
    P4.2 迁移脚本          :p42m, 0, 1
    P4.5 事件发布点        :p45e, 3.5, 1.5
```

- **主链串行**：P4.2 → P4.3 → P4.4 → P4.5 → P4.6（前一阶段回归门通过方可进入下一步）。
- **并行支线**：每个阶段内部的"迁移脚本 / 测试 / 事件发布"可与主改动并行开发，但合入需同阶段回归门通过。
- **双跑兼容期**：每阶段先加新权威并与旧路径 flag 双跑，再停用旧路径（见 Deprecation Matrix §4）。

---

## P4.2 · CANONICAL MEMORY CONTRACT

| 项 | 内容 |
|---|---|
| **目标** | 固化 `memories` 为唯一 Canonical Store；建立 Canonical Memory API；修正 distiller 写入方式；纠正 lifecycle stale 注释。 |
| **修改文件** | `memory.py`（新增 Canonical Memory API：create/read/update/upsert/archive/restore/delete/verify/link/merge/consolidate）、`memory_distiller.py`（改走真实列）、`memory_evolution/lifecycle.py`（修正注释） |
| **新增文件** | `memory_api.py`（Canonical API 模块，若不放 memory.py） |
| **迁移步骤** | M1 校验 content_hash UNIQUE 已存在；M2 distiller 改真实列；M3 lifecycle 注释修正；M5 mem_vectors scope 清理 |
| **测试** | Memory CRUD / UPSERT 幂等(content_hash) / MERGE 保高置信 / LINK 图边 / VERIFY / 无物理 DELETE(grep) / 既有数据保全 |
| **回归门** | 既有记忆读写用例全绿；distiller 产出 memories 的 confidence/source 在真实列；无物理 DELETE |
| **风险** | distiller 改写入后旧 tags JSON 兼容性 → 双写过渡期后停读 tags |
| **回滚** | flag/双写回退；不删新 API |
| **完成标准** | Canonical API 被所有 memories 写入者使用；content_hash 幂等生效；stale 注释消除 |

---

## P4.3 · UNIFIED RETRIEVAL

| 项 | 内容 |
|---|---|
| **目标** | `DefaultEvolutionPolicy` 提升为唯一 Retrieval 权威；`memory_intelligence.recall`/`cognitive.episodic.recall_episodes` 降为其内部实现；排名权重与 ContextRanker 同源。 |
| **修改文件** | `memory_evolution/retrieval_policy.py`（提升为权威，集成子能力）、`memory_intelligence.py`（recall 降为内部）、`cognitive/episodic.py`（recall_episodes 降为内部） |
| **新增文件** | `retrieval/__init__.py`（facade，包装 DefaultEvolutionPolicy） |
| **迁移步骤** | 将散落召回调用点统一改走 `retrieval` facade；保留 `memory_query.query_memory` 为对话内显式搜索 API（不进自动召回链） |
| **测试** | 检索幂等 / 排名一致性(与 ContextRanker) / 去重(content_hash) / 冲突降权×0.4 / 边界(排除 archived/decayed/pending) / Legacy Bypass(无第二检索入口) |
| **回归门** | 排名权重无漂移；召回质量不低于旧实现；Legacy Bypass 通过 |
| **风险** | 召回质量波动 → 与旧实现做 top-k 重叠度对比回归 |
| **回滚** | 调用点回退旧 recall 直调（双跑期保留） |
| **完成标准** | 全仓除 DefaultEvolutionPolicy 外无第二检索入口；HybridPolicy/RerankerPolicy 落实或明确弃用 |

---

## P4.4 · CONTEXT ENGINE REFACTOR

| 项 | 内容 |
|---|---|
| **目标** | 消除 `MemorySource` 单体 blob；`EvolvedMemorySource` 接管并补 `DEDUP_SCOPE_KEY`；UserModel 停止拼提示词；`FEATURE_MEMORY_EVOLUTION` 正式定义并注册。 |
| **修改文件** | `context/sources.py`（弃用 MemorySource 单体）、`memory_evolution/context_source.py`（补 DEDUP_SCOPE_KEY + 注册）、`cognitive/user_model.py`（移除 render_user_model_block，返回 ContextItem）、`context/builder.py`（注册 EvolvedMemorySource）、`config.py`（定义 FEATURE_MEMORY_EVOLUTION 默认 OFF） |
| **新增文件** | 无（复用现有） |
| **迁移步骤** | EvolvedMemorySource 与 MemorySource 双跑（flag 切换）；确认无三重注入后停用 MemorySource；MemoryRecallSource 并入 EvolvedMemorySource |
| **测试** | 颗粒度(多条 ContextItem 非 blob) / 去重(无三重注入) / 预算隔离(单条可裁剪) / 边界(UserModel 不产提示词) / Legacy Bypass(除 context.facade 外无第二装配入口) / 提示词逐字节一致(COND 保障) |
| **回归门** | 无三重记忆注入；提示词与旧实现逐字节一致；Legacy Bypass 通过 |
| **风险** | 提示词结构变化 → COND 逐字节比对 + 人工抽检 |
| **回滚** | `FEATURE_MEMORY_EVOLUTION=false` 回退 MemorySource |
| **完成标准** | MemorySource 停用；EvolvedMemorySource 注册且带 DEDUP_SCOPE_KEY；UserModel 返回 ContextItem |

---

## P4.5 · KNOWLEDGE BOUNDARY + EVENT CONTRACT + ORPHAN DROP

| 项 | 内容 |
|---|---|
| **目标** | 固化 knowledge_runtime 边界；扩展 EventBus 事件；count 校验后 DROP 孤儿表。 |
| **修改文件** | `knowledge_runtime.py`（边界契约）、`eventbus.py`（扩展 DOMAIN_EVENT_NAMES）、`db.py`（追加孤儿表 count→DROP 迁移块）、各发布点补 publish_domain |
| **新增文件** | 无 |
| **迁移步骤** | M4 孤儿表 `SELECT COUNT(*)` 校验（>0 则中止 fail closed）→ DROP knowledge_docs/knowledge_chunks；发布点补 MEMORY_RETRIEVED/CONSOLIDATED/DECAYED、CONTEXT_BUILT、KNOWLEDGE_UPDATED |
| **测试** | 边界(无路径写 knowledge 入 memories/mem_vectors) / 文件召回 / 孤儿 count 校验 / 事件名唯一+强制校验 / 无绕过 Executor |
| **回归门** | 孤儿表零数据确认后 DROP；事件名唯一；边界测试通过 |
| **风险** | 误删带数据表 → count 校验 fail closed 防护 |
| **回滚** | 孤儿表 DROP 不可回滚（因零数据无损失，可接受；若 count>0 已中止） |
| **完成标准** | 知识不入 Memory；5 个新事件发布；孤儿表移除（零数据确认） |

---

## P4.6 · CONFIG SINGLE-SOURCE + DEPRECATION CLOSEOUT

| 项 | 内容 |
|---|---|
| **目标** | 配置默认值收敛为单一权威；memory_v2 flag 迁入 config.py；ghost flag 登记；全弃用收口；全量回归。 |
| **修改文件** | `config.py`（DEFAULT_FEATURE_FLAGS 单一字典 + 登记 FEATURE_MEMORY_EVOLUTION + 去噪模块级默认）、`memory_v2/flags.py`（deprecate，迁入 config） |
| **新增文件** | 无 |
| **迁移步骤** | C1 抽取单一字典；C2 模块级注解去矛盾；C3 登记 ghost flag；C4 flag 迁入；C5 全局 grep 消除 ghost 引用；运行全仓 Legacy Bypass Test |
| **测试** | 单一权威(grep 无第二定义源) / ghost 消除 / 无矛盾默认 / 回退一致 / 安全边界 / 全量回归 |
| **回归门** | 全量 pytest + Legacy Bypass + 配置维度单一权威 |
| **风险** | flag 合并引发行为变化 → 对比 env 默认 true 行为一致 |
| **回滚** | 还原 config.py / memory_v2/flags.py |
| **完成标准** | 单一配置权威；memory_v2 默认 OFF 且无写入；Legacy Bypass 全绿；全弃用收口 |

---

## 阶段间依赖与门禁

```mermaid
flowchart TB
    G42[P4.2 回归门] --> G43[P4.3 回归门]
    G43 --> G44[P4.4 回归门]
    G44 --> G45[P4.5 回归门]
    G45 --> G46[P4.6 全量回归门]
    G46 --> DONE[P4 完成]
    G42 -.失败.-> RB[回退本阶段 flag]
    G43 -.失败.-> RB
    G44 -.失败.-> RB
    G45 -.失败.-> RB
    G46 -.失败.-> RB
```

| 门禁 | 必过项 |
|---|---|
| P4.2 | 无物理 DELETE + content_hash 幂等 + distiller 真实列 |
| P4.3 | 排名无漂移 + Legacy Bypass(检索) |
| P4.4 | 无三重注入 + 提示词逐字节一致 + Legacy Bypass(装配) |
| P4.5 | 孤儿零数据 + 事件唯一 + 边界 |
| P4.6 | 配置单一权威 + 全量回归 + Legacy Bypass 全绿 |

---

## 测试总矩阵（贯穿 P4.2–P4.6）

| 测试类 | 覆盖阶段 | 验证核心 |
|---|---|---|
| Memory CRUD / 幂等 / 合并 / 链接 / 核验 | P4.2 | Canonical Contract |
| 迁移幂等 / 数据保全 / 孤儿 count | P4.2/P4.5 | Schema Migration |
| 检索幂等 / 排名一致 / 去重 / 冲突降权 / 边界 | P4.3 | Unified Retrieval |
| 上下文预算 / 源隔离 / 颗粒度 / 去重 | P4.4 | Context Engine |
| 知识边界 / 事件契约 / 配置默认 | P4.5/P4.6 | Boundary / Event / Config |
| **Legacy Bypass Test** | P4.3/P4.4/P4.6 | 全仓无第二 Memory/Retrieval/Context/Event 入口 |
| 安全边界 | P4.6 | Memory 不直连 Agent/Tool/Executor |
| 全量回归 | P4.6 | 既有 pytest 全绿 |

---

## Legacy Bypass Test（核心防回归）

```mermaid
flowchart TB
    A[Legacy Bypass Test] --> B[grep: 除 Canonical API 外无 memories 写入]
    A --> C[grep: 除 DefaultEvolutionPolicy 外无第二检索入口]
    A --> D[grep: 除 context.facade 外无第二提示词装配]
    A --> E[grep: 除 eventbus 外无第二事件发布]
    A --> F[grep: Memory 模块无直连 Executor]
    B --> G[全部通过=门禁过]
    C --> G
    D --> G
    E --> G
    F --> G
```

> 本测试是 P4 各阶段回归门的**硬性前置**，任何一阶段若引入绕过即判失败。

---

## 下一阶段 P4.2 执行入口

- **起点文件**：`memory.py`（新增 Canonical Memory API）+ `memory_distiller.py`（修正写入）。
- **第一动作**：实现 Canonical Memory API 的 `upsert`（content_hash 幂等）+ `archive`/`restore`/`delete`(逻辑)/`verify`/`link`/`merge`/`consolidate`。
- **第二动作**：改 `memory_distiller._persist()` 经 Canonical API 写真实 `confidence`/`source` 列。
- **第三动作**：修正 `memory_evolution/lifecycle.py` stale 注释。
- **门禁**：P4.2 回归门（见上）。通过后进入 P4.3。
- 详细字段/操作契约见 `P4-MEMORY-CANONICAL-CONTRACT.md`。
