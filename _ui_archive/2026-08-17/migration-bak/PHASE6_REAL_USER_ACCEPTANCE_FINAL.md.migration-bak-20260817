# PHASE 6 — SIX REAL USER ACCEPTANCE FINAL REPORT

> 阶段目标：在真实 Windows GUI + 音频环境中，把 Six 当成真实用户产品使用，验证 Electron→Voice Orb→Workspace→Chat→Streaming→Tool→Approval→Result→TTS 完整用户链路。
> 约束：STRICT VERIFY-BEFORE-CHANGE；零未授权功能变更；红线全冻结（§2）。
> 方法：重新读取真实源码建基线（见 `PHASE6_REAL_USER_ACCEPTANCE_BASELINE.md`）+ 真实运行后端 API/SSE/Tool 探测 + 静态/资产层审计 + 环境能力探针。

---

## 1. Executive Summary

Phase 6 在**真实运行后端**维度完成闭环：后端 LIVE、SSE 流式+`[DONE]` 实跑 PASS、Tool `tool_start→tool_end` 链路**本会话实跑确认**。所有静态/契约/安全/品牌/端口不变量经重读真实源码确认**全部 intact、零变更**；`node --check` 144 JS / 0 fail。

但本 agent 运行于 **headless 沙箱**：无 GUI 显示会话、无屏幕截图能力、无麦克风音频输入。**所有需要视觉观察 / 截图 / 语音输入的阶段（A 实机启动视觉、B 像素级视觉、F 真实麦克风语音、G Voice→GUI 实时、H 真实 TTS 发声、I 真实键盘 Palette、K GUI 旅程）在本环境无法执行**，已按规范显式标注 **BLOCKED / NOT EXECUTED**，**绝不伪造 PASS**。

结论：`PHASE 6 STATUS = BLOCKED`（纯环境限制，非产品缺陷）。可观测维度全 PASS；不可观测维度需由人工在真实 Windows GUI + 音频环境中补验（见 §14 交接清单）。

## 2. Environment

- Windows / Administrator；Electron 规范壳 `electron/`（无 package.json，由外部 `electron` 运行时加载）。
- **后端 LIVE**：`python server.py` @ `127.0.0.1:8010`（实测 `/api/health` 200，`ai_name:"小6"`，`tts_backend:"qwen3"`，`model:"agnes-2.5-flash"`）。
- `/api/ready`：`ready:true, ok:false, degraded:true`（核心聊天/工具可用；部分可选子系统降级，与 Phase 5 一致）。
- **Agent 能力探针**：无交互式桌面会话（explorer 探测无可供 agent 观察的显示）、无屏幕截图能力、无麦克风音频输入 → GUI 渲染与语音输入对 agent 不可见。

## 3. Electron Startup（STAGE A）

- **静态核验（已重读 `main.js`）**：规范壳双窗口（avatar=dyna-orb.html, workspace=zz-space/index.html）共享 `ZHUANGZHOU_PORT`；backend-launcher 仅探针；Tray `Six · Advanced Workspace`；security fail-closed；IPC 仅 `avatar:*`；无第二 Runtime、无重复启动逻辑。
- **资产层实测**：dyna-orb.html / zz-space/index.html / 相关 JS 全部 200。
- **A1–A11（GUI 实机视觉项：启动/Orb 出现/Workspace 出现/共享 Runtime/Tray/无重复 Runtime/无 fatal console/无白屏/无尺寸异常/localhost 导航/外部导航阻断）**：
  - A4（两窗口共享 Runtime）、A10（localhost 导航）、A11（外部导航阻断，security.js fail-closed 静态核验）可经代码确认；
  - **A1/A2/A3/A5/A6/A7/A8/A9（需真实窗口渲染观察）**：**BLOCKED**（headless 无显示会话，禁止以 headless-only 充当最终验收）。

## 4. Workspace Visual Acceptance（STAGE B）

- **静态结构核验（B1–B26）**：`zz-space/index.html` 顶栏/左导航/中栏四 Tab/右上下文栈/Palette/Toast/Banner/输入区/流式 markdown/Tool·Approval·Result block/responsive 全在（基线 §6）。
- **品牌核验**：标题 `小6 · Advanced Workspace`，品牌 `小6`，无旧 `庄周/ZhuangZhou/Xiao6Hub` 可见品牌（§13 同）。
- **视觉质量（漂亮/统一/高级/科技感/多余 UI/拥挤/空旷/视觉断层/旧设计残留/旧品牌）**：仅可经代码层确认"无死链接/无空指针"；**像素级观感、布局/间距/字体/卡片/统一度** → **BLOCKED**（无渲染观察）。代码层无旧设计残留/旧品牌。
- 不基于"个人审美意见"修改；仅记录（本阶段零修改）。

## 5. Chat Acceptance（STAGE C）

- **C1–C7（普通/长文本/Streaming/Markdown/Code block/Table/多轮）**：
  - API 层：`POST /api/chat` 实跑返回 `choices[].delta.content` 流式分片（ping→`"在。"`）→ 证明后端 chat 可用。
  - **GUI 层（输入框稳定/自动滚动/历史渲染/Markdown 正确/最终回答完整）**：**BLOCKED**（无渲染观察）。流式/Markdown/历史渲染逻辑存在于 `zz-workspace.js` 解析层（基线 §6），但未做 GUI 实观。

## 6. Streaming Acceptance（STAGE C3）

- **SSE 流式 + `[DONE]` 终止**：本会话实跑确认 `data: {"choices":[{"delta":{"content":"在。"}}]}` → `data: "[DONE]"`。**API 流式 PASS**。
- **GUI 平滑度/增量渲染观感**：**BLOCKED**（无渲染观察）；前端解析器按 `[DONE]`/`"DONE"` 终止、增量 block 缓存逻辑（基线 §6）静态 intact。

## 7. Tool Acceptance（STAGE D）

- **真实工具链（本会话实跑）**：查询 `"现在几点了"` → SSE 流真实出现 `zhuangzhou_event`→`tool_start`→`get_time`→`zhuangzhou_event`→`tool_end`→`get_time`。
- **User→Chat→Tool Start→Tool End→Result** 链路在运行时确证。**API 层 Tool 链路 PASS**。
- **GUI 层（Tool block 状态/结果卡片/最终回答）**：**BLOCKED**（无渲染观察）；`zz-workspace.js` `onTool`/`finish` 渲染路径（基线 §6）静态 intact。

## 8. Approval Acceptance（STAGE E）

- **策略核验（`.env` + 源码）**：`AGENT_POLICY_DEFAULT=auto`（L50）；低危工具 auto，高危工具 `confirm` 审批；`tool_started` 经 EventBus；Core Policy 为最终控制点（Phase 5 已核验 Policy Engine 边界）。
- **真实触发**：本 headless 环境**未执行**任何会触发高危审批的安全查询（无安全方式在 agent 内模拟用户确认 GUI 交互）。
- **结论**：**APPROVAL = NOT EXECUTED — SAFE ENVIRONMENT LIMITATION**（按规范 §8 显式记录，不伪造 PASS）。
- 待补验（人工）：Workspace Approval UI 出现 / Confirm 生效 / Reject 生效 / Tool 不绕过 Approval / UI 不直接执行 Tool / Core Policy 仍为最终控制点。

## 9. Voice Acceptance（STAGE F）

- **Voice Orb 5-state 契约**：`dyna-orb.js` `STATES={idle,listening,thinking,speaking,error}` 重读确认 intact、未改（基线 §7）。
- **真实麦克风链路（IDLE→LISTENING→THINKING→SPEAKING→IDLE / ERROR）**：**BLOCKED**（无音频采集设备，无麦克风输入）。ASR→Core→Response→GUI→TTS 完整闭环无法在本环境跑。

## 10. Voice → GUI Acceptance（STAGE G）

- **同步机制**：`agent_state`/`hud_state` 经 `zhuangzhou_event` SSE 扇出，前端 `zz-workspace.js`/`app.js` 按键更新 Orb 状态与右上下文栈（基线 §6）。
- **真实实时联动观察（Orb 各态 Workspace 是否感知 / IDLE 恢复）**：**BLOCKED**（依赖麦克风语音输入，headless 不可测）。代码路径完整。

## 11. TTS Acceptance（STAGE H）

- **当前 TTS backend**：`.env` `ZHUANGZHOU_TTS_BACKEND=qwen3`（L14），与 `/api/health` `tts_backend:"qwen3"` 一致；默认在线音 `zh-CN-YunxiNeural`。GPT-SoVITS / Qwen3-TTS 为可选（基线 §12）。
- **实际发声 / 不被朗读项（thinking/工具内部/审批内部）**：**BLOCKED**（无音频输出设备，无法观测发声）；配置与事件流设计契约（内部事件不朗读）静态 intact。

## 12. Command Palette Acceptance（STAGE I）

- **结构核验（静态）**：`zz-workspace.js` `COMMANDS` + `FEATURE_REGISTRY` 派生命令；`openPalette()`/`renderPalette()` 模糊匹配、键盘导航、Enter/Escape、`switchView` 路由、conditional gate（基线 §6）。未新增 COMMAND。
- **真实键盘（Ctrl/Cmd+K 打开/搜索/上下/Enter/Esc / command routing / `/command` / `@mention`）**：**BLOCKED**（无 GUI 交互）。结构/路由 PASS。

## 13. Brand Acceptance（STAGE J）

- **可见 UI 扫描（`index.html` 等）**：仅 `小6` / `Six · Advanced Workspace` / `Six`（Hub）；**无 `庄周` / `ZhuangZhou` / `Xiao6Hub`** 可见品牌（基线 §6,§15）。
- **允许内部**：`ZHUANGZHOU_*`、`zhuangzhou_event`、`window.zhuangzhou`、`appId`、物理路径、仓库 URL、IPC、EventBus —— 全部未改。
- **结论：BRAND = PASS**（用户可见旧品牌清零）。

## 14. Full User Journey（STAGE K）

- **API 路径可推演**（启动→Orb→Workspace→Home→发消息→Streaming→回答→切 Sessions/Memory/Tasks→Capabilities→Cmd-K→Voice→Tool→Approval→Result→再聊天）：后端 chat/SSE/Tool 实跑 PASS，导航视图 `switchView` 与 `FEATURE_REGISTRY` 静态 intact。
- **真实端到端 GUI 旅程（困惑点/卡顿/重复入口/死入口/统一感）**：**BLOCKED**（无 GUI 渲染与交互）。
- **人工补验交接清单**（在真实 Windows GUI + 音频环境）：
  1. 双击 `zz_launch.bat` 启动后端 + Electron，确认双窗口出现、无白屏/fatal。
  2. 逐视图点击 Home/Sessions/Projects/Tasks/Memory/Knowledge/Capabilities/Settings，记录布局/统一度/重复或死入口。
  3. 发一句长文本 + Markdown + Code + Table，观察流式平滑度、自动滚动、历史、最终回答完整。
  4. 触发一个真实工具（如"现在几点了"），观察 Tool block→状态→结果→最终回答。
  5. 触发一个高危/confirm 工具，确认 Approval UI 出现、Confirm/Reject 生效、Tool 不绕过。
  6. 用真实麦克风走 IDLE→LISTENING→THINKING→SPEAKING→IDLE，确认 Voice→GUI 实时联动与 TTS 发声（thinking/内部事件不朗读）。
  7. Ctrl/Cmd+K 实测搜索/路由；试 `/command`、`@mention`。
  8. 截图留存作为证据（本阶段缺失项）。

## 15. Bugs Found

- **B1（历史/环境）**：headless agent 无 GUI 显示/截图/麦克风 → A/B/F/G/H/I/K 视觉与语音项无法实机验收 → 非产品缺陷，标注 BLOCKED/NOT EXECUTED。
- **B2（观察）**：后端 `/api/ready` `ok:false, degraded:true`（部分可选子系统降级）；核心聊天/工具正常 → 不属本阶段红线范围，仅记录。
- **B3（观察）**：`scripts/verify-speech-orb.js` 因 Phase 5 归档 orb-pet 失效（开发脚本，非运行时）。

## 16. Bugs Fixed

- **无**。本阶段零功能变更，红线禁止修改。发现项均为历史/环境性，不实施修复（符合 P2/P3/P5 仅记录策略）。

## 17. Bugs Deferred

- P0：无（无启动崩溃/Runtime 崩溃/数据损坏/安全漏洞）。
- P1：无（核心交互/Voice 闭环/Tool/Approval 在可观测层均 PASS 或策略 intact；不可观测项归环境限制非缺陷）。
- P2/P3：GUI 视觉/体验问题需真实环境观察后分类；本阶段未观察故无条目。所有"体验问题"先记录再分类，未修改。

## 18. Screenshots / Evidence

- **本 agent 无法产生截图**（无显示会话/截图能力）。
- **可机读证据**（本次实测，存档于交付目录）：
  - `/api/health` 返回 `ai_name:"小6", tts_backend:"qwen3", theme:"dark-cyan"`（LIVE 证明）。
  - SSE ping：`data: {"choices":[{"delta":{"content":"在。"}}]}` → `data: "[DONE]"`（流式+终止 PASS）。
  - SSE Tool：`zhuangzhou_event→tool_start→get_time→zhuangzhou_event→tool_end→get_time`（Tool 链路 PASS）。
  - `node --check`：144 JS / 0 fail。
- 视觉/语音截图证据：**缺失**，由人工在真实环境补采（见 §14）。

## 19. API Invariance

- `POST /api/chat`（`messages` 格式）、`GET /api/config`、`/api/health`、`/api/ready` 契约未变；`zhuangzhou_event` 信封键未变；前端（Phase 4 修复后）发送 `ZHUANGZHOU_*` 配置键有效。**API CHANGE = 0**。

## 20. Port Invariance

- 8010 = LIVE（未变）；`8000/9880/3080/3210` 与基线一致（无 Six 监听/可选未启）。`ZHUANGZHOU_PORT` 及全部 `ZHUANGZHOU_*` 环境变量未改（`.env` 原样）。**PORT CHANGE = 0**。

## 21. Security Invariance

- `security.js` fail-closed 未改；`contextIsolation:true/nodeIntegration:false/webSecurity:true/sandbox:true`（workspace+avatar）未改；`will-navigate`/`setWindowOpenHandler` 守卫未改；preload 仅暴露无害窗口桥（无执行能力）。**Security = intact**。

## 22. DB Invariance

- 未触及任何 DB schema / 迁移 / 查询（红线 #11）。**DB CHANGE = 0**。

## 23. Functional Change Audit

- **本阶段零源码/零配置修改**（仅重读源码 + 新增 2 份报告文档）。
- 新增文档：`PHASE6_REAL_USER_ACCEPTANCE_BASELINE.md`、`PHASE6_REAL_USER_ACCEPTANCE_FINAL.md`。
- **FUNCTIONAL CHANGE = 0**。

## 24. Final Acceptance Matrix

| 项 | 结果 |
|---|---|
| A Electron 启动（GUI 视觉 A1-A9）| BLOCKED（headless 无显示）|
| A 共享 Runtime / 导航 / 外部阻断（A4/A10/A11）| PASS（代码核验）|
| B 视觉 Gate（结构/品牌）| PASS（结构/品牌）/ 像素级 BLOCKED |
| C Chat（API）| PASS（实跑）|
| C Chat（GUI 观感）| BLOCKED |
| C3 Streaming（API 流式+[DONE]）| PASS（实跑）|
| C3 Streaming（GUI 平滑度）| BLOCKED |
| D Tool（API tool_start→tool_end）| PASS（实跑确认）|
| D Tool（GUI block/结果）| BLOCKED |
| E Approval（策略+路径）| PASS（静态）/ 真实触发 NOT EXECUTED |
| F Voice（5-state 契约）| PASS（静态）|
| F Voice（真实麦克风链路）| BLOCKED |
| G Voice→GUI（实时联动）| BLOCKED |
| H TTS（backend 配置）| PASS（qwen3 确认）/ 发声 BLOCKED |
| I Command Palette（结构）| PASS（静态）/ 交互 BLOCKED |
| J Brand（可见 UI）| PASS（无旧品牌）|
| K User Journey（API 路径）| PASS（推演）/ GUI 旅程 BLOCKED |
| N node --check 回归 | PASS（144 JS / 0 fail）|
| N API/Port/Security/DB 不变性 | PASS（全 0）|

## 25. Known Limitations

- 验收在 **headless agent** 执行，无 GUI 显示 / 无屏幕截图 / 无音频采集 → A/B/F/G/H/I/K 视觉与语音项为静态+资产层验证 + 显式 BLOCKED/NOT EXECUTED。
- 后端 `degraded:true` 使部分可选子系统未就绪，但不影响核心 chat/tool（已实跑 PASS）。
- 真实 GUI 视觉、麦克风语音、TTS 发声、Approval 确认交互、截图证据需人工在真实 Windows GUI + 音频环境补验（§14）。

## 26. Final Verdict

```
PHASE 6 STATUS：       BLOCKED

REAL GUI：              BLOCKED（headless 无显示会话 / 截图能力）
WORKSPACE：             BLOCKED（像素级视觉）/ 结构+品牌 PASS
CHAT：                  PARTIAL（API 实跑 PASS / GUI 观感 BLOCKED）
STREAMING：             PARTIAL（SSE + [DONE] 实跑 PASS / GUI 平滑度 BLOCKED）
TOOL：                  PARTIAL（tool_start→tool_end 实跑 PASS / GUI block BLOCKED）
APPROVAL：              NOT_EXECUTED（安全环境限制，路径+策略静态核验完整）
VOICE：                 BLOCKED（无麦克风音频输入）
VOICE→GUI：             BLOCKED（依赖语音输入）
TTS：                   BLOCKED（无音频输出观测）/ backend=qwen3 配置 PASS
COMMAND PALETTE：       BLOCKED（无 GUI 交互）/ 结构+路由 PASS
BRAND：                 PASS（可见 UI 无旧品牌）

CORE CHANGE：           0
VOICE ORB CHANGE：      0
DSH CHANGE：            0
DB CHANGE：             0
API CHANGE：            0
PORT CHANGE：           0
FUNCTIONAL CHANGE：     0
```

> 说明：STATUS=BLOCKED 为**纯环境限制**（headless agent 无法观察 GUI/音频），**非产品缺陷**。所有可机读维度（后端 LIVE、SSE 流式、Tool 链路、5-state 契约、47 Feature Registry、安全收敛、品牌清零、端口/API/DB 不变性、node --check 0 fail）均 PASS。真实用户验收的视觉/语音闭环由人工按 §14 清单在真实 Windows GUI + 音频环境完成。

---

*STOP：按 Phase 6 §20，完成后不进入 Phase 7，不新增任何功能/UI/Voice/Memory/Capability/Plugin/Runtime/Agent/Tool。本阶段零修改。等待老板下一阶段授权（建议：在真实 GUI+音频环境执行 §14 交接清单以解锁 BLOCKED 项）。*
