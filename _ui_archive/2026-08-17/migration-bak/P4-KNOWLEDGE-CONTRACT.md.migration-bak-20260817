# P4 · P4-KNOWLEDGE-CONTRACT

> 阶段：P4 Phase 1 DESIGN ONLY · 项目：ZhuangZhou Core v1.4.0
> 关联：`P4-MEMORY-CONTRACT.md` / `P4-CONTEXT-CONTRACT.md` / 主文档

---

## 1. 已验证的真实 Knowledge 架构

```mermaid
graph TB
    subgraph FACADE[knowledge.py 公共门面]
      KF[knowledge.load/reload/search/resolve/related/<br/>validate/stats/list_docs/ingest_document/...]
    end
    subgraph ENGINE[knowledge_runtime 引擎]
      KR[KnowledgeRuntime 单例]
      SE[search.Searcher 文件级关键词]
      RE[resolver.Resolver wikilink/id]
      LI[links.LinkGraph backlinks]
      LO[loader.KnowledgeDoc]
      VA[validator.ValidationReport]
      CA[cache.DocCache]
      MA[manifest]
    end
    KF --> KR
    KR --> SE
    KR --> RE
    KR --> LI
    KR --> LO
    KR --> VA
    KR --> CA
    KR --> MA
    KR -->|读取| MD[("knowledge/*.md<br/>Local First, git-tracked")]
```

**关键事实（verify-before-design）：**
- `knowledge.py` 是**门面**，委托 `knowledge_runtime`（第 25 行 `from knowledge_runtime import KnowledgeRuntime, get_runtime`）。**二者是同一系统**，非两套实现。
- `knowledge_runtime` 引擎：**无数据库 / 无 RAG / 无 embedding / 无向量库**（__init__.py:14 明确）。
- `.md` 文件是唯一真相源（Local First，git-tracked）。
- 召回方式：文件级关键词 / 结构化（wikilink `[[Note]]`、backlinks、shared tags）。**无语义/向量召回**（legacy RAG 后端已移除）。
- 唯一生产调用方：`context/knowledge_source.py:31` → `knowledge.search(query, limit=4)` → 门面 → 引擎。

---

## 2. Knowledge 必须与 Memory 分离（红线）

```mermaid
graph LR
    M[(memories 长期记忆)] -.隔离.-> KN[("knowledge/*.md")]
    KN -.经.-> KR[knowledge_runtime]
    KR -.经.-> KS[KnowledgeSource]
    KS --> CE[Context Engine]
    M -.经.-> RET[DefaultEvolutionPolicy]
    RET --> CE
```

- Knowledge **不进 `memories`**、不写 `mem_vectors`(knowledge scope 为 legacy 残留，应清)、不走 Memory Retrieval。
- Memory Retrieval 与 Knowledge Retrieval **不共享底层接口**（Memory 走 DefaultEvolutionPolicy + embed 语义；Knowledge 走 knowledge_runtime 关键词/结构化）。二者在 Context Engine 层汇合，但底层独立。

---

## 3. Knowledge 契约（KnowledgeDocument / Chunk / Link / SearchResult）

> 是否引入 Chunk abstraction？**根据真实源码决定：不需要。**
> `knowledge_runtime` 以**整个 .md 文档**为最小召回单元（`Searcher` 文件级，`loader.KnowledgeDoc` 整篇加载）。无 chunk 表、无 chunk 索引。因此**不新增 Chunk 抽象**，保持文档级。

| 概念 | 真实对应 | 说明 |
|---|---|---|
| KnowledgeDocument | `knowledge/*.md` + `KnowledgeDoc` | 单篇文档即单元 |
| KnowledgeChunk | **不引入** | 文档级召回，无需 chunk |
| KnowledgeLink | `links.LinkGraph` + `[[wikilink]]` | 文档间引用（backlinks/forward/shared tags） |
| KnowledgeSearchResult | `Searcher` 返回 hits（title/snippet/path） | `KnowledgeSource` 转 ContextItem |

---

## 4. 孤儿 DB 表处置（knowledge_docs / knowledge_chunks）

```mermaid
sequenceDiagram
    participant M as 迁移脚本
    participant DB as SQLite
    M->>DB: SELECT COUNT(*) FROM knowledge_docs
    DB-->>M: 0 (零 INSERT，仅 CREATE @ db.py:162)
    M->>DB: SELECT COUNT(*) FROM knowledge_chunks
    DB-->>M: 0
    M->>DB: DROP TABLE knowledge_docs / knowledge_chunks
    Note over M,DB: count>0 → 中止(fail closed)
```

- 真实复核：`knowledge_docs`/`knowledge_chunks` 仅 `CREATE TABLE`（db.py:162–168），**零 INSERT**（grep 确认）。
- 属 legacy RAG 后端残留，RAG 已移除。
- **本阶段禁止 DROP**（红线）；P4.5 经 count 校验（零数据）后 DROP，无信息损失。

---

## 5. semantic_fusion.py 的归属（重要）

```mermaid
graph TD
    SF[semantic_fusion.py] -->|唯一调用方| PR[perception_runtime.py]
    PR -->|Perception 层| P[视觉/语义感知]
```

- `semantic_fusion` **仅被 `perception_runtime.py` 引用**（grep 确认，唯一调用方）。
- 归属：**Perception（感知层）**，非 Knowledge、非 Semantic Processing 独立层。
- **不得因名字相似并入 Knowledge**（指令明确禁止）。P4 Knowledge 契约不包含 semantic_fusion。
- 与 Memory/Context/Knowledge 均无调用关系，无迁移影响。

---

## 6. KnowledgeContract 详表

| 维度 | 内容 |
|---|---|
| owner | `knowledge.py`(facade) + `knowledge_runtime`(engine) |
| storage | `knowledge/*.md`（文件系统，非 DB） |
| writer | `knowledge_runtime.ingest_document` / 直接文件编辑（git-tracked） |
| reader | `KnowledgeSource` → `knowledge.search` |
| lifecycle | 文件版本（git 历史）；archive_conversation 等写 API 经门面 |
| retrieval eligibility | 关键词/结构化（wikilink/backlinks/tags）；无语义向量 |
| retention | 文件永久（git） |
| confidence | 不适用（文档级，非置信打分） |
| provenance | 文档路径 / source 标记 |
| deduplication | 文档级（同 path 唯一）；wikilink 去环 |
| archive | 文件归档（门面 archive_conversation） |
| deletion | 文件删除（门面 delete_doc / delete_by_source） |
| event | 新增 `KNOWLEDGE_UPDATED`（文档增改时经 EventBus） |

---

## 7. 是否需要 chunk abstraction？

**否。** 真实 `knowledge_runtime` 为文档级召回，无 chunk 索引。强行引入 chunk 会制造第二套抽象，违背「不重建已有能力」红线。保持文档级。

---

## 8. 测试策略（DESIGN，P4.5 执行）

| 测试 | 验证 |
|---|---|
| 边界 | 无路径将 knowledge 写入 memories / mem_vectors(knowledge scope) |
| 文件召回 | knowledge.search 返回文档级 hits |
| 链接 | wikilink/backlinks 解析正确 |
| 孤儿 | knowledge_docs/chunks count=0 后 DROP |
| 事件 | KNOWLEDGE_UPDATED 唯一且经 EventBus |

---

## 9. P4.5 执行入口（摘要）

- **修改**：`knowledge_runtime`（边界契约固化）、`eventbus.py`（KNOWLEDGE_UPDATED）、`db.py`（孤儿表 count→DROP 迁移块）、写入点补 publish_domain。
- **不改**：`.md` 文件格式、`knowledge.py` 门面 API（保持兼容）。
- **回归门**：孤儿零数据 + 事件唯一 + 边界测试通过。
- 详见 `P4-MIGRATION-PLAN.md` P4.5。
