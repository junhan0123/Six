# PHASE 4 — SIX NAMING MIGRATION · 命名基线 / BASELINE

> 建立于迁移执行前（retrospectively documented），作为 **VERIFY-BEFORE-CHANGE** 的约束依据。
> 本阶段唯一目标：**产品品牌统一为 小6 / Six / Six Hub**，零功能变更。

## 1. 旧 → 新 官方品牌映射

| 维度 | 旧 | 新 |
|------|----|----|
| 中文产品名 | 庄周 | **小6** |
| 英文产品名 | ZhuangZhou | **Six** |
| Xiao6Hub（用户可见） | Xiao6Hub | **Six Hub** / **小6 Hub** |
| 原 "庄周 OS" / "ZhuangZhou OS" | 庄周 OS | **Six OS**（英文 OS 形态，如 `小6 · Six OS`） |

## 2. 扫描范围（三仓库）

- `G:\ZhuangZhou\zhuangzhou-ui` —— 主 UI + 规范 Electron shell（`electron/`）
- `G:\xiao6-hub` —— CODEX 风格 GUI 外壳，集成 DSH 与小6功能
- `G:\deepseek-harness` —— DSH（底层智能体基座），作为**内部基础设施**，不冠以产品品牌

## 3. 分类体系（A–E）

| 类 | 含义 | 处置 |
|----|------|------|
| **A USER_VISIBLE_BRAND** | 界面标题/菜单/工具提示/通知/品牌区/产品元数据 | **必须改** |
| **B PRODUCT_METADATA** | package.json 的 productName/description/author/copyright/artifactName；features.json 的 app.name/feature 描述 | **原则上改** |
| **C INTERNAL_IDENTIFIER** | 局部变量（如 `isZhuangZhou`）、内部 asset 路径前缀（`ZhuangZhou_Avatar`）、内部 config key（`ZhuangZhou_TTS_VOICE`） | **按项判定** |
| **D API/PROTOCOL_IDENTIFIER** | `process.env.ZHUANGZHOU_*` 全部环境变量、IPC 通道（`companion:*`/`xiao6:*`/`avatar:*`）、`localStorage.zhuangzhou_sid`、`zhuangzhou_event` JSON 字段、`appId`、repo URL | **默认保留** |
| **E FILESYSTEM/DIRECTORY** | `G:\ZhuangZhou`、`G:\zhuangzhou`、`G:\xiao6-hub`、`%APPDATA%\Xiao6Hub`、桌面 `*.bat`、`ZhuangZhou_AutoStart` 任务名 | **默认保留物理路径** |

## 4. 冻结的迁移范围

**包含（迁移）：**
- Layer1 用户可见品牌字符串：`庄周`/`ZhuangZhou`/`ZHUANGZHOU`（作为品牌）→ `小6`/`Six`
- `Xiao6Hub` → `Six Hub`
- `features.json` / `README.md` / `VERIFY.md` 中用户可见品牌文案
- 桌面快捷方式生成器中的品牌（`庄周.lnk` → `小6.lnk`，描述 `ZhuangZhou AI Copilot` → `Six AI Copilot`）

**排除（保留，本阶段不改）：**
- 所有端口（8010 / 8000 / 9880 / 3080 / 3210）
- `ZHUANGZHOU_PORT` 及全部 `ZHUANGZHOU_*` 环境变量
- IPC 通道、EventBus 事件、DB 字段、`appId`
- 文件系统物理路径与仓库 URL
- DSH 作为内部基础设施（不冠产品品牌）
- 语音唤醒词（`庄周`/`小周`/`小6`，属 Voice 配置，归 Phase 5 Voice 范围）

## 5. 执行方式（VERIFY-BEFORE-CHANGE）

- **白名单 + 显式 token map**（非盲扫 sed/grep 替换）：
  - HTML 白名单：10 个文件（zz-space/index.html、index.html、companion.html、desktop-avatar/orb-ui.html、mobile-app.html、selfcheck.html、weather-modal-preview.html、xiao6-hub/renderer/index.html、electron/firstlaunch.html、pet/pet.html）
  - JS 白名单：20 个文件（zz-space/js ×3、xiao6-hub/renderer/renderer.js、pet/pet.js、根目录 controller ×13 等）
- **token map（HTML）**：`庄周→小6`, `ZhuangZhou→Six`, `ZHUANGZHOU→SIX`, `Zhuang Zhou→six`, `zhuang zhou→six`, `Xiao6Hub→Six Hub`, `Xiao6→Six`
- **token map（JS）**：同 HTML **减去 `Xiao6→Six`**（避免破坏 `onXiao6Log` 等内部回调）
- **安全哨兵**：`ZHUANGZHOU_PORT` 以 `\x00` 哨兵包裹，map 执行后还原，确保端口环境变量不被改写
- **启动器单独脚本**：仅做 `庄周→小6`，避免 `ZHUANGZHOU→SIX` 误伤 `G:\ZhuangZhou` 路径

## 6. 保留标识符安全清单（不得改）

```
ports:            8010 / 8000 / 9880 / 3080 / 3210
env:              ZHUANGZHOU_PORT 及全部 ZHUANGZHOU_*（ASR/TTS/KWS/WAKE_PHRASE/PROXY/WEB_SEARCH/SANDBOX/…）
preload bridge:   window.zhuangzhou（ZhuangZhou shell）、window.xiao6（xiao6-hub shell）
localStorage:     zhuangzhou_sid
JSON 字段:        zhuangzhou_event
IPC:              companion:* / xiao6:* / avatar:*
appId:            com.zhuangzhou.desktop / com.zhuangzhou.xiao6hub
路径:             G:\ZhuangZhou / G:\zhuangzhou / G:\xiao6-hub / %APPDATA%\Xiao6Hub
仓库 URL:         github.com/junhan0123/ZhuangZhou、github.com/AGI-ZhuangZhou/ZhuangZhou
任务名:           ZhuangZhou_AutoStart
```

## 7. 基线时已知风险登记

| ID | 风险 | 缓解 |
|----|------|------|
| R1 | `ZHUANGZHOU→SIX` 误伤 `ZHUANGZHOU_*` 环境变量引用 | 哨兵保护 `ZHUANGZHOU_PORT` + 二次全仓 `SIX_` 反向核查 + `node --check` |
| R2 | `ZhuangZhou→Six` 误伤仓库 URL | 二次扫描 `junhan0123/Six` / `AGI-ZhuangZhou/-/ZhuangZhou` |
| R3 | `Xiao6Hub→Six Hub` 误伤文件系统路径 | 核查 `Six Hub` 仅出现在标题/注释，无路径形态 |

---

*基线文档结束。迁移执行与验证见 `PHASE4_SIX_NAMING_MIGRATION_FINAL_REPORT.md`。*
