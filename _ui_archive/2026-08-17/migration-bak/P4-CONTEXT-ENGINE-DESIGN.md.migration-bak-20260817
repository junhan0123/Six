# P4.1 · P4-CONTEXT-ENGINE-DESIGN

> 阶段：P4.1 DESIGN ONLY · 项目：ZhuangZhou Core v1.4.0
> 关联：`P4-MEMORY-CANONICAL-CONTRACT.md` / `P4-UNIFIED-RETRIEVAL-DESIGN.md`

---

## 1. 现状与反模式

真实源码已确认：**`context.facade.build_context_prompt` 是全仓唯一提示词装配入口**（tools.py / server.py / server_handlers_* 全部 `from context import build_context_prompt`，零绕过）。

**但存在反模式**（用户要求消除）：

```mermaid
graph TD
    subgraph BAD[当前 MemorySource 反模式]
      MS[MemorySource.collect] -->|整段委托| M[memory.build_system_prompt<br/>单体大块]
      M -->|返回单条 ContextItem<br/>DEDUP_SCOPE=memory| CI[ContextItem 大 blob]
    end
    CI --> R[ContextRanker]
    R -->|颗粒度已丢失| B[Build]
```

`context/sources.py:18–39` 的 `MemorySource.collect()` 把**整个记忆系统提示词**委托给 `memory.build_system_prompt`，返回一个 `DEDUP_SCOPE_KEY="memory"` 的**单体 blob**。这导致：
- ContextRanker 的 5 维加权（source_priority/priority/recency/importance/user_relevance）**对记忆内部颗粒度完全失效**——记忆块被当成一个不可分割整体。
- Budget 裁剪只能整块砍，无法精细保留高价值单条记忆。
- 与"统一检索返回 ScoredItem"的设计冲突。

---

## 2. Canonical Context Assembly（唯一装配入口）

```mermaid
flowchart TB
    subgraph PIPE[context.facade 五阶段管线]
      C1[1 Collect<br/>各 Source 返回 ContextItem] --> C2[2 Rank<br/>ContextRanker 5维加权]
      C2 --> C3[3 Budget<br/>ContextBudget 裁剪]
      C3 --> C4[4 Bundle<br/>dedup_items_by_scope]
      C4 --> C5[5 Build<br/>拼装最终 Prompt]
    end
    C5 --> LLM[LLM]
```

| 阶段 | 职责 | 当前状态 |
|---|---|---|
| Collect | 各 Source 返回 `list[ContextItem]` | ✓ 已有骨架 |
| Rank | ContextRanker 加权 | ✓ 已有，但被 MemorySource blob 架空 |
| Budget | ContextBudget 按 token/calls 裁剪（FAIL CLOSED） | ✓ 已有，默认不裁剪 |
| Bundle | `dedup_items_by_scope` 按 DEDUP_SCOPE_KEY 去重 | ✓ 已有 |
| Build | 拼装 | ✓ 已有 |

---

## 3. SourceRegistry 模式（每源一个适配器）

```mermaid
graph LR
    subgraph SOURCES[Context Sources 适配器]
      S1[EvolvedMemorySource]
      S2[UserModelSource]
      S3[EpisodicSource]
      S4[KnowledgeSource]
      S5[PersonalContextSource]
      S6[PersonalAISource]
      S7[IdentitySource]
      S8[WeatherSource/...]
    end
    S1 -->|ContextItem 列表| COLL[Collect]
    S2 --> COLL
    S3 --> COLL
    S4 --> COLL
    S5 --> COLL
    S6 --> COLL
    S7 --> COLL
    S8 --> COLL
```

每个 Source 的契约：
- 返回 `list[ContextItem]`，**每条可独立排名/裁剪**。
- 必须带 `DEDUP_SCOPE_KEY`（scope 级去重）。
- **绝不**内部调用 `memory.build_system_prompt` 拼大块。

---

## 4. 改造：消除 MemorySource 单体

```mermaid
sequenceDiagram
    participant F as context.facade
    participant EMS as EvolvedMemorySource
    participant RP as DefaultEvolutionPolicy
    participant R as ContextRanker
    F->>EMS: collect(ctx)
    EMS->>RP: retrieve(ctx.user_text, top_k)
    RP-->>EMS: List[ScoredItem]
    EMS->>EMS: 逐条转 ContextItem<br/>+ DEDUP_SCOPE_KEY=memory
    EMS-->>F: List[ContextItem]  (颗粒度保留)
    F->>R: Rank(所有源 ContextItem)
    R-->>F: 全局排序
    F->>F: Budget + Bundle + Build
```

**关键修复（P4.4）：**
1. `EvolvedMemorySource.collect()` 改为逐条把 `ScoredItem` 转 `ContextItem`（保留 priority/recency/importance/token_est），并补 `DEDUP_SCOPE_KEY="memory"`（当前缺失，见 §7）。
2. `MemorySource`（`context/sources.py` 单体）弃用——其职责由 `EvolvedMemorySource` 接管。
3. `memory.build_system_prompt` 仅保留为 `FEATURE_CONTEXT_ENGINE=false` 时的回退路径，**绝不被任何 Context Source 直接调用**。

---

## 5. UserModel 边界修复

```mermaid
graph TD
    subgraph BAD[当前违规]
      UM[cognitive.user_model] -->|render_user_model_block 拼提示词| P[Prompt]
    end
    subgraph GOOD[P4.4 修正]
      UM2[cognitive.user_model] -->|返回结构化 ContextItem| CE[Context Engine]
      CE -->|Build 阶段渲染| P2[Prompt]
    end
```

`cognitive/user_model.py:269–332` 的 `render_user_model_block()` **越界拼提示词**（UserModel 不应装配提示词）。修正：UserModel 只返回结构化 `ContextItem`，渲染交给 Context Engine 的 Build 阶段。

---

## 6. 预算与隔离（Budget / Isolation）

```mermaid
graph LR
    A[各源 ContextItem] --> B[ContextRanker 排序]
    B --> C{ContextBudget}
    C -->|超 token 上限| D[按权重裁剪尾部]
    C -->|超 calls 上限| E[FAIL CLOSED 拒绝]
    D --> F[Bundle 去重]
    E --> F
```

- Budget 默认不裁剪（保持现状），但裁剪逻辑必须能**精细到单条记忆**（依赖 §4 的颗粒度修复）。
- 源隔离：每源独立 `DEDUP_SCOPE_KEY`，跨源不串扰。

---

## 7. 已确认的缺陷（真实源码）

| 缺陷 | 位置 | 影响 | P4.4 修复 |
|---|---|---|---|
| EvolvedMemorySource 缺 DEDUP_SCOPE_KEY | `memory_evolution/context_source.py:34–42` | 若启用会与 MemorySource + MemoryRecallSource **三重记忆注入** | 补 `DEDUP_SCOPE_KEY="memory"` |
| EvolvedMemorySource 永不注册 | `FEATURE_MEMORY_EVOLUTION` 未定义（ghost flag） | 治理引擎不可达 | config.py 正式定义该 flag |
| UserModel 拼提示词 | `cognitive/user_model.py:269–332` | 边界违反 | 改为返回 ContextItem |
| MemorySource 单体 blob | `context/sources.py:18–39` | Ranker/Budget 颗粒度失效 | 弃用，由 EvolvedMemorySource 接管 |

---

## 8. 配置开关矩阵（context/builder.py 注册，真实）

| Source | 开关 | 默认 |
|---|---|---|
| MemorySource（弃用） | `FEATURE_CONTEXT_ENGINE` | ON（临时，P4.4 后转 EvolvedMemorySource） |
| EvolvedMemorySource | `FEATURE_MEMORY_EVOLUTION`（待定义） | OFF→P4.4 后 ON |
| UserModelSource | `FEATURE_USER_MODEL` | env true |
| EpisodicSource | `FEATURE_EPISODIC_MEMORY` | env true |
| KnowledgeSource | `FEATURE_KNOWLEDGE_PLATFORM` | env true |
| PersonalContextSource | `FEATURE_PERSONAL_CONTEXT` | true |
| PersonalAISource | `FEATURE_PERSONAL_AI` | true |
| IdentitySource | `FEATURE_IDENTITY` | true |
| MemoryRecallSource | `FEATURE_MEMORY_INTELLIGENCE`（默认未注册） | 并入 EvolvedMemorySource |
| MemoryV2Source | `MEMORY_V2_ENABLE`（memory_v2/flags 独立） | OFF |

---

## 9. 测试策略（DESIGN，P4.4 执行）

| 测试 | 验证 |
|---|---|
| 颗粒度 | EvolvedMemorySource 返回多条 ContextItem（非单体 blob） |
| 去重 | 启用后无三重记忆注入（DEDUP_SCOPE_KEY 生效） |
| 预算隔离 | 超预算时单条记忆可独立裁剪 |
| 边界 | UserModel 不产出提示词文本 |
| Legacy Bypass | 全仓除 `context.facade` 外无第二提示词装配入口 |

---

## 10. P4.4 执行入口（摘要）

- **修改**：`context/sources.py`（弃用 MemorySource 单体）、`memory_evolution/context_source.py`（补 DEDUP_SCOPE_KEY + 注册）、`cognitive/user_model.py`（移除 render）、`context/builder.py`（注册 EvolvedMemorySource）、`config.py`（定义 `FEATURE_MEMORY_EVOLUTION`）。
- **回归门**：Legacy Bypass Test + 无三重注入 + 提示词逐字节与旧实现一致（已有 COND 保障）。
- 详见 `P4-IMPLEMENTATION-ROADMAP.md` P4.4。
