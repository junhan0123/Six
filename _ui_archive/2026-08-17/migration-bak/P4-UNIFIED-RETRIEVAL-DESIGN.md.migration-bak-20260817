# P4.1 · P4-UNIFIED-RETRIEVAL-DESIGN

> 阶段：P4.1 DESIGN ONLY · 项目：ZhuangZhou Core v1.4.0
> 关联：`P4-MEMORY-CANONICAL-CONTRACT.md`

---

## 1. 问题陈述

当前检索逻辑**散落多处、权威不唯一**：

```mermaid
graph TD
    A[memory_intelligence.recall<br/>0.55*importance+0.45*overlap] -->|被调用| B[多处]
    C[cognitive.episodic.recall_episodes] -->|被调用| B
    D[memory_v2 embed/retrieval 适配器] -->|休眠可达| B
    E[memory_query.query_memory<br/>SQL LIKE] -->|被调用| B
    B -->|各自拼提示词| F[Prompt]
    style A fill:#5f1f1f,color:#fff
    style C fill:#5f1f1f,color:#fff
    style D fill:#5f1f1f,color:#fff
    style E fill:#5f1f1f,color:#fff
```

P4.1 目标：**唯一检索权威 = `memory_evolution.retrieval_policy.DefaultEvolutionPolicy`**。其余召回逻辑作为其内部实现被复用，不再作为对外第二入口。

---

## 2. Canonical Retrieval Authority

```mermaid
flowchart LR
    subgraph IN[输入]
      Q[用户查询/上下文文本]
    end
    Q --> D[DefaultEvolutionPolicy.retrieve]
    subgraph INTERN[内部复用（非对外入口）]
      MI[memory_intelligence.recall<br/>语义+重要性]
      EP[cognitive.episodic.recall_episodes<br/>情节]
      MV[memory_v2 embed 适配器<br/>向量召回可选]
    end
    D --> MI
    D --> EP
    D --> MV
    D --> FL[过滤: decayed/archived/pending_review]
    D --> RK[排序: 统一权重]
    FL --> RK
    RK --> OUT[ScoredItem 列表]
```

**权威三要素：**
1. **唯一对外入口**：所有"取记忆喂上下文"的请求只走 `DefaultEvolutionPolicy.retrieve`。
2. **内部复用不重复**：`memory_intelligence.recall`、`cognitive.episodic.recall_episodes` 作为子能力被调用，不各自直接对外。
3. **排序权重唯一**：复用 `context.ranker.ContextRanker` 的加权模型（source_priority 0.35 / priority 0.25 / recency 0.15 / importance 0.15 / user_relevance 0.10），检索层与上下文层共享同一套排序语义，避免两处权重漂移。

---

## 3. 检索管线（Unified Retrieval Pipeline）

```mermaid
sequenceDiagram
    participant CE as context.facade
    participant RP as DefaultEvolutionPolicy
    participant MI as memory_intelligence
    participant EP as cognitive.episodic
    participant MV as memory_v2 embed(可选)
    participant TR as Truth/Conflict 治理
    CE->>RP: retrieve(query, top_k)
    RP->>MI: recall(query, top_k)
    RP->>EP: recall_episodes(query, top_k)
    RP->>MV: embed_recall(query, top_k) [if enabled]
    MI-->>RP: candidates
    EP-->>RP: candidates
    MV-->>RP: candidates
    RP->>TR: filter(decayed/archived/conflict×0.4/pending)
    TR-->>RP: filtered
    RP->>RP: dedupe + rank(统一权重)
    RP-->>CE: List[ScoredItem]
```

| 阶段 | 动作 | 负责 |
|---|---|---|
| Candidate 收集 | 并发调用 memory_intelligence + episodic（+ 可选向量） | DefaultEvolutionPolicy |
| Truth 过滤 | 排除 status=deprecated；conflict ×0.4；confidence 折减 | memory_evolution/Truth |
| 去重 | content_hash + 关系归并 | DefaultEvolutionPolicy |
| 排序 | 统一权重（与 ContextRanker 对齐） | DefaultEvolutionPolicy |
| 输出 | `ScoredItem(source_kind, content, score, ...)` | DefaultEvolutionPolicy |

---

## 4. 与 Context Engine 的边界

```mermaid
graph LR
    RET[Retrieval 层<br/>返回 ScoredItem] -->|不拼提示词| CE[Context Engine<br/>ContextRanker 再排]
    CE -->|ContextItem| BUNDLE[Bundle]
    BUNDLE --> BUILD[Build Prompt]
    RET -.不直接.-> BUILD
```

- Retrieval 只返回**结构化 `ScoredItem`**，绝不拼装提示词（修复当前 `memory_intelligence.build_block` 越界拼文本的问题——它应只产出数据，由 Context Engine 决定如何渲染）。
- 排序分两段：Retrieval 做"召回内排序"，ContextRanker 做"跨源全局排序 + 预算裁剪"。二者权重同源。

---

## 5. 排名契约（Ranking Contract）

```mermaid
graph TD
    A[ScoredItem] --> B{权重合成}
    B -->|source_priority| C[0.35]
    B -->|priority| D[0.25]
    B -->|recency| E[0.15]
    B -->|importance| F[0.15]
    B -->|user_relevance| G[0.10]
    C --> H[final_score]
    D --> H
    E --> H
    F --> H
    G --> H
```

| 维度 | 权重 | 来源 |
|---|---|---|
| source_priority | 0.35 | ContextRanker.SOURCE_BASE_PRIORITY |
| priority | 0.25 | ScoredItem.priority |
| recency | 0.15 | 时间戳衰减 |
| importance | 0.15 | memory_intelligence.importance |
| user_relevance | 0.10 | 查询相关度 |

---

## 6. 去重与合并（Dedup / Merge）

```mermaid
flowchart TB
    R[raw candidates] --> H[content_hash 去重]
    H --> REL[relations 图归并<br/>同实体多记忆聚簇]
    REL --> CONF[保高置信<br/>conflict 降权]
    CONF --> OUT[最终 ScoredItem]
```

- `content_hash` 命中 → 同一条，合并元数据（保高置信）。
- `relations` 连通 → 同一实体簇，召回时同进同出。
- conflict 记忆 ×0.4（沿用 memory_intelligence 规则）。

---

## 7. 配置与开关

| 开关 | 当前 | P4.1 |
|---|---|---|
| `FEATURE_MEMORY_INTELLIGENCE` | env 默认 true | 保留（控制是否注入【长期记忆】块，由 Context Source 决定） |
| 语义向量召回（memory_v2 embed） | `MEMORY_V2_ENABLE` 默认 OFF | 作为 DefaultEvolutionPolicy 的**可选子能力**，受 config.py 单一开关控制（P4.6 合并 flag） |
| `FEATURE_MEMORY_EVOLUTION` | **从未定义（ghost）** | P4.4 在 config.py 正式定义，作为"启用 evolved Memory Source"的总闸 |

---

## 8. Deprecation / 迁移影响

- `memory_intelligence.recall`：保留为内部实现，对外调用点改为经 DefaultEvolutionPolicy。
- `memory_query.query_memory`：保留为"对话内显式搜索"API，底层读 `memories`；不进入上下文自动召回链。
- `memory_v2` 检索适配器：抽取为可复用 embed 工具，写入能力禁用。
- `HybridPolicy` / `RerankerPolicy`：当前为占位委托，P4.3 落地为真实策略或明确弃用。

---

## 9. 测试策略（DESIGN，P4.3 执行）

| 测试 | 验证 |
|---|---|
| 检索幂等 | 同 query 多次 retrieve 结果稳定 |
| 排名一致性 | Retrieval 排序与 ContextRanker 权重同源、无漂移 |
| 去重 | content_hash 命中只返回 1 条 |
| 冲突降权 | conflict 记忆 score ×0.4 |
| 边界 | 不返回 archived/decayed/pending_review |
| Legacy Bypass | 扫描全仓，除 DefaultEvolutionPolicy 外无第二检索入口 |

---

## 10. P4.3 执行入口（摘要）

- **修改文件**：`memory_evolution/retrieval_policy.py`（提升为权威）、新增 `retrieval/__init__.py`（facade）。
- **复用**：`memory_intelligence.recall`、`cognitive.episodic.recall_episodes`。
- **回归门**：Legacy Bypass Test 通过 + 排名权重无漂移。
- 详见 `P4-IMPLEMENTATION-ROADMAP.md` P4.3。
