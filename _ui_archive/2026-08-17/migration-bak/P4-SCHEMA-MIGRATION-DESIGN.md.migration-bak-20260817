# P4.1 · P4-SCHEMA-MIGRATION-DESIGN

> 阶段：P4.1 DESIGN ONLY · 项目：ZhuangZhou Core v1.4.0
> 关联：`P4-MEMORY-CANONICAL-CONTRACT.md` / `P4-KNOWLEDGE-BOUNDARY-DESIGN.md`
> 红线：不丢历史数据；不 DROP 带数据的表；迁移幂等可重放。

---

## 1. 迁移原则

```mermaid
graph TD
    A[迁移脚本] --> B[幂等: 可重跑无副作用]
    A --> C[无 DROP 带数据表]
    A --> D[content_hash 统一为唯一幂等键]
    A --> E[孤儿表 count 校验后处置]
    A --> F[失败 fail closed 回滚]
```

| 原则 | 说明 |
|---|---|
| 幂等 | 每步 ALTER/INDEX 可重复执行（IF NOT EXISTS / 存在性先查） |
| 保数据 | 任何带数据的表不物理 DROP |
| 孤儿处置 | 仅零数据孤儿表可 DROP（前置 count 校验） |
| 可回滚 | 迁移失败整体回滚，不影响既有数据 |

---

## 2. 真实 Schema 现状（复核）

| 表 | 现状 | 处置 |
|---|---|---|
| `memories` | 基础 CREATE(db.py:73–80) 已含 `content_hash TEXT UNIQUE`；archived/confidence/source/status/verified_at 由迁移 662–745 补齐 | **固化契约为 Canonical** |
| `mem_vectors` | scope='memory'/'knowledge'；记忆投影有写入 | 保留（Projection 层） |
| `knowledge_docs` / `knowledge_chunks` | 仅 CREATE(db.py:162–168)，**零 INSERT** | 孤儿 → P4.5 count 校验后 DROP |
| `chat_log` / `conversation_memories` | 对话层 | 保留（非 Memory Store） |
| `user_model` / `episodes` / `learnings` / `important_dates` | 各自领域 | 保留 |

**Phase 0 基线偏差纠正：** 基线称 `memories` 基础 CREATE 缺 content_hash 等列 —— **错误**。真实 db.py:80 已含 `content_hash TEXT UNIQUE`，其余由既有迁移补齐。本设计**不新建表、不重复加列**，仅固化契约 + 修正 distiller 写入方式。

---

## 3. content_hash 统一（幂等去重键）

```mermaid
flowchart LR
    A[写入 content] --> B[memory_content_hash<br/>sha1(归一化)]
    B --> C[content_hash]
    C --> D{UNIQUE 约束}
    D -->|冲突| E[ON CONFLICT DO NOTHING<br/>或合并元数据]
    D -->|新| F[INSERT]
```

- `memory_content_hash()`(db.py:244) = sha1 空白归一化内容，已为唯一幂等键。
- 所有 `memories` 写入者必须走此键保证幂等（distiller 修正重点）。
- `mem_vectors` 复用同一 content_hash 关联（scope 区分 memory/knowledge）。

---

## 4. 孤儿表处置（knowledge_docs / knowledge_chunks）

```mermaid
sequenceDiagram
    participant M as 迁移脚本
    participant DB as SQLite
    M->>DB: SELECT COUNT(*) FROM knowledge_docs
    DB-->>M: 0
    M->>DB: SELECT COUNT(*) FROM knowledge_chunks
    DB-->>M: 0
    M->>DB: DROP TABLE knowledge_docs
    M->>DB: DROP TABLE knowledge_chunks
    Note over M,DB: 若任一 count>0 → 中止迁移(fail closed)
```

- 因零数据，DROP 不损失信息 → **唯一被放行 DROP 的表**。
- 前置 `SELECT COUNT(*)` 校验，非空则中止（fail closed），绝不强删带数据表。

---

## 5. 既有迁移复用（不重写）

db.py 已有**安全幂等迁移**，直接复用：
- `_migrate_memories(conn)`(662–712)：加 archived + content_hash（3 步：ALTER 普通 → CREATE UNIQUE INDEX → 回填，含重复行 DELETE 兜底）。
- `_migrate_memory_truth(conn)`(715–745)：加 confidence/source/status/verified_at（memories）+ project/source/event（episodes）。

```mermaid
graph TD
    A[现有 _migrate_memories] --> B[加 archived]
    A --> C[加 content_hash UNIQUE]
    A --> D[重复行 DELETE 兜底]
    E[现有 _migrate_memory_truth] --> F[加 confidence/source/status/verified_at]
    E --> G[加 project/source/event(episodes)]
```

P4.1 设计：保留上述迁移，仅在其后追加"契约固化"步骤（索引校验、distiller 修正由 P4.2 应用层完成，不由迁移脚本做数据改写）。

---

## 6. 不做的迁移（红线）

```mermaid
graph TD
    A[禁止] --> B[DROP 带数据的表]
    A --> C[删除历史对话/记忆]
    A --> D[改 content_hash 算法导致旧键失效]
    A --> E[隐写测试掩盖 bug]
```

| 禁止项 | 原因 |
|---|---|
| DROP 带数据表 | 红线：不丢历史数据 |
|---|---|
| 删除历史 | 红线 |
| 改 hash 算法 | 会使既有 UNIQUE 约束失效、破坏幂等 |
| 隐写测试 | 红线：不以改测试掩盖 bug |

---

## 7. 迁移步骤清单（P4.2 / P4.5）

| 步骤 | 动作 | 文件 | 风险 |
|---|---|---|---|
| M1 | 固化 content_hash 为唯一键（已存在，校验） | db.py | 低 |
| M2 | distiller 改走 Canonical API 真实列 | memory_distiller.py | 中（需回归） |
| M3 | lifecycle stale 注释修正 | memory_evolution/lifecycle.py | 低 |
| M4 | 孤儿表 count 校验 → DROP | db.py 迁移 | 低（零数据） |
| M5 | mem_vectors scope 清理（knowledge 投影无写入则归并） | db.py | 低 |

---

## 8. 迁移幂等验证（DESIGN，P4.2/P4.5 执行）

| 测试 | 验证 |
|---|---|
| 重跑幂等 | 同迁移脚本连跑 2 次无报错、数据一致 |
| 孤儿校验 | count>0 时迁移中止 |
| 数据保全 | 迁移前后 memories 行数不变（除孤儿 DROP） |
| hash 一致 | 同内容 content_hash 不变 |

---

## 9. 执行入口（摘要）

- **修改**：`db.py`（追加孤儿表 count→DROP 迁移块）、`memory_distiller.py`（P4.2 修正）。
- **复用**：`_migrate_memories` / `_migrate_memory_truth`，不重写。
- **回归门**：幂等重跑 + 数据保全 + 孤儿 count 校验。
- 详见 `P4-IMPLEMENTATION-ROADMAP.md` P4.2/P4.5。
