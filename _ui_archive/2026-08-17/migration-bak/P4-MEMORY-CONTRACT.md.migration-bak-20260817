# P4 · P4-MEMORY-CONTRACT

> 阶段：P4 Phase 1 DESIGN ONLY（不修改产品代码 / 数据库 / 配置 / UI / 测试）
> 项目：ZhuangZhou Core v1.4.0 · `G:\ZhuangZhou\zhuangzhou-ui`
> 关联主文档：`P4-MEMORY-CONTEXT-KNOWLEDGE-DESIGN.md` · 复核基线：`P4-MEMORY-CONTEXT-BASELINE.md`

---

## 0. 真实源码复核结论（本节依据）

| 表/模块 | 真实用途 | 是否 Canonical Memory |
|---|---|---|
| `memories` | 长期记忆主存储（content_hash UNIQUE；archived/confidence/source/status/verified_at 由迁移补齐） | ✓ **是** |
| `chat_log` | 对话轮次（db.save_turn 写入） | ✗ 对话记录 |
| `memory_summary` | 历史对话压缩快照（memory.compress_memory 写） | ✗ 派生摘要 |
| `conversation_memories` | 对话级沉淀摘要（date/topic/key_points） | ✗ 对话记录（派生） |
| `learnings` | 自我学习经验（self-learning 写） | ✗ 独立学习库 |
| `user_model` | 结构化用户画像 | ✗ 用户模型（C 类） |
| `profile` | 旧版 KV 用户画像（build_memory_block 读） | ✗ 用户模型（legacy，与 user_model 重叠） |
| `episodes` | 情节记忆 | ✗ 情节（C 类，独立） |
| `important_dates` | 重要日期提醒 | ✗ 时间型专用库 |
| `mem_vectors` | 语义向量投影（scope=memory/episode/knowledge） | ✗ 派生索引 |

**写入者复核（verify-before-design）：**
- `memories` 写入者：**`memory_distiller._persist`**（agent_runtime.py:947 `distill()` 调用，ON CONFLICT content_hash）+ hotspots / notes / imports。**但 distiller 把 confidence/source 塞进 `tags` JSON（memory_distiller.py:147–152），违反契约** → P4.2 修正。
- `cognitive/extractor.py` **不写 memories**：只写 `user_model` + `episodes`（明确委托 memories/summary 给 memory.compress_memory）。已确认非越界写入者。
- `build_memory_block`（memory.py:237）读 `profile` + `memory_summary` + `chat_log`(recent) + `learnings` → 这些是「记忆相关展示」，但**不是 memories 表本身**。

---

## 1. 什么叫 Memory？（§4 九问裁决）

```mermaid
graph TD
    Q1[什么叫 Memory] --> A1[memories 表的长期结构化记忆]
    Q2[chat_log 是 Memory?] --> A2[否: 对话记录层]
    Q3[memory_summary 是?] --> A3[否: 派生压缩快照]
    Q4[learnings 是?] --> A4[否: 独立学习库]
    Q5[user_model 是?] --> A5[否: 用户模型 C 类]
    Q6[episodes 是?] --> A6[否: 情节记忆 C 类]
    Q7[memories 是 canonical?] --> A7[是: 唯一 Canonical Memory 记录]
    Q8[conversation_memories 定位] --> A8[对话级沉淀摘要(派生)]
    Q9[important_dates 定位] --> A9[时间型专用提醒库]
```

| 问题 | 裁决 | 理由 |
|---|---|---|
| 什么叫 Memory | `memories` 表的长期结构化记忆 | 唯一承载「事实/事件/偏好/经验」的持久化层 |
| chat_log 是 Memory | 否 | 对话记录层（D 类），经 db.save_turn |
| memory_summary 是 Memory | 否 | 派生摘要（B 类），压缩产物 |
| learnings 是 Memory | 否 | 独立学习经验库，自成一格 |
| user_model 是 Memory | 否 | 用户模型（C 类），结构化画像 |
| episodes 是 Memory | 否 | 情节记忆（C 类），独立表 |
| memories 是 canonical | **是** | 唯一 Canonical Memory Store |
| conversation_memories 定位 | 对话级沉淀摘要 | D 类，date/topic/key_points |
| important_dates 定位 | 时间型专用库 | 提醒系统专用，非记忆 |

---

## 2. 分类（Canonical / Derived / Projection / Runtime Metadata）

```mermaid
graph LR
    subgraph CANON[Canonical]
      M[(memories)]
    end
    subgraph DERIV[Derived]
      MS[(memory_summary)]
      CM[(conversation_memories)]
      LN[(learnings)]
    end
    subgraph PROJ[Projection]
      MV[(mem_vectors scope=memory/episode)]
    end
    subgraph RUNTIME[Runtime Metadata]
      CL[(chat_log)]
      UM[(user_model)]
      EP[(episodes)]
      ID[(important_dates)]
      PR[(profile legacy)]
    end
    M -.治理.-> UM
    M -.治理.-> EP
    M -.投影.-> MV
```

| 分类 | 成员 | 是否 Memory Store |
|---|---|---|
| **Canonical (A)** | `memories` | ✓ 唯一 |
| **Derived (B)** | `memory_summary`, `conversation_memories`, `learnings` | ✗ 派生/独立 |
| **Projection** | `mem_vectors` | ✗ 语义索引 |
| **Runtime/User/State** | `chat_log`, `user_model`, `episodes`, `important_dates`, `profile` | ✗ 各自领域 |

---

## 3. 六类契约详表（owner / storage / writer / reader / lifecycle / retrieval / retention / confidence / provenance / dedup / archive / deletion / event）

### A. Canonical Memory
| 维度 | 内容 |
|---|---|
| owner | Memory 子系统（`memory.py` + `memory_evolution` 治理门面） |
| storage | `memories` 表 |
| writer | `memory_distiller`（修正后）、hotspots、notes、imports — 全部经 **Canonical Memory API** |
| reader | `memory_intelligence.recall`、`memory_query.query_memory`、`memory_evolution.retrieval_policy` |
| lifecycle | `memory_evolution.lifecycle` 状态机（active→decayed→archived→deprecated/tombstone） |
| retrieval eligibility | 经 `DefaultEvolutionPolicy`；排除 archived/decayed(可选)/deprecated/pending_review |
| retention | 永久（软归档，不物理删） |
| confidence | `confidence` 列（0–1）；distiller 修正后写入真实列（非 tags） |
| provenance | `source` 列（真实列）；distiller 修正后写入 |
| deduplication | `content_hash TEXT UNIQUE`（幂等 UPSERT） |
| archive | `archived=1`（软） |
| deletion | **仅逻辑/合规擦除**（tombstone，GDPR 右被遗忘） |
| event | `MEMORY_CREATED/UPDATED/STORED/LINKED/ARCHIVED` + 新增 `MEMORY_RETRIEVED/CONSOLIDATED/DECAYED` |

### B. Derived Memory
| 维度 | memory_summary | conversation_memories | learnings |
|---|---|---|---|
| owner | memory.compress_memory | conversation 子系统 | self-learning |
| storage | `memory_summary` | `conversation_memories` | `learnings` |
| writer | compress_memory | 对话沉淀 | self-learning 蒸馏 |
| reader | build_memory_block | memory_query | build_learnings_block |
| lifecycle | 随对话压缩重写 | 追加 | 追加 |
| retrieval | 非自动召回（注入块） | 经 memory_query | 注入块 |
| retention | 滚动覆盖 | 长期 | 长期 |
| dedup | 整段覆盖 | 按 date | 经验去重 |
| archive/deletion | compress_memory 管理 | 同左 | 合规擦除 |
| event | （可选 MEMORY_SUMMARY_UPDATED，非必需） | — | — |

### C. User Model
| 维度 | 内容 |
|---|---|
| owner | `cognitive.user_model` |
| storage | `user_model` 表（profile KV 为 legacy 重叠，待 reconcile） |
| writer | `cognitive.user_model.upsert_user_model`、`cognitive.extractor` |
| reader | `UserModelSource`、`canonical_project`、`build_memory_block`（profile） |
| lifecycle | L1–L4 置信；低可信不覆盖高可信（iron rule） |
| retrieval | 经 `UserModelSource` 转 ContextItem（**不得拼提示词**） |
| confidence | L1–L4 分级 |
| dedup | 字段级合并（数组去重 / 字典浅更新） |
| archive | 版本保留 |
| deletion | 合规擦除 |
| event | （可选 USER_MODEL_UPDATED，非必需） |

### D. Conversation Record
| 维度 | chat_log | conversation_memories |
|---|---|---|
| owner | db / conversation 子系统 | conversation 子系统 |
| storage | `chat_log` | `conversation_memories` |
| writer | `db.save_turn` | 对话沉淀 |
| reader | build_memory_block(recent)、extractor(old)、compress_memory(prune) | memory_query |
| lifecycle | 旧轮次经 compress_memory 裁剪 | 追加 |
| retention | 滚动（MEM_KEEP 阈值） | 长期 |
| deletion | compress_memory 删旧轮次 | 合规擦除 |
| event | （可选 CHAT_LOG_*, 非 P4 范围） | — |

### E. Runtime State
| 维度 | 内容 |
|---|---|
| owner | 各运行时子系统（reminders/geo/foci/tasks/hotspot/prefetch） |
| storage | 各自表（reminders/geo/foci/tasks）或外部（hotspot/prefetch） |
| writer | 对应业务模块 |
| reader | `memory.build_context_prefix`（当前被 monolith 包入） |
| lifecycle | 瞬态（时间与任务驱动） |
| retrieval | **不经 Memory Retrieval**；由独立 RuntimeStateSource 注入 |
| archive/deletion | 业务自身管理 |
| event | （非 P4 Memory 范围） |

### F. Knowledge（见 P4-KNOWLEDGE-CONTRACT.md）
| 维度 | 内容 |
|---|---|
| owner | `knowledge.py`(facade) + `knowledge_runtime`(engine) |
| storage | `knowledge/*.md`（文件，非 DB） |
| writer | knowledge_runtime.ingest_document / 文件编辑 |
| reader | `KnowledgeSource` → `knowledge.search` |
| lifecycle | 文件版本（git-tracked） |
| retrieval | 关键词/结构化（无 RAG/向量） |
| archive/deletion | 文件管理 |
| event | 新增 `KNOWLEDGE_UPDATED` |

---

## 4. Memory API DESIGN（设计，不实现）

```mermaid
graph TD
    W[写入者] --> API[Canonical Memory API]
    API -->|CREATE/UPSERT| M[(memories)]
    API -->|ARCHIVE/RESTORE| M
    API -->|DELETE 逻辑| M
    API -->|VERIFY/LINK/MERGE/CONSOLIDATE| M
    API -->|GET/SEARCH/RETRIEVE| M
    R[读取者] --> API
```

| API | 分类 | 说明 |
|---|---|---|
| `memory.create` | **canonical** | 新建（content_hash 幂等） |
| `memory.get` | **canonical** | 按 id/条件读 |
| `memory.update` | **canonical** | 改字段（不碰 content_hash） |
| `memory.archive` / `restore` | **canonical** | 软归档/恢复 |
| `memory.delete` | **canonical** | 逻辑/合规擦除（tombstone） |
| `memory.search` | **canonical** | 模糊/结构化搜索（复用 memory_query） |
| `memory.retrieve` | **canonical** | 经 `DefaultEvolutionPolicy`（召回+排序+过滤） |
| `memory.consolidate` | **canonical** | 合并重复（保高置信） |
| `memory.record_learning` | **compatibility adapter** | 写 `learnings`（独立库，不进 memories） |
| `memory.record_episode` | **compatibility adapter** | 写 `episodes`（独立库） |
| `memory.record_user_model` | **compatibility adapter** | 写 `user_model` |
| 旧 `memory.build_memory_block` / `build_system_prompt` | **deprecated**（保留为回退） | 单体提示词；新引擎不再调用 |

**约束：**
- hotspots / notes / cognitive.extractor / memory_distiller **不得直接操作 `memories` 底层表**，必须经由 Canonical Memory API（经 `memory_evolution` 治理）。
- `memory_evolution` 为唯一治理门面；不得再建第二套 Memory API（红线 §4）。

---

## 5. 与 Retrieval / Context / Knowledge / Event 的关系

```mermaid
flowchart TB
    W[写入者] --> API[Canonical Memory API]
    API --> M[(memories)]
    M --> RET[DefaultEvolutionPolicy 统一检索]
    RET --> CE[context.facade 统一装配]
    M --> LC[lifecycle 状态机]
    M --> EB[(EventBus)]
    KN[knowledge/*.md] --> KRT[knowledge_runtime] --> CE
```

- Memory 不拼提示词 → 经 Retrieval → 经 Context Engine。
- Memory 不存知识 → Knowledge 走 knowledge_runtime 边界。
- 每次写/归档/衰减 → 经 EventBus 发事件。

---

## 6. 待 P4.2 落实的契约修正

1. distiller：弃用 `tags` 塞 confidence/source，改走 Canonical API 真实列。
2. 所有 `memories` 写入者统一经 Canonical Memory API（含 UPSERT 幂等）。
3. 固化 `content_hash` 为唯一幂等键（db.py 已建 UNIQUE）。
4. DELETE 仅逻辑/合规擦除，保留 tombstone，永不物理 DROP 带数据表。
5. `profile` vs `user_model` 重叠：canonical = user_model；profile 标记 legacy，经 canonical_project 冲突解析（已存在），后续 reconcile。

---

## 7. 安全边界

- Memory 模块**不直连** Agent / Tool / Executor（capability_runtime 仅由 server_handlers_chat / tools 调用，与 Memory 无关）。
- Memory 事件**不经** Executor；事件仅表域状态变化/观察。

---

## 8. 设计裁决（16 问节选）

| # | 问题 | 裁决 |
|---|---|---|
| 1 | Memory 权威数 | **1**（`memories` + `memory_evolution` 门面） |
| 6 | Memory Storage owner | `memories`（经 Canonical API + 治理） |
| 13 | 安全迁移 | ✓（幂等、无 DROP 带数据表） |
| 16 | Policy/Permission/Approval/Executor 不被绕过 | ✓ |

> 完整 16 问见主文档 `P4-MEMORY-CONTEXT-KNOWLEDGE-DESIGN.md` §DESIGN GATE。
